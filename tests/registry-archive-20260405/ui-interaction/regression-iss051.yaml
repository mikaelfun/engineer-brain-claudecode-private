id: "regression-iss051"
name: "Regression: ISS-051 — Agent Monitor Session 展开后看不到实时消息"
category: "ui-interaction"
source: "issues/ISS-051.json"
description: "回归测试：验证 ISS-051 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-051"]
regression_for: "ISS-051"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "`formatMessageForSSE` correctly extracts text from SDK structured content (array"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Persisted messages use frontend-compatible types (`thinking`, `tool-call`, `tool"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Chat route persists messages via `appendSessionMessage()`"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Chat route uses `getSSEEventType()` for correct event dispatch"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "TypeScript compiles without errors"

timeout_seconds: 60
