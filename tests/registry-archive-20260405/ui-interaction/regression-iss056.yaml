id: "regression-iss056"
name: "Regression: ISS-056 — UI Test 未根据 Issue 问题做功能复现验证，测试覆盖过于简单"
category: "ui-interaction"
source: "issues/ISS-056.json"
description: "回归测试：验证 ISS-056 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-056"]
regression_for: "ISS-056"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "workflow.md Step 2 includes a new 'Test Plan Generation' sub-step that reads the"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "A test classification system distinguishes between interaction-heavy Issues (but"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Test plan is shown to user before execution for confirmation"

timeout_seconds: 60
