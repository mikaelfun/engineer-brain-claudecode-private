id: "regression-iss048"
name: "Regression: ISS-048 — Agent Monitor Session 实时状态查看 + 交互控制"
category: "ui-interaction"
source: "issues/ISS-048.json"
description: "回归测试：验证 ISS-048 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-048"]
regression_for: "ISS-048"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Expanded panel shows real-time SSE message stream (thinking, tool-call, tool-res"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Message display reuses the same grouping/collapsing logic as CaseAIPanel (consec"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "On page refresh, persisted messages are recovered from the backend for case sess"

timeout_seconds: 60
