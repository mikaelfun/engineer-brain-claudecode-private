id: "regression-iss021"
name: "Regression: ISS-021 — case 2602130040001034的标题显示被截断"
category: "ui-interaction"
source: "issues/ISS-021.json"
description: "回归测试：验证 ISS-021 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-021"]
regression_for: "ISS-021"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Case titles containing `"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Other fields parsed from case-info.md tables still work correctly"

timeout_seconds: 60
