# Test: Architecture Compliance — POSIX Paths in SKILL.md
# Generated: Round 30 | Source: arch-scan
# Recipe: none

id: "arch-posix-paths-skill"
name: "SKILL.md files use POSIX paths (no Windows backslash paths)"
category: "workflow-e2e"
source: "tests/executors/architecture-scanner.sh"
safety_level: "safe"
description: "Verify SKILL.md files in .claude/skills/ do not contain Windows-style backslash paths, per CLAUDE.md Git Bash POSIX path requirement"

steps:
  - action: "bash"
    params:
      command: "grep -r '\\\\\\\\' .claude/skills/*/SKILL.md 2>/dev/null | grep -v '^Binary' | head -5 || echo 'NONE'"
      description: "Search for Windows-style paths in SKILL.md files"

assertions:
  - type: "stdout_equals"
    expected: "NONE"

metadata:
  generatedAt: "2026-03-30T17:20:00.000Z"
  round: 30
  gapId: "arch-posix-paths"
  source: "arch-scan"
  recipeUsed: null
