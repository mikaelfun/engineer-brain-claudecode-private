# Test: Matched CC emails are written to `casehealth-meta.json` (fields: `ccEmails`, `cc
# Generated: Cycle 0 | Source: spec-driven
# Recipe: none

id: "cc-finder_20260320-gap-20"
name: "Matched CC emails are written to `casehealth-meta.json` (fields: `ccEmails`, `cc"
category: "workflow-e2e"
source: "spec-driven"
description: "Matched CC emails are written to `casehealth-meta.json` (fields: `ccEmails`, `ccAccount`, `ccKnowMePage`)"
safety_level: "safe"
priority: "medium"
impact: "P2"
impactReason: "Spec AC 未覆盖"
tags: ["spec-driven","cc-finder"]

steps:
  - action: "validate"
    params:
      description: "Matched CC emails are written to `casehealth-meta.json` (fields: `ccEmails`, `ccAccount`, `ccKnowMeP"

assertions:
  - type: "file_exists"
    target: "cases/"
    expected: "true"
    description: "Matched CC emails are written to `casehealth-meta.json` (fields: `ccEmails`, `cc"

timeout_seconds: 30
metadata:
  generatedAt: "2026-04-05T00:45:22.682Z"
  round: 0
  gapId: "cc-finder_20260320-gap-20"
  recipeUsed: null
