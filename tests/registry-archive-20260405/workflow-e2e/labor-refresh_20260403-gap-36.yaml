# Test: `view-labor.ps1` 支持 `-TicketNumber` 和 `-OutputDir` 参数，输出 `labor.md` 到 case 目录
# Generated: Cycle 0 | Source: spec-driven
# Recipe: none

id: "labor-refresh_20260403-gap-36"
name: "`view-labor.ps1` 支持 `-TicketNumber` 和 `-OutputDir` 参数，输出 `labor.md` 到 case 目录"
category: "workflow-e2e"
source: "spec-driven"
description: "`view-labor.ps1` 支持 `-TicketNumber` 和 `-OutputDir` 参数，输出 `labor.md` 到 case 目录"
safety_level: "safe"
priority: "medium"
impact: "P2"
impactReason: "Spec AC 未覆盖"
tags: ["spec-driven","labor-refresh"]

steps:
  - action: "validate"
    params:
      description: "`view-labor.ps1` 支持 `-TicketNumber` 和 `-OutputDir` 参数，输出 `labor.md` 到 case 目录"

assertions:
  - type: "file_exists"
    target: "cases/"
    expected: "true"
    description: "`view-labor.ps1` 支持 `-TicketNumber` 和 `-OutputDir` 参数，输出 `labor.md` 到 case 目录"

timeout_seconds: 30
metadata:
  generatedAt: "2026-04-05T00:45:22.682Z"
  round: 0
  gapId: "labor-refresh_20260403-gap-36"
  recipeUsed: null
