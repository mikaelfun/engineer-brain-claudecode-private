# Test: inspection-writer adds a todo reminder with CC list when `ccEmails` exists
# Generated: Cycle 0 | Source: issue-driven
# Recipe: none

id: "ISS-009-gap-7"
name: "inspection-writer adds a todo reminder with CC list when `ccEmails` exists"
category: "workflow-e2e"
source: "issue-driven"
description: "inspection-writer adds a todo reminder with CC list when `ccEmails` exists"
safety_level: "safe"
priority: "medium"
impact: "P2"
impactReason: "缺失测试覆盖：workflow-e2e 功能"
tags: ["issue-driven","ISS-009"]

steps:
  - action: "validate"
    params:
      description: "inspection-writer adds a todo reminder with CC list when `ccEmails` exists"

assertions:
  - type: "file_exists"
    target: "cases/"
    expected: "true"
    description: "inspection-writer adds a todo reminder with CC list when `ccEmails` exists"

timeout_seconds: 30
metadata:
  generatedAt: "2026-04-05T00:45:22.682Z"
  round: 0
  gapId: "ISS-009-gap-7"
  recipeUsed: null
