# Test: `restartBackend()` 在 spawn 新进程前先 kill 旧端口进程
# Generated: Cycle 0 | Source: issue-driven
# Recipe: issue-driven-api

id: "ISS-089-gap-15"
name: "`restartBackend()` 在 spawn 新进程前先 kill 旧端口进程"
category: "backend-api"
source: "issue-driven"
description: "`restartBackend()` 在 spawn 新进程前先 kill 旧端口进程"
safety_level: "safe"
priority: "medium"
impact: "P2"
impactReason: "缺失测试覆盖：regression 功能"
tags: ["issue-driven","ISS-089"]

steps:
  - action: "http_request"
    params:
      description: "Verify backend is reachable"

assertions:
  - type: "status_code"
    target: "response"
    expected: "200"
    description: "`restartBackend()` 在 spawn 新进程前先 kill 旧端口进程"

timeout_seconds: 30
metadata:
  generatedAt: "2026-04-05T00:45:22.682Z"
  round: 0
  gapId: "ISS-089-gap-15"
  recipeUsed: "issue-driven-api"
