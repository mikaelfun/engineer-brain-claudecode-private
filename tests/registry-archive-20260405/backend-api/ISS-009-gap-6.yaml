# Test: email-drafter adds `CC:` header line in initial-response drafts when `ccEmails` 
# Generated: Cycle 0 | Source: issue-driven
# Recipe: issue-driven-api

id: "ISS-009-gap-6"
name: "email-drafter adds `CC:` header line in initial-response drafts when `ccEmails` "
category: "backend-api"
source: "issue-driven"
description: "email-drafter adds `CC:` header line in initial-response drafts when `ccEmails` exists"
safety_level: "safe"
priority: "medium"
impact: "P2"
impactReason: "缺失测试覆盖：backend-api 功能"
tags: ["issue-driven","ISS-009"]

steps:
  - action: "http_request"
    params:
      description: "Verify backend is reachable"

assertions:
  - type: "status_code"
    target: "response"
    expected: "200"
    description: "email-drafter adds `CC:` header line in initial-response drafts when `ccEmails` "

timeout_seconds: 30
metadata:
  generatedAt: "2026-04-05T00:45:22.682Z"
  round: 0
  gapId: "ISS-009-gap-6"
  recipeUsed: "issue-driven-api"
