id: "regression-iss083"
name: "Regression: ISS-083 — AI Assistant step 执行时应随时可发送消息，消息在 session 队列自动排队"
category: "ui-interaction"
source: "issues/ISS-083.json"
description: "回归测试：验证 ISS-083 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-083"]
regression_for: "ISS-083"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Chat input remains enabled during step execution (not disabled when `isStepActiv"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Queued messages are auto-sent sequentially when the step completes (session stat"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "If multiple messages are queued, they are sent one-by-one in order"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Queue is cleared and messages update to 'sent' status after processing"

timeout_seconds: 60
