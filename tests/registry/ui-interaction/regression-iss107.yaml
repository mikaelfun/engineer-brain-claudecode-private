id: "regression-iss107"
name: "Regression: ISS-107 — CaseAIPanel Timeline + Filter Tab Redesign"
category: "ui-interaction"
source: "issues/ISS-107.json"
description: "回归测试：验证 ISS-107 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-107"]
regression_for: "ISS-107"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "CaseAIPanel full mode message area uses `SessionMessageList` with `groupByStep` "
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Chat input always routes to the real backend session regardless of selected filt"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "`StepQuestionForm` extracted as shared component, usable in both CaseAIPanel and"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "`caseAssistantStore` executionId message bucketing removed; messages flow throug"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Compact mode remains functional: shows current active step + latest message prev"

timeout_seconds: 60
