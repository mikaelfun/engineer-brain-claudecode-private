id: "regression-iss018"
name: "Regression: ISS-018 — Issue中restart 前后端按钮，没有杀掉旧进程，导致前端端口被占用开了新端口"
category: "ui-interaction"
source: "issues/ISS-018.json"
description: "回归测试：验证 ISS-018 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-018"]
regression_for: "ISS-018"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Question dialog always appears when AskUserQuestion is intercepted, regardless o"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Backend always stores pendingQuestion even when sessionId is not yet available"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Answer submission works correctly even when sessionId was initially missing (bac"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Recovery endpoint always returns current pendingQuestion state"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "TrackProgressPanel recovery correctly handles all edge cases (question-before-mo"

timeout_seconds: 60
