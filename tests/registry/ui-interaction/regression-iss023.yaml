id: "regression-iss023"
name: "Regression: ISS-023 — CaseDetail AI Panel 需要更多单项操作按钮（刷新邮件、刷新Teams、拉取附件等）"
category: "ui-interaction"
source: "issues/ISS-023.json"
description: "回归测试：验证 ISS-023 实现的功能未退化"
safety_level: "safe"
priority: "medium"
tags: ["regression", "issue-driven", "iss-023"]
regression_for: "ISS-023"

steps:
  - action: "navigate"
    params:
      url: "{frontend_url}"
    safety_check: "ui.safe[navigate_to_page]"
  - action: "verify_element"
    params:
      selector: "body"
      expected: "visible"
      timeout: 5000

assertions:
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Buttons are visually grouped by category: Data (refresh, teams-search), Analysis"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Full Process button remains prominently placed as the primary action"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Each button shows its description on hover (tooltip)"
  - type: "text_contains"
    target: "page"
    expected: "visible"
    description: "Draft Email retains its split-button with email type dropdown"

timeout_seconds: 60
