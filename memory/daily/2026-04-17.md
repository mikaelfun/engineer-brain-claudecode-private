
## T2 Casework V2 — Assess Skill (CLOSED)

**交付**：`/casework:assess` sub-skill 完成 8-task TDD 交付，8 个 commit 全绿。

- **Wave 1 utilities**: compliance-hash / gate-subagents / write-execution-plan.py（各带 3 个单测，9/9 通过）
- **Wave 2 docs**: assess SKILL.md (10 节) + casework / casework-light / teams-search 引用迁移
- **Wave 3 smoke**: DELTA_EMPTY 零 LLM 快速路径端到端通过；DELTA_OK 延迟到真实 case

**踩坑**：
1. Subagent-driven 策略与 plan mode 不兼容——plan mode 快照会冻结到子 agent context，即便父 session ExitPlanMode 后新 spawn 的 agent 仍禁止写。**解法**：主 session 直接执行，只有 T2.3 通过 subagent 完成。
2. Git Bash heredoc + `set -e` 在 `/tmp/` 静默失败。**解法**：用 python3 做 I/O 彻底绕开。

**下一步 track**：T3 (casework/act) or T4 (casework/summarize)。
