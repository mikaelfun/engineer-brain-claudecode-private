# Daily Log — 2026-03-17

## 今日执行

### Casework 2603090040000814
- 客户: 梅赛德斯-奔驰 / 杨昭
- 问题: Azure RBAC unknown 用户删除失败（PIM + delegation conditions）
- actualStatus: pending-customer（4 天无回复）
- 21v Convert: 是 (20260309398206, owner zhang.lihong@oe.21vianet.com)
- IR: Succeeded | FDR: Expired | FWR: Expired
- 生成了 follow-up 邮件草稿

### 脚本修复 x3

1. **check-ir-status.ps1 — FDR/FWR unknown**
   - D365 UI 的 Expired 状态有两种 selector: `ViolatedTimerLabelID`（已覆盖）和 `UnsucceededLabelId`（遗漏）
   - FDR/FWR 用的是后者，导致全部返回 unknown
   - 两处 parseKpi 函数（行 ~58 和 ~90）都补上了 UnsucceededLabelId 检查

2. **fetch-emails.ps1 — 签名图片过滤 + cid 图片无后缀**
   - 新增 `Test-SignatureImage`: 按文件名模式（logo/icon/linkedin/image00x.png 等）和大小（< 2KB）跳过签名/图标
   - 新增 `Ensure-Extension`: cid 图片没后缀时从 MIME type 自动补（inlineimg_XXX → inlineimg_XXX.jpg）
   - 新增 `Get-ImageExtension`: MIME type → 扩展名映射
   - data URI 图片也加了 estimatedBytes < 2KB 跳过
   - ⚠️ 已做单元测试（12/12 通过），但未端到端验证（D365 session 过期）

3. **write-teams.ps1 — 新建替代 fetch-teams.ps1**
   - fetch-teams.ps1 依赖 mcporter（Openclaw），Claude Code 无法使用
   - 拆分为: MCP 搜索由 subagent 直接调用 → JSON 结果传给 write-teams.ps1 写文件
   - 修复 DateTimeOffset 时区 bug: ConvertFrom-Json 把 "...Z" 解析为 DateTime(Kind=Utc)，但 ToString() 丢失 Z 后缀，导致 DateTimeOffset::Parse 当成本地时间
   - 已删除旧的 fetch-teams.ps1

### 待后续验证
- fetch-emails.ps1 的图片修复需要在 D365 session 正常时端到端验证
- download-attachments.ps1 的 DTM token 获取仍依赖 playwright session
