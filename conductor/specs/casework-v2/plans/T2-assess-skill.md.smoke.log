# T2 Assess Skill — Smoke Log

Date: 2026-04-17
Executor: Main session (subagent strategy abandoned due to plan-mode inheritance)

## Wave 1 Regression (utilities + schema)
- compliance-hash: OK (3/3)
- gate-subagents:  OK (3/3)
- write-execution-plan: OK (3/3)

## Wave 2 Doc Integrity
- assess SKILL.md sections: 10 (>= 6 required)
- casework SKILL references /casework:assess: ✓
- casework-light references onenote-classifier: ✓
- teams-search references teams-digest-writer: ✓

## Wave 3 Smoke — DELTA_EMPTY fast path
Fixture → extract deltaStatus=DELTA_EMPTY → reuse meta.actualStatus=pending-customer
→ invoke write-execution-plan.py with actions=[] routingSource=rule-table
→ PLAN_WRITTEN, schema assertions pass.
Result: SMOKE_OK.

## Wave 3 Smoke — DELTA_OK path
Owner-driven, deferred to next real-case encounter (by design — requires live LLM).

## Commits
5c57602 T2.3 write-execution-plan
2a79e9a T2.1 compliance-hash
ba7f338 T2.2 gate-subagents
cb5bae7 T2.4 assess SKILL.md
cf39b1e T2.5 casework SKILL delegation
08025ea T2.6 casework-light OneNote migration
5008fd2 T2.7 teams-search digest notice

T2 track CLOSED.

---

## Wave 3 Smoke — DELTA_OK (real case)

Date: 2026-04-17
Case: 2604140040001804 (Mooncake VM PoD / 华晨宝马)
Procedure: backup → clean .casework/ → run data-refresh.sh → exec assess Steps 2-6.

### Results
- data-refresh: **DATA_REFRESH_OK | delta=DELTA_OK | elapsed=106.1s** (teams path DEGRADED, others OK)
- Step 2 compliance gate: re-infer path (cache empty)
- Step 3 gating: SPAWN_TEAMS=0 SPAWN_ONENOTE=1 (matches plan — teams.newMessages=0, onenote.newPages=1)
- Step 4 decision (inline main-LLM, no subagent for smoke): actualStatus=pending-customer, days=3, actions=[email-drafter follow-up]
- Step 4b write-execution-plan.py: **PLAN_WRITTEN | actions=1** — schema validates
- Step 5 meta upsert: OK
- **Smoke Signal**: `ASSESS_SMOKE_OK|delta=DELTA_OK|status=pending-customer|actions=1`

### Issues found (to track)
1. **compliance-hash.sh field mismatch** — T2.1 fixture uses `| Entitlement | Premier |` rows, real case-info.md uses `## Entitlement\n| Service Level | Premier |`. Hash returns empty → all real cases hit re-infer. Needs fixture + grep pattern update.
2. **Git Bash `/tmp/` path instability** — passing `/tmp/foo.json` between MSYS Bash and Python causes Windows Python to resolve it as `C:\Users\fangkun\AppData\Local\Temp\...`. Workaround applied: put decision tmp file under `{caseDir}/.casework/`.

### Rollback
Case restored from `_backup/2604140040001804.1776404088/`. Only casework-meta.json + execution-plan.json were changed; regenerated `.casework/` left in place as T1 artifact (data-refresh re-ran cleanly).

### DELTA_EMPTY path (prior entry)
Already validated via tempdir fixture smoke (see first log entry). Fast path zero-LLM confirmed.

---

## T2.9 Follow-up Fixes (real-case smoke debt)

### T2.9.1 compliance-hash field alignment — d4d5e54
- Real case-info.md uses `## Entitlement` heading + `| Service Level | Premier |`, not `| Entitlement | Premier |` row.
- Utility now extracts `Service Level` + `SAP` (Support Plan never existed in prod, removed).
- Fixtures replaced with real-format `case-info-real-{premier,prodirect}.md`.
- Regression test verifies prod case 2604140040001804 → hash `c728508b` (non-empty).

### T2.9.2 gate-subagents degraded flags — a002574
- Old: teams.status=FAILED + newMessages=0 → SPAWN_TEAMS=0 (silent skip, indistinguishable from empty).
- New: also emit `TEAMS_DEGRADED=1`/`ONENOTE_DEGRADED=1` when status in {FAILED,TIMEOUT}.
- Fixture `data-refresh-output-teams-failed.json` pins the contract.

### T2.9.3 SKILL.md pitfalls section — 6a00129
- Step 3 snippet updated to show 4-flag output.
- Pitfalls: `/tmp/` path drift (decision tmp must live under `{caseDir}/.casework/`),
  compliance-hash field names, degraded-source handling.

### T2.9.4 Teams search retry bug — ISS-216 filed
- RCA: `head -1` in `grep -o 'data: {.*}' | sed | head -1` discards valid JSON-RPC
  result when agency emits auth-challenge SSE frame first.
- Scope: `.claude/skills/teams-search/scripts/teams-search-inline.sh` (out of T2 assess).
