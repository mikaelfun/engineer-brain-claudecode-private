# Implementation Plan: Fix Case Title Truncation When Title Contains Pipe Characters

**Track ID:** title-truncate-fix_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

Fix `extractTableValue()` in `case-reader.ts` to handle values containing `|` characters. The current regex `([^|]*)` stops at the first pipe; we need to capture everything between the key column and the last `|` on the line.

## Phase 1: Fix Parser

Fix the core parsing logic.

### Tasks

- [x] Task 1.1: Update `extractTableValue()` in `dashboard/src/services/case-reader.ts` — change regex to capture from key's trailing `|` to the line's last `|`, preserving internal pipes in the value
- [x] Task 1.2: Verify the fix handles edge cases: title with 0, 1, multiple `|` characters; title as first/last/middle row; empty value

### Verification

- [x] `npm test` passes (existing tests still work)
- [x] Manual check: parse case 2602130040001034's case-info.md and confirm full title extracted

## Phase 2: Testing & Completion

### Tasks

- [x] Task 2.1: Add/update unit tests in `dashboard/src/services/case-reader.test.ts` covering pipe-in-value scenarios
- [x] Task 2.2: Run full test suite, verify no regressions

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Case titles containing `|` are displayed in full on the Case Detail header | Visual | Navigate to page → verify element is visible with correct content |
| 2 | Other fields parsed from case-info.md tables still work correctly | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | The fix handles edge cases: title with multiple `|`, title as last row, title... | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] 关联 Issue JSON 状态已更新 (ISS-021 → done)
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
