# Implementation Plan: Dashboard API 返回 agent executionSummary

**Track ID:** agent-log-visible_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [x] Complete

## Overview

定义 ExecutionSummary TypeScript 接口，修改 case-session-manager.ts 从 SDK session result 收集 tool calls、MCP servers、agent type、turns 数据，然后修改 API response 格式附加 executionSummary 字段。

## Phase 1: 定义 ExecutionSummary TypeScript 接口

创建类型定义，明确数据结构。

### Tasks

- [x] Task 1.1: 定义 `ExecutionSummary` 接口（agentType, turns, mcpServers, toolCalls）
- [x] Task 1.2: 定义 `ToolCallRecord` 接口（name, success, error?）
- [x] Task 1.3: 确定接口文件位置（dashboard/src/types/ 或 inline）

### Verification

- [x] TypeScript 类型定义编译通过

## Phase 2: 修改 case-session-manager.ts 收集 summary 数据

在 session 执行后从 SDK result 提取执行数据。

### Tasks

- [x] Task 2.1: 分析 Claude Agent SDK session result 结构，确认可提取的字段
- [x] Task 2.2: 在 session 执行完成后收集 toolCalls 数据（遍历 result messages 提取 tool_use/tool_result）
- [x] Task 2.3: 收集 mcpServers 数据（从 session config 或 result 提取实际可用 MCP 列表）
- [x] Task 2.4: 收集 agentType 和 turns 数据
- [x] Task 2.5: 封装为 ExecutionSummary 对象

### Verification

- [x] ExecutionSummary 对象正确包含所有字段
- [x] tool calls 的 success/fail 状态正确

## Phase 3: 修改 API response 格式

在现有 API response 中添加 executionSummary 字段。

### Tasks

- [x] Task 3.1: 修改 case process/chat/step API route handler，将 executionSummary 附加到 response
- [x] Task 3.2: 确保 executionSummary 是 additive（不改变现有字段结构）
- [x] Task 3.3: 添加 executionSummary 为 optional 字段（如果收集失败，返回 null 而非报错）

### Verification

- [ ] API response 包含 executionSummary 字段
- [ ] 现有 API 消费者不受影响

## Phase 4: 单元测试

添加单元测试覆盖 summary 收集和 API 响应。

### Tasks

- [ ] Task 4.1: 测试 ExecutionSummary 构建逻辑 — mock SDK result → 验证 summary 字段
- [ ] Task 4.2: 测试 API response 格式 — 验证 executionSummary 存在且结构正确
- [ ] Task 4.3: 测试 error case — SDK result 异常时 executionSummary 为 null

### Verification

- [ ] 单元测试全部通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: API response 含 executionSummary | API | POST /api/case/:id/process → 验证 response 含 executionSummary 字段 |
| 2 | AC2: toolCalls 数组 | API | 执行含 tool 调用的 case → 验证 toolCalls 数组非空，每项有 name/success |
| 3 | AC3: mcpServers 列表 | API | 执行含 MCP 的 case → 验证 mcpServers 列表非空 |
| 4 | AC4: agentType 和 turns | API | 执行 case → 验证 agentType 为 string，turns 为 number > 0 |
| 5 | AC5: 不影响现有消费者 | API | 执行现有 API 调用 → 验证原有字段不变，executionSummary 为新增 |

**Test Type Legend:**
- **API** — Call API endpoint → verify response structure + status code

## Post-Implementation Checklist

- [ ] ExecutionSummary 接口已定义
- [ ] case-session-manager.ts 已修改收集 summary
- [ ] API response 已添加 executionSummary 字段
- [ ] 单元测试已添加并通过
- [ ] TypeScript 编译成功
- [ ] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
