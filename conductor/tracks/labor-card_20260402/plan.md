# Implementation Plan: Case 详情页 Labor 估算卡片

**Track ID:** labor-card_20260402
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-02
**Status:** [ ] Not Started

## Overview

新建 `LaborEstimateCard` 组件，嵌入 CaseDetail 右侧边栏。使用现有 API hooks，无后端改动。

## Phase 1: LaborEstimateCard 组件

### Tasks

- [ ] Task 1.1: 创建 `LaborEstimateCard.tsx` 组件
  - 使用 `useLaborEstimate(caseNumber)` 获取数据
  - 三种状态：无数据（Estimate 按钮）、pending（可编辑 + Write/Dismiss）、submitted（只读确认）
  - 可编辑 totalMinutes input（number 类型，blur 时 PUT 保存）
  - classification badge + description 摘要（截断 2 行）
  - "Write to D365" 按钮（调用 submit mutation，loading 状态）
  - "Dismiss" 按钮（隐藏卡片，sessionStorage 记录）
  - "Estimate" 按钮（无数据时，调用 trigger mutation，loading 状态）

- [ ] Task 1.2: 在 CaseDetail.tsx 右侧边栏集成
  - 在 `<CaseAIPanel mode="compact" />` 下方添加 `<LaborEstimateCard caseNumber={id} />`
  - 仅在 xl+ 屏幕的侧边栏显示（与 CaseAIPanel 一致）

- [ ] Task 1.3: 暗色/浅色主题适配
  - 使用 design system CSS 变量（参考 dashboard-design-system.md）
  - 卡片背景、边框、文字颜色适配双主题

### Verification

- [ ] TypeScript 编译通过
- [ ] 截图验证卡片在右侧边栏正确显示

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 右侧边栏显示 LaborEstimateCard | Visual | 导航到 case 详情页 → 截图验证卡片在 AI Panel 下方 |
| 2 | 显示估算数据（分钟/分类/描述） | API + Visual | 构造 labor-estimate.json → API 验证数据 → 截图验证渲染 |
| 3 | 时间值可编辑 | Interaction | 点击时间 input → 修改值 → blur → 验证 PUT API 调用 |
| 4 | Write to D365 按钮 | Skip | D365 write operation — 安全红线 |
| 5 | Dismiss 隐藏卡片 | Interaction | 点击 Dismiss → 验证卡片隐藏 → 刷新页面验证恢复 |
| 6 | 无数据时显示 Estimate 按钮 | Visual | 删除 labor-estimate.json → 刷新 → 截图验证 Estimate 按钮 |
| 7 | 双主题适配 | Visual | 切换暗色/浅色 → 截图对比 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor._
