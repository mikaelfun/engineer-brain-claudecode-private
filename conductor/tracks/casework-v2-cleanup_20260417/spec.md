# Specification: casework SKILL.md v1 残留清理

**Track ID:** casework-v2-cleanup_20260417
**Type:** Refactor
**Created:** 2026-04-17
**Status:** Draft

## Summary

清理 casework/SKILL.md 中 61% 的 v1 残留代码（changegate/fast-path/Agent Probe/QUEUE_MODE/B0-B5 路由/AR PATH v1），重写为纯 v2 四步编排器（data-refresh → assess → act → summarize），从 623 行压缩到 ~90 行。同时删除 PRD §6 明确标记删除的 v1 脚本文件。

## Context

PRD §2.2 明确取消 changegate，§1 目标 #5 要求消除 changegate/fast-path/Agent Probe/timing 样板代码。v2 四步子 skill（assess/act/summarize）已交付并通过冷启动 patrol 冒烟验证。casework/SKILL.md 中的 v1 代码路径在 patrol 模式下从未被执行（patrol spawn 的 casework agent 直接走 data-refresh.sh + assess），但在 full mode 下 LLM agent 仍可能读到并尝试执行 v1 路径，造成混淆。

## Acceptance Criteria

- [ ] casework/SKILL.md 总行数 < 120 行（PRD 目标 200，实际可控 90-120）
- [ ] 所有 changegate/fast-path/Agent Probe/QUEUE_MODE/DIRECT_MODE 引用删除
- [ ] frontmatter steps 更新为 v2 四步（data-refresh/assess/act/summarize）
- [ ] v1 路径 A（NO_CHANGE → fast-path）和路径 B（CHANGED → B0-B5）全部删除
- [ ] AR PATH v1 段删除，AR 分路逻辑保留为简洁的 mode 参数说明
- [ ] v1 脚本 casework-fast-path.sh、casework-timing.sh 删除
- [ ] patrol 冷启动 + 热启动行为不变（patrol 走 data-refresh.sh + assess，不经过 casework SKILL.md 的 Step 1）
- [ ] 单 case `/casework {caseNumber}` full mode 走 v2 四步路径

## Dependencies

- assess/SKILL.md（已交付，本 session 更新了 Step 3 inline 设计）
- act/SKILL.md（已交付）
- summarize/SKILL.md（已交付）
- data-refresh.sh（已交付，本 session 修复了 Teams 路径 + ICM cold-start）

## Out of Scope

- casework SKILL.md 中 Step 4（inspection-writer 引用）的 v2 替代——已由 summarize/SKILL.md 完成
- patrol/SKILL.md 重写——patrol 的 v1 残留较少（仅注释级），不在本 track
- v1 独立 skill 目录清理（status-judge/compliance-check 等已删除）
- casework-ar/SKILL.md 独立化——PRD §2.7 设计，作为后续 track

## Technical Notes

- casework/SKILL.md 是 LLM agent 的 prompt 文件（非代码），重写 = 重新组织 markdown 指令
- v2 四步子 skill 已各自独立可执行，casework/SKILL.md 只需引用，不复制内容
- AR 检测逻辑（case number 19 位判断）保留在 Step 1，作为 data-refresh 参数传递
- 归档/转移检测（原 B0）已由 patrol 统一处理，casework full mode 不需要

---

_Generated by Conductor. Review and edit as needed._
