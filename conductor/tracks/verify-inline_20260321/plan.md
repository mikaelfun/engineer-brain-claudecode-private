# Implementation Plan: Verify 内联进度展示

**Track ID:** verify-inline_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [~] In Progress

## Overview

将 verify 从同步请求+modal 改为异步执行+SSE inline panel。后端拆为异步 fire-and-forget + SSE 广播，前端新增 VerifyProgressPanel + Zustand store 状态 + useSSE 监听。

## Phase 1: 后端异步化 + SSE 事件

将 verify 后端从同步改为异步，通过 SSE 推送实时进度。

### Tasks

- [x] Task 1.1: 新增 verify session manager（类似 implement-session-manager），管理 verify 锁和消息存储
- [x] Task 1.2: 重写 `POST /api/issues/:id/verify` 为异步 fire-and-forget，执行中通过 SSE 广播 `issue-verify-started`、`issue-verify-progress`、`issue-verify-completed`、`issue-verify-error`
- [x] Task 1.3: 新增 `GET /api/issues/:id/verify-status` 端点，返回 verify 进度消息（刷新恢复用）

### Verification

- [x] TypeScript 编译通过
- [x] 后端启动无报错

## Phase 2: 前端 inline panel

在 Issues 页面中用 inline panel 替代 loading overlay + 结果 modal。

### Tasks

- [x] Task 2.1: 在 `issueTrackStore` 中新增 verify 相关状态（verifyMessages、activeVerify、verifyResult）
- [x] Task 2.2: 在 `useSSE` hook 中新增 `issue-verify-*` 事件监听，更新 store
- [x] Task 2.3: 创建 `VerifyProgressPanel` 组件，展示 verify 实时进度和结果
- [x] Task 2.4: 修改 `Issues.tsx`，移除 verify overlay/modal，改用 inline VerifyProgressPanel

### Verification

- [x] TypeScript 编译通过（前端 + 后端）
- [ ] 单元测试通过

## Phase 3: 测试 + 收尾

### Tasks

- [ ] Task 3.1: 从 WebUI 点击 Verify 测试 inline 进度展示
- [ ] Task 3.2: 测试页面刷新后 verify 状态恢复

### Verification

- [ ] 单元测试通过

## Post-Implementation Checklist

- [ ] 单元测试文件已创建并通过
- [ ] 关联 Issue JSON (ISS-037) 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
