# Implementation Plan: State Tracking Authoritative Status

**Track ID:** state-authority_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [~] In Progress

## Overview

Add authoritative `pipelineStatus` and `status` fields to pipeline.json and supervisor.json respectively. Update the two agent definitions (stage-worker, supervisor-runner) to explicitly write these fields on start/end. Then update all three consumers (dashboard-renderer, health-check, WebUI SSE) to read authoritative fields instead of heuristic inference.

## Phase 1: State Model — Add Authoritative Fields to Agents

Add explicit status writes to the two sources of state mutations.

### Tasks

- [x] Task 1.1: Update `test-supervisor-runner.md` — add `echo '{"status":"running"}' | bash state-writer.sh --target supervisor --merge` at session start (after active write), and ensure `echo '{"status":"idle","step":null,"active":null}' | bash state-writer.sh --target supervisor --merge` at session end (already partially exists, ensure `status` is included)
- [x] Task 1.2: Update stage-worker agent — add `echo '{"pipelineStatus":"running"}' | bash state-writer.sh --target pipeline --merge` at stage start, and `echo '{"pipelineStatus":"idle"}' | bash state-writer.sh --target pipeline --merge` at stage completion/error
- [x] Task 1.3: Ensure current supervisor.json and pipeline.json have correct initial values (`status: "idle"`, `pipelineStatus: "idle"`) — fix current stale state

### Verification

- [ ] Run `/test-supervisor run`, check supervisor.json shows `status: "running"` during execution, `status: "idle"` after
- [ ] Check pipeline.json shows `pipelineStatus: "running"` during stage-worker execution, `"idle"` after

## Phase 2: Consumer Updates — Dashboard, Health-Check, WebUI

Update all three consumers to read authoritative fields.

### Tasks

- [x] Task 2.1: Update `dashboard-renderer.sh` — replace heuristic `runnerActive` detection (lines 119-124: `sup.active` timestamp check) with `sup.status === 'running' || pl.pipelineStatus === 'running'`; update health logic (line 132-137) to use authoritative status
- [x] Task 2.2: Update `dashboard-renderer.sh` supervisor step rendering — detect "idle" state (step=null + status=idle) and render all steps as ✅ or show "idle" instead of frozen 🔶
- [x] Task 2.3: Update `health-check.sh` — N/A, health-check uses currentStage not timestamp heuristic, no change needed
- [x] Task 2.4: Update WebUI SSE endpoint — N/A, file-watcher broadcasts full JSON on change, new fields flow through automatically

### Verification

- [ ] When supervisor+stage-worker both idle: dashboard shows `✅ IDLE`, not `🏃 RUNNING`
- [ ] When supervisor running: dashboard shows `🏃 RUNNING` with correct supervisor step
- [ ] When stage-worker running: dashboard shows stage progress with `🏃 RUNNING`
- [ ] WebUI TestLab page reflects correct idle/running state via SSE

## Phase 3: Regression Testing

### Tasks

- [ ] Task 3.1: Run full `/test-supervisor run` cycle and verify dashboard shows correct transitions: IDLE → RUNNING → stage progress → IDLE
- [ ] Task 3.2: Verify `/test-supervisor db` output matches actual state at each point
- [ ] Task 3.3: Commit all changes

### Verification

- [ ] End-to-end: dashboard status matches JSON state at all times

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | pipeline.json has `pipelineStatus` field | E2E | Read pipeline.json → verify field exists with value `idle` or `running` |
| 2 | supervisor.json `status` correctly written | E2E | Run supervisor-runner → check status=running during execution → check status=idle after |
| 3 | stage-worker writes pipelineStatus | E2E | Run stage-worker → check pipelineStatus=running during → idle after |
| 4 | dashboard shows IDLE when idle | E2E | Ensure both idle → run dashboard-renderer.sh → verify output contains `IDLE` not `RUNNING` |
| 5 | dashboard shows RUNNING when running | E2E | During active run → dashboard-renderer.sh → verify `RUNNING` |
| 6 | WebUI SSE reflects authoritative status | API | Start backend → GET /api/test-runner/status → verify response uses authoritative fields |
| 7 | health-check uses authoritative fields | E2E | Run health-check.sh when idle → verify no false "running" detection |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
