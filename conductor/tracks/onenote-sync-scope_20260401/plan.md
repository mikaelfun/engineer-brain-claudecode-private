# Implementation Plan: OneNote Sync 范围记忆 + Content Hash 增量校验

**Track ID:** onenote-sync-scope_20260401
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-01
**Status:** [x] Complete

## Overview

分两个独立改动：1) SKILL.md sync 流程增加 scope 文件读写逻辑；2) PS1 脚本增加 content hash 校验。两者互不依赖，可独立验证。

## Phase 1: Export Scope 记忆

在每次导出后记录范围，sync 时读取并应用。

### Tasks

- [x] Task 1.1: 修改 SKILL.md — 导出完成后（Step 5 之后），写入 `.export-scope.json` 到 notebook 输出目录。记录 notebookName、sections（从 `-SectionPath` 提取，null 表示全量）、lastSync 时间戳
- [x] Task 1.2: 修改 SKILL.md Sync 模式（Step 2.1c）— 逐个 notebook 导出前，读取 `.export-scope.json`。有 sections 列表时对每个 section 分别调用脚本（传 `-SectionPath`）；sections 为 null 时不传 filter（全量同步）
- [x] Task 1.3: 修改 SKILL.md Sync 模式 — 无 `.export-scope.json` 的 notebook，用 AskUserQuestion 提示用户选择：全量同步 / 选择 sections / 跳过。选择后写入 scope 文件
- [x] Task 1.4: 为现有已导出 notebook 生成初始 `.export-scope.json`（Kun Fang OneNote: sections=null，MCVKB: sections=null）

### Verification

- [ ] 手动导出带 `-SectionPath` 后，scope 文件正确记录 sections
- [ ] sync 读取 scope 文件后只同步指定 sections

## Phase 2: Content Hash 增量校验

PS1 脚本增加 hash 比较，避免 timestamp 抖动导致的假增量。

### Tasks

- [x] Task 2.1: 修改 export-onenote.ps1 — 在 frontmatter 生成时增加 `content_hash:` 字段，存储正文（frontmatter 之后）的 MD5 hash
- [x] Task 2.2: 修改 export-onenote.ps1 增量检测逻辑（line 721-728）— timestamp 不同时，读取本地文件的 `content_hash:`，计算新导出内容的 hash，hash 相同则只更新 frontmatter（不重写正文），hash 不同则正常全量写入
- [x] Task 2.3: 添加 `Get-FrontmatterContentHash` 函数 — 从本地 .md 文件 frontmatter 读取 `content_hash:` 值
- [x] Task 2.4: 添加 `Get-ContentHash` 函数 — 计算 markdown 字符串的 MD5 hash

### Verification

- [ ] timestamp 相同 → SKIP（现有行为不变）
- [ ] timestamp 不同 + hash 相同 → 只更新 frontmatter，日志显示 "UPDATE (timestamp only)"
- [ ] timestamp 不同 + hash 不同 → 全量重导出，日志显示 "OK"

## Phase 3: 测试验证

### Tasks

- [ ] Task 3.1: 对 Kun Fang OneNote 执行 sync，验证 324 个之前假增量的页面现在显示 "UPDATE (timestamp only)" 而非 "OK"
- [ ] Task 3.2: 确认 sync 遵循 scope 文件，不拉入 scope 之外的 sections

### Verification

- [ ] sync 完成后 git diff 只显示 frontmatter 变化（无正文变化）
- [ ] 无 scope 之外的新文件产生

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 导出后写入 .export-scope.json | E2E | 用 -SectionPath 导出 → 检查 scope 文件存在且 sections 正确 |
| 2 | sync 读取 scope 并按 sections 过滤 | E2E | 写入 scope 含 2 个 section → 运行 sync → 验证脚本只收到对应 -SectionPath |
| 3 | 无 scope 文件时提示确认 | Skip | 需要 AskUserQuestion 交互，无法自动化 |
| 4 | hash 相同时只更新 frontmatter | E2E | 手动修改 .md 的 modified: → 运行脚本 → 验证正文不变、modified: 更新、content_hash: 不变 |
| 5 | hash 不同时全量重导出 | E2E | 手动修改 .md 正文 → 运行脚本 → 验证正文被覆盖、content_hash: 更新 |
| 6 | frontmatter 含 content_hash 字段 | E2E | 导出任意页面 → 检查 frontmatter 含 content_hash: 且值为 32 字符 hex |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] export-onenote.ps1 增量逻辑已更新并通过手动测试
- [ ] SKILL.md sync 流程已更新
- [ ] 现有 notebook 的 .export-scope.json 已生成
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
