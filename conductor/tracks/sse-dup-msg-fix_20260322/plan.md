# Implementation Plan: Fix SSE Duplicate Messages in AI Assistant Panel

**Track ID:** sse-dup-msg-fix_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Fix duplicate SSE message display in CaseAIPanel by: (1) eliminating backend dual terminal event emission, (2) adding store-level de-duplication, (3) preventing multi-instance recovery hydration.

## Phase 1: Backend — Eliminate Duplicate Terminal Events

Fix step routes to emit only one terminal event per step lifecycle transition.

### Tasks

- [x] Task 1.1: In `steps.ts`, remove the raw `case-session-completed` broadcast after `case-step-completed` for step completion — keep only `case-step-completed`
- [x] Task 1.2: In `steps.ts`, fix step cancel to emit only `case-step-failed` — remove the extra `case-session-completed` broadcast
- [x] Task 1.3: Review `case-step-progress` semantic events vs raw `case-session-*` fallback in step message processing — ensure no overlapping events for the same SDK message

### Verification

- [ ] Run a step execution and confirm only one terminal SSE event is broadcast per step

## Phase 2: Frontend Store — Add De-duplication Guard

Add a lightweight de-duplication mechanism to `caseSessionStore.addMessage()`.

### Tasks

- [x] Task 2.1: In `caseSessionStore.ts`, add content+timestamp based de-duplication in `addMessage()` — skip if last N messages contain same type+content
- [x] Task 2.2: Apply same de-duplication guard to `addSessionMessage()` for per-session bucket

### Verification

- [ ] Unit test: adding duplicate message is silently dropped
- [ ] Unit test: distinct messages with same type but different content are kept

## Phase 3: Frontend — Prevent Multi-Instance Recovery

Ensure only one CaseAIPanel instance performs recovery hydration per case.

### Tasks

- [x] Task 3.1: In `CaseDetail.tsx`, ensure only the primary (full-size) CaseAIPanel mounts recovery effects — pass a `skipRecovery` prop to the compact panel instance
- [x] Task 3.2: In `CaseAIPanel.tsx`, respect `skipRecovery` prop to skip persisted message and step-progress recovery effects

### Verification

- [ ] On xl screens with AI tab active, only one set of recovered messages appears in the timeline
- [ ] TypeScript compiles: `npx tsc --noEmit`

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Step completion no longer produces two terminal messages | Skip | Backend-only fix, verify via server logs during manual test |
| 2 | Only one CaseAIPanel instance performs recovery hydration per case | Skip | Structural code change, covered by unit tests |
| 3 | Store-level de-duplication prevents identical messages appearing twice | Skip | Covered by unit tests on caseSessionStore |
| 4 | Step cancel produces single terminal message | Skip | Backend-only fix, verify via manual test |
| 5 | Agent Monitor continues to work correctly | Visual | Navigate to Agent Monitor → view a session → screenshot → verify messages display normally |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
