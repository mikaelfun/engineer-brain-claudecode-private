# Implementation Plan: Track Progress Auto-Collapse After Completion

**Track ID:** track-autocollapse_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

在 TrackProgressPanel 中，track 创建完成后自动折叠消息流区域，只显示 header 状态行 + 展开按钮。创建中/等待问题/错误时保持展开。

## Phase 1: 自动折叠逻辑 + UI 改造

### Tasks

- [x] Task 1.1: 添加 `collapsed` state — 默认 false，当检测到 `completedMsg` 且无 `isActive`/`pendingQuestion` 时自动设为 true
- [x] Task 1.2: 改造 Header — 右侧增加 History + ChevronRight/Down toggle 按钮，完成后 header 内联显示 trackId
- [x] Task 1.3: 条件渲染消息流 — collapsed 时隐藏消息流 div + PlanSummary + QuestionForm，只保留 header 和 toggle

### Verification

- [x] TypeScript 编译通过：`npx tsc --noEmit`

## Phase 2: 测试

### Tasks

- [x] Task 2.1: 确保现有 processTrackMessages 测试仍通过
- [x] Task 2.2: `npm run test:web` 全部通过（50 tests, 4 files）

### Verification

- [x] `npm run test:web` 全部通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Track created 后，消息流区域自动折叠，只显示 header 行（✅ Track Created + trackId） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Header 右侧有展开/折叠按钮，点击可查看之前的完整状态流记录 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 创建进行中（isActive）和等待问题（pendingQuestion）时保持展开 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | Error 状态保持展开（需要看到错误信息和 Retry 按钮） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | TypeScript 编译通过 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 单元测试通过 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
