# Implementation Plan: Fix WebUI Issue Track Button

**Track ID:** issue-track-fix_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

修改 `create-track` 路由为异步 Claude session spawn 模式，与 `start-implement` 保持一致。添加 SSE 事件类型支持。

## Phase 1: Backend — 重写 create-track 路由

### Tasks

- [x] Task 1.1: 在 `dashboard/src/types/index.ts` 的 SSEEventType 添加 4 个 track 创建事件类型：`issue-track-started`、`issue-track-progress`、`issue-track-completed`、`issue-track-error`
- [x] Task 1.2: 重写 `dashboard/src/routes/issues.ts` 的 `create-track` 路由 — 移除 `createTrackFromIssue()` 调用，改为异步 spawn Claude session 执行 `/conductor:new-track`，通过 SSE 广播进度
- [x] Task 1.3: TypeScript 编译验证

### Verification

- [x] TypeScript 前后端编译通过

## Final Verification

- [x] create-track 路由 spawn Claude session
- [x] SSE 事件类型正确定义
- [x] TypeScript 前后端编译通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | "Create Track" 按钮触发后端 spawn Claude session 执行 `/conductor:new-track` | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 通过 SSE 广播 track 创建进度 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 创建完成后 issue 自动更新 trackId 和 status | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | SSEEventType 包含 track 创建相关事件类型 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | TypeScript 编译无错误 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
