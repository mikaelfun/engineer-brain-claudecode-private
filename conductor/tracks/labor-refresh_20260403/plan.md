# Implementation Plan: data-refresh 增加拉取 D365 Labor 记录

**Track ID:** labor-refresh_20260403
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-03
**Status:** [x] Complete

## Overview

分三阶段：增强 PowerShell 脚本支持文件输出 → 更新 data-refresh 流程 → 更新 labor-estimate 流程 + schema。

## Phase 1: 增强 view-labor.ps1 脚本

为 `view-labor.ps1` 添加 `-TicketNumber` 和 `-OutputDir` 参数，支持将 labor 记录输出为 Markdown 文件。

### Tasks

- [x] Task 1.1: 添加 `-TicketNumber` 参数 — 提供时直接用该值查询 D365 API（跳过 `Get-CurrentCaseId`）
- [x] Task 1.2: 添加 `-OutputDir` 参数 — 提供时将 labor 记录写入 `{OutputDir}/{TicketNumber}/labor.md`，格式为 Markdown 表格
- [x] Task 1.3: labor.md 输出格式设计 — 包含表头（Date | Duration | Classification | Description）+ 数据行，无记录时写 "No labor records found."

### Verification

- [ ] 手动运行 `pwsh view-labor.ps1 -TicketNumber {caseNumber} -OutputDir cases/active` 验证 labor.md 文件生成

## Phase 2: 更新 data-refresh SKILL.md

在 data-refresh 流程中增加 labor 拉取步骤。

### Tasks

- [x] Task 2.1: 在 Step 1（D365 数据拉取）之后、Step 2（附件下载）之前，增加 Step 1.5 — 调用 `view-labor.ps1 -TicketNumber {caseNumber} -OutputDir {casesRoot}/active`
- [x] Task 2.2: 错误处理 — labor 拉取失败记录 warning 到日志，不阻塞后续步骤
- [x] Task 2.3: AR Mode 处理 — AR case 使用自身 caseNumber 拉取 labor（labor 是按 case 记录的）

### Verification

- [ ] 读取 SKILL.md 确认 Step 1.5 位置正确、参数格式与其他步骤一致

## Phase 3: 更新 labor-estimate + Schema

labor-estimate 读取已有 labor 记录，case-directory schema 更新。

### Tasks

- [x] Task 3.1: labor-estimate SKILL.md Step 3 增加读取 `{caseDir}/labor.md`，解析当天已有 labor 记录
- [x] Task 3.2: 如果当天已有 labor 记录 → 在估算结果中展示已记录量，并在 AskUserQuestion 中增加 "Already recorded today: X min" 提示
- [x] Task 3.3: 更新 `playbooks/schemas/case-directory.md`，在文件列表中增加 `labor.md`（D365 labor 记录快照，由 data-refresh 生成）

### Verification

- [ ] 读取 labor-estimate SKILL.md 确认新增的读取步骤与已有步骤风格一致
- [ ] 读取 case-directory.md 确认 labor.md 已添加

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | view-labor.ps1 支持 -TicketNumber 和 -OutputDir 参数 | E2E | 对已有 labor 记录的 case 运行脚本 → 验证 labor.md 文件生成 + 内容格式正确 |
| 2 | data-refresh SKILL.md 增加 labor 拉取步骤 | Skip | 需运行完整 data-refresh（D365 交互），通过代码审查验证 |
| 3 | labor-estimate 读取 labor.md 并提示已有记录 | Skip | 需运行完整 labor-estimate 流程（AI 推理），通过代码审查验证 |
| 4 | case-directory schema 包含 labor.md | API | 读取 case-directory.md → 验证 labor.md 条目存在 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
