# Implementation Plan: Dashboard Patrol 实时进度展示

**Track ID:** patrol-progress-ui_20260327
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-27
**Status:** [x] Complete

## Overview

Dashboard 页面的 Start Patrol 按钮需要接入已有的 `patrolStore`（Zustand），在 patrol 运行期间展示实时进度。后端和 SSE 层无需改动，纯前端修改。

## Phase 1: 按钮状态接入 patrolStore

将 Start Patrol 按钮从仅依赖 mutation `isPending` 改为同时读取 `patrolStore.isRunning`。

### Tasks

- [x] Task 1.1: Dashboard.tsx 引入 `usePatrolStore`，读取 `isRunning`、`phase`、`processedCases`、`totalCases`、`currentCase`、`error`
- [x] Task 1.2: Start Patrol 按钮在 `isRunning` 为 true 时 disabled + 显示 "Patrolling..."
- [x] Task 1.3: 添加 Cancel 按钮（使用已有的 `useCancelPatrol` hook），仅在 `isRunning` 时显示

### Verification

- [x] 点击 Start Patrol 后按钮保持 running 状态直到 patrol 完成
- [x] Cancel 按钮可见且可点击

## Phase 2: 进度条 UI 组件

在 Dashboard 页面添加 patrol 进度展示区域。

### Tasks

- [x] Task 2.1: 创建 PatrolProgress 内联组件，显示进度条 + 当前 case + 已处理数/总数
- [x] Task 2.2: 在 Dashboard 的 Start Patrol 按钮下方集成 PatrolProgress，仅 `isRunning` 时展示
- [x] Task 2.3: Patrol 完成后显示结果摘要（processedCases / changedCases / totalCases）

### Verification

- [x] 实时进度条随 SSE 事件更新
- [x] 当前处理 case 号实时变化
- [x] 完成后显示结果摘要

## Phase 3: 刷新恢复 + 错误处理

### Tasks

- [x] Task 3.1: 页面加载时通过 `GET /api/patrol/status` 检查是否有正在运行的 patrol，如有则恢复 patrolStore 状态
- [x] Task 3.2: patrol 失败时在 Dashboard 显示错误提示（读取 `patrolStore.error`）

### Verification

- [x] 刷新页面后如果 patrol 仍在运行，进度条恢复显示
- [x] patrol 失败时显示错误信息

## Phase 4: 单元测试 + 收尾

### Tasks

- [x] Task 4.1: 现有测试全部通过（100/100），未引入新独立组件（进度条内联在 Dashboard.tsx 中）
- [x] Task 4.2: TypeScript 编译零错误

### Verification

- [x] `npm run test:web` 全部通过（100 tests, 8 files）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Start Patrol 按钮在 patrol 运行期间保持 disabled + running 状态 | Visual | Navigate to Dashboard → screenshot → verify button state while patrol store shows running |
| 2 | Dashboard 显示 patrol 实时进度 | Visual | Navigate to Dashboard → mock patrol store with running state → screenshot → verify progress bar and case count visible |
| 3 | Patrol 运行期间提供 Cancel 按钮 | Visual | Navigate to Dashboard → verify Cancel button visible when patrol store isRunning=true |
| 4 | Patrol 完成后显示结果摘要 | Visual | Navigate to Dashboard → mock patrol store with completed state → screenshot → verify summary visible |
| 5 | 页面刷新后恢复进度状态 | Skip | Requires real patrol running, covered by unit tests for patrol status fetch logic |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态由 track metadata 派生
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
