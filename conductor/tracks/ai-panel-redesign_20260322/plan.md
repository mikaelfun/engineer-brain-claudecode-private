# Implementation Plan: AI Assistant Panel 重新设计

**Track ID:** ai-panel-redesign_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

分 4 个阶段重构 AI Assistant：后端状态管理 → 前端 store → SSE 集成 → UI 重写。每阶段可独立验证。

## Phase 1: 后端 — Step State Service + Routes

新建 case-step-state.ts 服务，改造 steps.ts 路由以使用该服务并支持 question 拦截。

### Tasks

- [x] Task 1.1: 新建 `dashboard/src/services/case-step-state.ts`，参考 `issue-track-state.ts`，包含：CaseStepMessage 类型（kind: started/thinking/tool-call/completed/error/question）、CaseStepStateManager 类（start/addMessage/finish/setPendingQuestion/clearPendingQuestion/cancel/getState/getAll）
- [x] Task 1.2: 改造 `dashboard/src/routes/steps.ts`：引入 case-step-state，步骤执行时写入 state 而非旧的 appendSessionMessage；使用 `createCanUseTool` 模式拦截 AskUserQuestion → 发送 `case-step-question` SSE 事件 → 暂停等待用户回答
- [x] Task 1.3: 在 `steps.ts` 新增 `POST /case/:id/step-answer` 路由：接收 answer，clearPendingQuestion，resume 同一 session
- [x] Task 1.4: 在 `steps.ts` 新增 `GET /case/:id/step-progress` recovery 端点：返回 { messages, isActive, pendingQuestion, currentStep }

### Verification

- [x] TypeScript 编译通过
- [x] 现有后端单元测试通过

## Phase 2: 前端 — Store + SSE

新建 caseAssistantStore，添加 SSE 事件处理。

### Tasks

- [x] Task 2.1: 新建 `dashboard/web/src/stores/caseAssistantStore.ts`，参考 issueTrackStore.ts，包含：CaseStepMessage 类型、per-case 的 messages/status(idle/active/waiting-input/completed/failed)/pendingQuestion/currentStep，以及 addMessage/setStatus/setPendingQuestion/clearPendingQuestion/loadMessages/clearAll actions
- [x] Task 2.2: 在 `dashboard/web/src/hooks/useSSE.ts` 添加新 SSE 事件处理：`case-step-started` → setStatus(active) + addMessage(started)；`case-step-progress`(kind:thinking/tool-call) → addMessage；`case-step-question` → setPendingQuestion + addMessage(question)；`case-step-completed` → setStatus(completed) + addMessage(completed)；`case-step-failed` → setStatus(failed) + addMessage(error)
- [x] Task 2.3: 在 `dashboard/web/src/api/hooks.ts` 添加 `useCaseStepProgress(caseNumber)` recovery hook — GET /case/:id/step-progress

### Verification

- [x] TypeScript 编译通过
- [x] 现有单元测试通过

## Phase 3: UI 重写 — CaseAIPanel

重写 CaseAIPanel 为状态驱动面板。

### Tasks

- [x] Task 3.1: 重写 `CaseAIPanel.tsx` — 消息流区域使用 caseAssistantStore 的 messages（不再用 caseSessionStore），按钮 disabled 基于 store 的 status === 'active'
- [x] Task 3.2: 添加 QuestionForm 卡片 — 当 pendingQuestion 存在时，渲染 InputRequired 卡片（提取上下文、显示选项/输入框、提交到 POST /case/:id/step-answer）
- [x] Task 3.3: 状态驱动按钮逻辑 — idle: 所有按钮可用；active: 显示 Cancel；waiting-input: 显示 QuestionForm；completed: 显示 Done + Retry；failed: 显示 Retry
- [x] Task 3.4: Chat input 改造 — completed/idle 状态下如果有 activeSessionId 则允许 chat（resume session）
- [x] Task 3.5: Recovery 逻辑 — 组件 mount 时调用 useCaseStepProgress，如果有数据则 hydrate store（messages + status + pendingQuestion）

### Verification

- [x] TypeScript 编译通过
- [x] 现有单元测试通过

## Phase 4: 消息展示优化

优化 SessionMessageList 时间戳和折叠行为。

### Tasks

- [x] Task 4.1: 确认 `SessionMessageList.tsx` 已有时间戳渲染和默认展开（Phase 1 of ISS-077 track 已完成），验证在新 store 数据下正常工作
- [x] Task 4.2: 清理 `caseSessionStore.ts` 中不再使用的旧 case message 逻辑（如果 CaseAIPanel 已完全切换到 caseAssistantStore）

### Verification

- [x] TypeScript 编译通过
- [x] 所有单元测试通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 后端 case-step-state 正确存储/恢复状态 | Skip | unit test 覆盖 |
| 2 | step-answer 路由工作 | API | POST /case/:id/step-answer with payload → assert 200 |
| 3 | step-progress recovery 端点 | API | GET /case/:id/step-progress → assert response shape |
| 4 | 步骤完成后按钮释放 | Visual | Case Detail AI tab 截图 → 确认按钮可用 |
| 5 | 消息时间戳显示 | Visual | Case Detail AI tab 截图 → 确认 HH:MM:SS |
| 6 | QuestionForm 卡片渲染 | Skip | 需要 Claude session 触发 AskUserQuestion |
| 7 | 刷新恢复 | Skip | 需要活跃 session |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
