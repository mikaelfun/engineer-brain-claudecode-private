# Implementation Plan: TestLab Runner 状态与 CLI Run 同步

**Track ID:** runner-cli-sync_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

在 runner status API 层增加 supervisor.json file-based 检测，当内存 runnerState 为 idle 但 supervisor.json 显示 running 时，返回 external 状态。前端识别此状态并调整控制条 UI 和 Stop 按钮行为。

## Phase 1: Backend — Runner Status External Detection

增强 `GET /api/tests/runner/status` 和 `POST /api/tests/runner/stop` 路由。

### Tasks

- [x] Task 1.1: 在 `test-runner.ts` 的 status 路由中，当 `runnerState.status === 'idle'` 时，调用 `readSupervisor()` 检查 supervisor.json 的 status 字段
- [x] Task 1.2: 若 supervisor.json status 为 running/scanning/generating/testing/fixing/verifying，返回 `{ status: 'external', source: 'cli', supervisorStatus: '...' }`
- [x] Task 1.3: 在 stop 路由中，当检测到 external run 时，直接写入 supervisor.json 的 `directive: "pause"` 字段（而非依赖 abortController）
- [x] Task 1.4: 在 file-watcher 的 supervisor.json 变化回调中，检测 status 从 running→idle 的转变，广播 `runner-status-changed` SSE 事件

### Verification

- [ ] 启动 CLI run → GET /api/tests/runner/status 返回 external 状态

## Phase 2: Frontend — External Run UI

TestLab 页面识别 external 状态并更新 UI。

### Tasks

- [x] Task 2.1: 在 `TestLab.tsx` 中处理 `status === 'external'`：显示 amber/yellow 状态指示器 + "External run" 标签
- [x] Task 2.2: Stop 按钮在 external 状态下可用，点击调用 `POST /api/tests/runner/stop`（后端已处理 directive 注入）
- [x] Task 2.3: Start 按钮在 external 状态下禁用（外部 run 进行中不允许启动新 run）
- [x] Task 2.4: SSE `runner-status-changed` 事件触发时刷新 runner status，实现外部 run 结束后自动恢复 idle

### Verification

- [ ] 前端 runner 控制条在 external run 时显示正确状态和按钮

## Phase 3: 向后兼容验证

### Tasks

- [x] Task 3.1: 验证 WebUI Start 启动的 run 行为不变（内存 runnerState 正常流转）
- [x] Task 3.2: 验证外部 run 结束后 runner 控制条恢复 idle

### Verification

- [ ] 完整流程：WebUI start → running → stop → idle（不受影响）
- [ ] 完整流程：CLI run → external detected → CLI 结束 → idle 恢复

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: Status API 检测 external run | API | 构造 supervisor.json status=running → GET /api/tests/runner/status → assert response.status === 'external' → 恢复 supervisor.json |
| 2 | AC2: 前端显示 External run 状态 | Visual | 构造 supervisor.json status=running → 打开 TestLab 页 → 截图验证 amber 状态指示器 + "External run" 文字 |
| 3 | AC3: Stop 按钮注入 pause directive | API | 构造 supervisor.json status=running → POST /api/tests/runner/stop → 读取 supervisor.json → assert directive === 'pause' → 清理 |
| 4 | AC4: 外部 run 结束后恢复 idle | API | 构造 supervisor.json status=running → GET status → assert external → 修改 supervisor.json status=idle → 等待 SSE → GET status → assert idle |
| 5 | AC5: WebUI Start 向后兼容 | Skip | 需要实际 spawn agent session，涉及 SDK 交互，跳过自动化 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
