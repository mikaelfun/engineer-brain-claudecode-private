# Implementation Plan: Supervisor gate=error 推理驱动恢复

**Track ID:** gate-recovery_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

分 2 个阶段：先创建 recovery-tools.yaml + 注册首个工具，再修改 supervisor-runner Step 1 的 gate=error 处理逻辑。改动集中在 1 个 yaml 新文件 + 1 个 agent md 文件。

## Phase 1: Recovery Tools 注册表

创建声明式恢复工具注册表，供 supervisor 推理匹配。

### Tasks

- [x] Task 1.1: 创建 `tests/recovery-tools.yaml` — 定义 schema（name, description, check, action, safety, waitSeconds）+ 注册首个工具 `backend-restart`
- [x] Task 1.2: `backend-restart` 工具定义 — description 描述适用场景（"后端服务未运行或不可达"），check 命令（`curl -sf http://localhost:3010/api/health`），action 分两级：先尝试 restart API，不可达则 fallback 到进程级启动（`cd dashboard && npx tsx src/index.ts &`），safety=safe，waitSeconds=5
- [x] Task 1.3: 注册第二个工具 `state-repair` — 描述场景（"state.json 损坏或解析失败"），check（`node -e "JSON.parse(require('fs').readFileSync('tests/state.json'))"`），action（`bash tests/executors/state-repair.sh`），safety=safe

### Verification

- [ ] recovery-tools.yaml 格式正确，包含 2 个工具定义
- [ ] 每个工具的 check 和 action 命令可独立运行

## Phase 2: Supervisor Step 1 推理恢复逻辑

修改 `test-supervisor-runner.md`，将 `gate=error` 从一刀切退出改为推理恢复。

### Tasks

- [x] Task 2.1: 修改 Step 1 gate=error 处理 — 不再直接跳到 Step 5，而是进入「Recovery Reasoning」流程
- [x] Task 2.2: 编写 Recovery Reasoning 推理段 — 读取 `tests/recovery-tools.yaml`，对每个工具推理三个问题：(a) gateReason 与工具 description 是否语义匹配？(b) 运行 check 命令确认问题存在？(c) action 的 safety 等级是否允许自动执行？匹配则执行 action → 等待 waitSeconds → 重新 `pre-flight.sh`
- [x] Task 2.3: 恢复结果处理 — 成功：记录 learnings + 继续正常流程；失败：记录 learnings + 跳到 Step 5（保留兜底）
- [x] Task 2.4: 验证 `npm test` 无回归

### Verification

- [ ] gate=error 时 supervisor 不再直接退出，而是尝试推理恢复
- [ ] recovery-tools.yaml 新增工具后无需改 supervisor 代码
- [ ] npm test 全部通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: gate=error 进入恢复推理 | E2E | 读 test-supervisor-runner.md → grep 确认 gate=error 不再直接跳 Step 5 → 确认有 "Recovery Reasoning" 段 |
| 2 | AC2: recovery-tools.yaml 格式正确 | E2E | 读取 tests/recovery-tools.yaml → 验证 YAML 解析成功 → 验证包含 name/description/check/action/safety/waitSeconds 字段 → 验证至少 2 个工具 |
| 3 | AC3: 推理三问题结构存在 | E2E | grep test-supervisor-runner.md 确认包含三个推理问题（语义匹配/check 确认/safety 判断） |
| 4 | AC4: 恢复成功后重新 pre-flight | E2E | grep test-supervisor-runner.md 确认恢复后有 "重新运行 pre-flight.sh" 逻辑 |
| 5 | AC5: 恢复失败兜底退出 | E2E | grep 确认失败路径仍跳到 Step 5 |
| 6 | AC6: learnings 记录 | E2E | grep 确认恢复动作调用 learnings-writer.sh |
| 7 | npm test 不回归 | E2E | 运行 `npm test` → 全部通过 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
