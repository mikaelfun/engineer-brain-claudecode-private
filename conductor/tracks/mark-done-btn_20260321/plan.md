# Implementation Plan: Implemented Issue Mark Done Button

**Track ID:** mark-done-btn_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

Add `implemented` as a new issue lifecycle status between `in-progress` and `done`. Change the auto-done behavior on implementation completion to set `implemented` instead. Add a backend endpoint and frontend button for manual mark-done with conductor track metadata sync.

## Phase 1: Backend — Type & Status Model

Add the `implemented` status to the type system and update the implementation completion handler.

### Tasks

- [x] Task 1.1: Add `'implemented'` to `IssueStatus` type in `dashboard/src/types/index.ts`
- [x] Task 1.2: Change `start-implement` route success handler in `dashboard/src/routes/issues.ts` to set status `'implemented'` instead of `'done'`

### Verification

- [ ] TypeScript compiles without errors (`npx tsc --noEmit`)

## Phase 2: Backend — Mark Done Endpoint + Conductor Sync

Add the `POST /api/issues/:id/mark-done` endpoint that updates both issue JSON and conductor track metadata.

### Tasks

- [x] Task 2.1: Add `updateTrackMetadata(trackId, updates)` function to `dashboard/src/services/conductor-reader.ts`
- [x] Task 2.2: Add `POST /api/issues/:id/mark-done` route in `dashboard/src/routes/issues.ts` — sets issue status to `done`, updates conductor track `metadata.json` status to `completed` + `updated` timestamp
- [x] Task 2.3: Update `tracks.md` row for the track (change `[ ]` / `[~]` to `[x]`)

### Verification

- [ ] TypeScript compiles without errors
- [ ] Unit tests pass (`npm run test:server`)

## Phase 3: Frontend — Mark Done Button + Status Group

Update the Issues page to show the Mark Done button and handle the `implemented` status display.

### Tasks

- [x] Task 3.1: Add `implemented` to frontend `IssueStatus` type (if duplicated in frontend types)
- [x] Task 3.2: Add `useMarkDone` mutation hook in `dashboard/web/src/api/hooks.ts` — calls `POST /api/issues/:id/mark-done`
- [x] Task 3.3: Update `renderActionButton(issue)` in `Issues.tsx` — show "Mark Done" button when `status === 'implemented'`
- [x] Task 3.4: Update status grouping logic in `Issues.tsx` — add `implemented` to a visible group (e.g., new "Implemented" group or alongside "Tracked")
- [x] Task 3.5: Add status badge color for `implemented` (e.g., teal/blue to distinguish from in-progress yellow and done green)

### Verification

- [ ] TypeScript compiles without errors (`npx tsc --noEmit` in both `dashboard/` and `dashboard/web/`)
- [ ] Frontend renders correctly with implemented issues

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | New `implemented` status added to `IssueStatus` type | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Implementation completion sets issue status to `implemented` (not `done`) | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Issues page shows "Mark Done" button for `implemented` status issues | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 4 | Clicking "Mark Done" updates issue JSON status to `done` | Skip | Covered by unit tests |
| 5 | Clicking "Mark Done" also updates conductor track `metadata.json` status to `... | Skip | Covered by unit tests |
| 6 | `implemented` issues appear in a distinct visual group on the Issues page | Visual | Navigate to page → verify items are correctly grouped/sorted |
| 7 | Status badge for `implemented` has appropriate styling (e.g., blue/teal) | Visual | Navigate to page → screenshot → verify visual appearance |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Phase 4: Post-Implementation Checklist

- [x] Unit test files created and passing
- [x] Browser test covers new Mark Done button (if UI test applicable)
- [x] ISS-038 JSON status updated
- [x] Track metadata.json updated
- [x] tracks.md status mark updated

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
