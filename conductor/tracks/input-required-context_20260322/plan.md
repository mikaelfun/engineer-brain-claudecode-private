# Implementation Plan: INPUT REQUIRED 卡片显示 specification 上下文

**Track ID:** input-required-context_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

修复 `QuestionForm` 的 `contextContent` useMemo 逻辑，使其在倒序遍历消息时跳过 `tool-call` 和 `question` 消息，从而正确提取 thinking 消息中的 spec/plan 内容。

## Phase 1: Fix contextContent Extraction

修复 `TrackProgressPanel.tsx` 中 `QuestionForm` 组件的 `contextContent` 提取逻辑。

### Tasks

- [x] Task 1.1: 在 `contextContent` useMemo 的倒序遍历中，将 `tool-call` 加入 skip 列表（与 `question` 同级处理）
- [x] Task 1.2: 添加单元测试验证 thinking → tool-call → question 序列能正确提取 contextContent

### Verification

- [x] `npm run test:web` 通过
- [x] TypeScript 编译无错误

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | INPUT REQUIRED 卡片显示 spec 上下文 | Skip | 需要实际 agent 运行，通过单元测试验证 contextContent 提取逻辑 |
| 2 | contextContent 跳过 tool-call | Skip | 单元测试覆盖 processTrackMessages + contextContent 提取 |
| 3 | 不影响现有消息折叠逻辑 | Skip | 单元测试验证 processTrackMessages 行为不变 |

**Test Type Legend:**
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
