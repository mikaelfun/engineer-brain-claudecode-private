# Implementation Plan: /issue Skill 相似 Issue 检测 + 合并/Reopen

**Track ID:** issue-dedup_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

修改 `.claude/skills/issue/SKILL.md`，在步骤 1（解析参数）和步骤 2（生成 Issue）之间插入"相似 Issue 检测"步骤。这是纯 SKILL.md 指令修改，无代码变更。

## Phase 1: 修改 SKILL.md — 插入相似检测流程

在现有 SKILL.md 的步骤 1 和步骤 2 之间插入新步骤，并调整后续步骤编号。

### Tasks

- [x] Task 1.1: 在步骤 1（解析参数）后插入新步骤"2. 相似 Issue 检测"，定义扫描逻辑：
  - 读取所有 `issues/*.json`
  - 对比新 title 与每个 issue 的 title/description
  - 匹配策略：title 子字符串匹配 + 前 3 个关键词匹配（至少 2 个命中）
  - 按匹配度排序，取 top 5
- [x] Task 1.2: 定义"匹配到活跃 issue"分支流程（AskUserQuestion）：
  - 选项 1: 合并到已有 issue `ISS-XXX`
  - 选项 2: 忽略相似，单独新建
  - 合并时的描述处理选项（追加/替换/手动编辑）
- [x] Task 1.3: 定义"匹配到已完成 issue"分支流程（AskUserQuestion）：
  - 选项 1: Reopen `ISS-XXX` 并更新描述
  - 选项 2: 忽略，单独新建
  - Reopen 时清空 trackId/verifyResult/closedReason/closedAt，status → pending
  - 描述处理选项（追加/替换/手动编辑）
- [x] Task 1.4: 定义"无匹配"分支——直接继续原有步骤 3（生成 Issue）
- [x] Task 1.5: 调整后续步骤编号（原步骤 2 → 步骤 3，原步骤 3 → 步骤 4）

### Verification

- [x] 通读修改后的 SKILL.md，确认流程完整且无遗漏分支

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 扫描 issues/*.json 并关键词匹配 | Skip | SKILL.md 是 LLM 指令文件，匹配行为由 Claude 运行时执行，无法自动化测试 |
| 2 | 匹配活跃 issue 时提示合并或新建 | Skip | 同上 — 交互流程由 Claude 运行时执行 |
| 3 | 匹配已完成 issue 时提示 reopen 或新建 | Skip | 同上 |
| 4 | 提供描述处理选项 | Skip | 同上 |
| 5 | Reopen 清空旧字段 | Skip | 同上 |
| 6 | 无匹配时行为不变 | Skip | 同上 — 可通过人工测试 `/issue` 验证 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — SKILL.md 是 Claude 运行时指令，验证靠人工执行 `/issue` 命令确认

> **Note:** 此 track 修改的是 SKILL.md（Claude 行为指令），不是可执行代码。验证方式为人工运行 `/issue` 命令，观察是否出现相似检测提示。

## Post-Implementation Checklist

- [x] SKILL.md 修改后流程完整，所有分支有明确出口
- [x] 原有功能（无参数交互式创建、有参数快速创建、Track 创建提示）未被破坏
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
