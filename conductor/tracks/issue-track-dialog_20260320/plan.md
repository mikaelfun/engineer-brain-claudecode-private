# Implementation Plan: Issue Track 对话框补充信息

**Track ID:** issue-track-dialog_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

前端添加 TrackDialog 模态框，后端 create-track 接受 extraContext body 参数，拼入 Claude session prompt。

## Phase 1: 后端 — 接受 extraContext 参数

### Tasks

- [x] Task 1.1: 修改 `dashboard/src/routes/issues.ts` 的 `create-track` 路由 — 从 request body 读取 `extraContext?: string`，拼入 spawn prompt

### Verification

- [x] TypeScript 后端编译通过

## Phase 2: 前端 — TrackDialog 模态框

### Tasks

- [x] Task 2.1: 在 `dashboard/web/src/api/hooks.ts` 修改 `useCreateTrack` — mutationFn 接受 `{ id: string; extraContext?: string }`，POST body 带 extraContext
- [x] Task 2.2: 在 `dashboard/web/src/pages/Issues.tsx` 添加 TrackDialog 模态框 — 包含 textarea 输入额外说明，"Skip — auto-generate" 和 "Create Track" 两个按钮
- [x] Task 2.3: 修改 Create Track 按钮点击逻辑 — 先打开对话框，用户提交/跳过后再调用 mutation

### Verification

- [x] TypeScript 前端编译通过

## Final Verification

- [x] 点击 Create Track 弹出对话框
- [x] Skip 直接创建（无 extraContext）
- [x] 填写后 Create 提交（带 extraContext）
- [x] TypeScript 前后端编译通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 点击 Create Track 按钮后弹出模态对话框 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 对话框包含可选输入字段：额外说明 / 验收标准 / 技术约束 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 用户可直接跳过（使用 issue description 自动推断） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 用户提交后，补充信息附加到 spawn Claude session 的 prompt 中 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | TypeScript 编译无错误 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
