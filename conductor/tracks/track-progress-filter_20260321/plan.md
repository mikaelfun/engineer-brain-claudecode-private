# Implementation Plan: Track Progress Panel SSE Message Filtering

**Track ID:** track-progress-filter_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

在 TrackProgressPanel 中实现消息合并逻辑，将连续的 tool-call 消息折叠为紧凑状态行，连续 thinking 只保留最后一条。复用 CaseAIPanel 已验证的 UI 模式。

## Phase 1: 消息合并逻辑 + 渲染重构

### Tasks

- [x] Task 1.1: 创建 `processTrackMessages()` 函数和 `TrackDisplayMessage` 类型 — 输入 `IssueTrackMessage[]`，输出合并后的展示消息数组。规则：连续 tool-call 合并为 collapsed-tools（显示工具名列表+数量），连续 thinking 合并为 collapsed-thinking（只保留最后一条），started/completed/error/question 始终单独保留
- [x] Task 1.2: 新增 `CollapsedToolGroup` 组件 — 紧凑一行显示（"Read, Write... (5 tools)"），带展开/折叠交互查看原始工具列表
- [x] Task 1.3: 重构消息渲染 — 用 `processTrackMessages()` 预处理消息流，替换直接 map 为合并后的渲染

### Verification

- [x] TypeScript 编译通过：`npx tsc --noEmit`

## Phase 2: 单元测试

### Tasks

- [x] Task 2.1: 为 `processTrackMessages()` 编写单元测试 — 覆盖：纯 tool-call 合并、混合消息流、空输入、单条消息、started/completed/error/question 不被折叠、工具名去重
- [x] Task 2.2: 为 `CollapsedToolGroup` 编写渲染测试

### Verification

- [x] `npm run test:web` 全部通过（50 tests, 4 files）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 连续的 `tool-call` 消息合并为一行，显示工具名列表和数量（如 "Read, Write... (5 tools)"） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 连续的 `thinking` 消息只保留最后一条 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | `started`, `completed`, `error`, `question` 类型始终单独显示 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 合并的消息组可点击展开查看原始详情 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | TypeScript 编译通过 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 单元测试覆盖合并逻辑 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
