# Implementation Plan: WebUI Case Email/Teams Inline Image Display Fix

**Track ID:** inline-img-fix_20260423
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-23
**Status:** [ ] Not Started

## Overview

先诊断 email 和 teams 的 inline 图片不显示的根因，定位具体断点后修复。后端路由和前端渲染逻辑代码已存在，问题大概率在数据层（文件缺失）、格式匹配（正则不匹配实际 markdown 格式）、或渲染管道（内容被提前清洗掉）。

## Phase 1: 诊断 — 定位断点

诊断思路：从数据源到渲染管道逐层排查

### Tasks

- [ ] Task 1.1: 选取一个包含 inline 图片的 case，检查 `{caseDir}/emails/` markdown 文件中的图片引用格式
- [ ] Task 1.2: 检查 `{caseDir}/images/` 目录是否有对应的图片文件
- [ ] Task 1.3: 用 curl 测试 `GET /api/cases/:id/images/:filename` 是否正常返回图片
- [ ] Task 1.4: 检查 `renderBody()` 正则是否匹配实际的 markdown 图片引用格式
- [ ] Task 1.5: 检查 `cleanBody()` 和 `splitSignature()` 是否误删了图片标记
- [ ] Task 1.6: 选取一个包含 inline 图片的 case，检查 `{caseDir}/teams/` markdown 文件中的图片引用格式
- [ ] Task 1.7: 检查 `{caseDir}/teams/assets/` 目录是否有对应的图片文件
- [ ] Task 1.8: 用 curl 测试 `GET /api/cases/:id/teams/assets/:filename` 是否正常返回图片
- [ ] Task 1.9: 检查 `rewriteImagePaths()` 正则是否匹配实际的 teams markdown 图片引用格式
- [ ] Task 1.10: 汇总诊断结果，记录每个断点

### Verification

- [ ] 完成断点诊断报告，明确 email 和 teams 各自的失败原因

## Phase 2: 修复 — Email Inline 图片

根据 Phase 1 诊断结果修复 email 图片渲染

### Tasks

- [ ] Task 2.1: 修复图片文件缺失问题（如需要：添加 data-refresh 下载逻辑或后端路由修正）
- [ ] Task 2.2: 修复正则匹配问题（如需要：调整 `renderBody()` 或 `cleanBody()` 正则）
- [ ] Task 2.3: 修复 AuthImage 认证问题（如需要）
- [ ] Task 2.4: 添加图片加载失败 fallback（alt 文本或 placeholder icon）

### Verification

- [ ] Email Tab 中 inline 图片正常渲染

## Phase 3: 修复 — Teams Inline 图片

根据 Phase 1 诊断结果修复 teams 图片渲染

### Tasks

- [ ] Task 3.1: 修复图片文件缺失问题（如需要）
- [ ] Task 3.2: 修复 `rewriteImagePaths()` 正则匹配问题（如需要）
- [ ] Task 3.3: 修复 digest 中图片引用问题（如需要）
- [ ] Task 3.4: 添加图片加载失败 fallback

### Verification

- [ ] Teams Tab 中 inline 图片正常渲染
- [ ] Teams digest 中引用图片正常显示

## Phase 4: 验证与收尾

### Tasks

- [ ] Task 4.1: 在多个包含图片的 case 上验证 email 和 teams 图片渲染
- [ ] Task 4.2: 验证没有图片的 case 不受影响（回归测试）
- [ ] Task 4.3: 编写/更新 unit test（renderBody、rewriteImagePaths 等函数）

### Verification

- [ ] 多 case 验证通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Email Tab inline 图片正确渲染 | E2E | 找含图片的 case → curl API 验证图片返回 → Playwright 打开 CaseDetail email tab → 截图验证图片显示 |
| 2 | Teams Tab inline 图片正确渲染 | E2E | 找含图片的 case → curl API 验证图片返回 → Playwright 打开 CaseDetail teams tab → 截图验证图片显示 |
| 3 | Teams digest 图片正确显示 | Visual | 打开含 digest 的 teams tab → 截图验证 digest 区域图片显示 |
| 4 | 图片加载失败 fallback | Interaction | 构造不存在的图片 URL → 验证 fallback UI 显示 |
| 5 | 无图片 case 不受影响 | E2E | 打开无图片 case → 验证 email/teams tab 正常渲染无报错 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
