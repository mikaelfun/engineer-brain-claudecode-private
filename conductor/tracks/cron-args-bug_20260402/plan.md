# Implementation Plan: Cron Job Prompt Arguments Not Passed to Skill

**Track ID:** cron-args-bug_20260402
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-02
**Status:** [x] Complete

## Overview

修改 `cron-manager.ts` 的 `executeCronPrompt()` 函数，在 prompt 以 slash command 格式开头时，改写为显式指令，确保 Claude 在 `-p` 模式下通过 Skill 工具正确传递 args 参数。

## Phase 1: Fix prompt formatting in cron-manager

修改 `executeCronPrompt()` 对 slash command 格式的 prompt 进行预处理。

### Tasks

- [x] Task 1.1: 在 `executeCronPrompt()` 中添加 slash command 检测和 prompt 改写逻辑
  - 检测 prompt 是否以 `/xxx arg1 arg2` 格式开头
  - 如果是，改写为明确指令，告诉 Claude 使用 Skill 工具并传递 args
  - 示例：`/onenote-export sync` → `Use the Skill tool to invoke "onenote-export" with args "sync"`
- [x] Task 1.2: 处理无参数的 slash command（如 `/patrol`），保持不变或同样格式化
- [x] Task 1.3: 处理非 slash command 的普通 prompt（如自然语言），保持不变

### Verification

- [ ] 手动执行已有的 onenote-export sync cron job，确认 sync 参数被正确传递

## Phase 2: Verify backward compatibility

确保已存储的 cron jobs 和各种 prompt 格式都能正常工作。

### Tasks

- [x] Task 2.1: 验证已存储在 `cron-jobs.json` 中的 job 无需修改即可生效（prompt 字段不变，改写在执行时动态处理）
- [x] Task 2.2: 验证 PROMPT_PRESETS 中的所有模板都能正确执行

### Verification

- [ ] 检查 cron-jobs.json 中已有 job 的 prompt 字段未被修改
- [ ] 手动 Run 触发已有 job，确认执行成功

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | `/onenote-export sync` 参数正确传递 | E2E | 手动触发 cron job → 检查 lastOutput 不再显示"需要参数" → 确认执行了增量同步 |
| 2 | 无参数 slash command 正常工作 | API | POST /api/agents/triggers 创建 `/patrol` job → POST run → 检查不报错 |
| 3 | 已有 cron job 无需重配 | API | 重启 server → GET /api/agents/triggers → 确认已有 job prompt 字段不变 → POST run → 检查参数传递正确 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
