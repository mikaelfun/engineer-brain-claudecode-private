# Implementation Plan: Case Detail Inspection 默认首位展示 + 格式美化

**Track ID:** inspection-beautify_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

安装 @tailwindcss/typography 修复 prose 类空转问题，将 Inspection Tab 移到首位并设为默认，然后针对深色主题精细化定制 markdown 元素样式。

## Phase 1: 安装 Typography 插件 + Tab 重排

安装缺失的 Tailwind Typography 插件，调整 tab 顺序和默认值。

### Tasks

- [x] Task 1.1: npm install @tailwindcss/typography (dashboard/web)
- [x] Task 1.2: tailwind.config.js 添加 require('@tailwindcss/typography') 到 plugins
- [x] Task 1.3: CaseDetail.tsx 中 tabs 数组将 Inspection 移到第一位
- [x] Task 1.4: 默认 activeTab 从 'info' 改为 'inspection'

### Verification

- [ ] 页面打开默认显示 Inspection tab
- [ ] prose 样式生效（标题有层级、表格有样式）

## Phase 2: 深色主题适配 + 精细化样式

针对深色背景主题定制 markdown 各元素的渲染效果。

### Tasks

- [x] Task 2.1: InspectionTab 的 prose div 添加 prose-invert（适配深色主题）
- [x] Task 2.2: 为 ReactMarkdown 添加 components override：自定义 table/th/td 样式（边框、padding、深色背景）
- [x] Task 2.3: 自定义 blockquote 样式（左边框颜色 + 背景）
- [x] Task 2.4: 自定义 code/pre 样式（深色背景 + 圆角）
- [x] Task 2.5: index.css 中添加 prose 在深色主题下的微调（如表格交替行色）

### Verification

- [ ] 表格渲染美观（边框 + 交替行色 + 合适的 padding）
- [ ] 标题/列表/代码/引用在深色主题下清晰可读
- [ ] 整体视觉一致，无色彩冲突

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Inspection Tab 排在所有 tab 的第一位 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 打开 Case Detail 页面时默认展示 Inspection Tab | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Markdown 表格有清晰的边框和交替行色 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 标题层级有明显的视觉区分（字号、粗细） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | 列表（有序/无序）有正确的缩进和标记 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 行内代码和代码块有背景色区分 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 7 | 引用块（blockquote）有左边框和背景色 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 8 | 整体深色主题适配（prose-invert） | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建并通过
- [ ] browser-test.mjs 已覆盖新页面/路由（如有 UI 变更）
- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
