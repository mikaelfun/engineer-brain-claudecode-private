# Implementation Plan: Agents Page Unified Session View

**Track ID:** agent-session-view_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

Add a `GET /api/sessions/all` endpoint that aggregates sessions from all four managers (case, implement, verify, track-creation) into a unified format. Redesign the AgentMonitor page to display all session types with grouping, filtering, and type-specific badges.

## Phase 1: Backend — Unified Sessions Type & Endpoint

Expose list-all functions from each session manager, create a unified response type, and add an aggregation endpoint.

### Tasks

- [x] Task 1.1: Add `getAllImplementSessions()` export to `implement-session-manager.ts` — returns all entries from the in-memory Map
- [x] Task 1.2: Add `getAllVerifySessions()` export to `verify-session-manager.ts` — returns all entries from the in-memory Map
- [x] Task 1.3: Add `getAllTrackStates()` export to `issue-track-state.ts` — returns all entries from the in-memory Map
- [x] Task 1.4: Add `UnifiedSession` type to `types/index.ts` with fields: `id`, `type` (`'case'|'implement'|'verify'|'track-creation'`), `status`, `context` (case# or issue ID), `intent`, `startedAt`, `lastActivityAt`, `metadata`
- [x] Task 1.5: Add `GET /api/sessions/all` route in a new file `routes/sessions.ts` — aggregates all four sources into `UnifiedSession[]`, sorted by `lastActivityAt` desc

### Verification

- [ ] TypeScript compiles without errors
- [ ] `curl /api/sessions/all` returns combined sessions

## Phase 2: Frontend — API Hook & Types

Add the frontend hook and type for the unified sessions endpoint.

### Tasks

- [x] Task 2.1: Add `UnifiedSession` type to frontend types (mirror backend)
- [x] Task 2.2: Add `useUnifiedSessions()` hook in `hooks.ts` — calls `GET /api/sessions/all`

### Verification

- [ ] TypeScript compiles without errors

## Phase 3: Frontend — AgentMonitor Page Redesign

Replace the current "Live Sessions" card with a full unified sessions view.

### Tasks

- [x] Task 3.1: Redesign AgentMonitor page — replace "Live Sessions" card with "All Sessions" section using `useUnifiedSessions()`, grouped by type with collapsible groups
- [x] Task 3.2: Create session type badges — visual badges for case/implement/verify/track-creation with distinct colors (using design system CSS variables)
- [x] Task 3.3: Add status indicator per session — active (pulsing dot), paused (yellow), completed (green), failed (red)
- [x] Task 3.4: Add filter controls — filter by session type (checkboxes) and status (dropdown)
- [x] Task 3.5: Show type-specific metadata — case: case number + intent; implement: issue ID + track ID; verify: issue ID + test results summary; track-creation: issue ID + pending question indicator

### Verification

- [ ] TypeScript compiles without errors
- [ ] Page renders all session types correctly

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Agents page shows case sessions of all statuses (active, paused, completed, f... | Visual | Navigate to page → verify element is visible with correct content |
| 2 | Agents page shows implement sessions with issue ID, track ID, and status | Visual | Navigate to page → verify element is visible with correct content |
| 3 | Agents page shows verify sessions with issue ID and test results summary | Visual | Navigate to page → verify element is visible with correct content |
| 4 | Agents page shows track-creation sessions with issue ID and status | Visual | Navigate to page → verify element is visible with correct content |
| 5 | Sessions are grouped by type with clear visual distinction | Visual | Navigate to page → verify items are correctly grouped/sorted |
| 6 | Each session shows: type badge, context (case#/issue ID), status, intent/desc... | Visual | Navigate to page → screenshot → verify visual appearance |
| 7 | Filter/search capability: filter by session type and status | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Phase 4: Post-Implementation Checklist

- [x] Unit test files created and passing
- [x] Browser test covers Agents page session view (if UI test applicable)
- [x] ISS-044 JSON status updated
- [x] Track metadata.json updated
- [x] tracks.md status mark updated

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
