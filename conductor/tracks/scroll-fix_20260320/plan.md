# Implementation Plan: Fix CaseAIPanel Double-Scroll Bug

**Track ID:** scroll-fix_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

将 `CaseAIPanel.tsx` 中的 `scrollIntoView()` 替换为 `container.scrollTop = container.scrollHeight`，使 auto-scroll 只作用于内层消息容器，不影响页面级滚动。

## Phase 1: Fix Auto-Scroll Logic

修改 `CaseAIPanel.tsx` 中的滚动实现。

### Tasks

- [x] Task 1.1: 将 `chatEndRef` 改为 `messagesContainerRef`，指向消息列表的外层 `<div className="max-h-60 overflow-y-auto ...">` 容器
- [x] Task 1.2: 将 `useEffect` 中的 `scrollIntoView()` 替换为 `container.scrollTop = container.scrollHeight`
- [x] Task 1.3: 移除不再需要的 `<div ref={chatEndRef} />` sentinel 元素

### Verification

- [x] 启动 Dashboard，对一个 Case 执行 full process，确认内层消息列表自动滚动而页面 scrollbar 不动

## Final Verification

- [x] All acceptance criteria met
- [x] 内层消息列表在 streaming 时正常追尾
- [x] 页面 scrollbar 在 streaming 时保持不动
- [x] 无 console 报错

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Full process streaming 时，内层消息列表自动滚动到最新消息 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Full process streaming 时，页面（外层）scrollbar 位置保持不变 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 手动滚动内层消息列表后，新消息到达时仍能自动追尾 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
