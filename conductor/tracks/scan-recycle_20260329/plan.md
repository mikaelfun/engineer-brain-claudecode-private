# Implementation Plan: SCAN 阶段回收 regression/retry/unverified 旧问题

**Track ID:** scan-recycle_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [~] In Progress

## Overview

在 SKILL.md 的 SCAN 阶段添加"回收检查"步骤，位于加载状态之后、扫描端点之前。读取 state.json 的 regressionQueue/fixQueue/verifyQueue，将符合条件的项移入 testQueue，并记录回收操作到 phaseHistory。

## Phase 1: SKILL.md SCAN 逻辑修改

修改 `.claude/skills/test-loop/SKILL.md` 的 SCAN 阶段，插入回收步骤。

### Tasks

- [x] Task 1.1: 在 SCAN 阶段 Step 0（加载状态）和 Step 1（扫描 API 端点）之间，插入新的 "Step 0: 回收旧问题" 段落
- [x] Task 1.2: 回收逻辑定义 — regressionQueue 全部回收；fixQueue 中 retryCount < 3 的回收；verifyQueue 全部回收
- [x] Task 1.3: 回收后清空原 queue 中已移动的项，更新 state.json
- [x] Task 1.4: 记录 phaseHistory：`{ phase: "SCAN", action: "recycle", recycled: { fromRegression: N, fromFix: N, fromVerify: N } }`

### Verification

- [x] SKILL.md SCAN 阶段包含回收步骤，位置正确

## Phase 2: 创建 queue-recycler.sh executor

创建独立 bash executor 处理回收逻辑，便于 SCAN 阶段调用。

### Tasks

- [x] Task 2.1: 创建 `tests/executors/queue-recycler.sh` — 读取 state.json，输出回收计划
- [x] Task 2.2: 输出格式：`RECYCLE|{fromQueue}|{testId}|{reason}` per item，最后 `RECYCLE_SUMMARY|{total}|{fromRegression}|{fromFix}|{fromVerify}`
- [x] Task 2.3: fixQueue 过滤逻辑 — retryCount >= 3 的跳过（已标记需人工）
- [x] Task 2.4: 通过 state-writer.sh 原子更新 state.json（testQueue 追加回收项，原 queue 清空已移项）

### Verification

- [x] queue-recycler.sh 可独立运行，正确输出回收计划
- [x] state.json 更新后 testQueue 包含回收项，原 queue 正确清空

## Phase 3: 空转检测 + 决策逻辑

确保回收后的 SCAN 决策逻辑正确：即使无新 gap，如果 testQueue 因回收而非空，应进入 TEST。

### Tasks

- [x] Task 3.1: 验证 SCAN 结尾决策逻辑兼容回收场景 — "无 gap + testQueue 非空 → phase=TEST"
- [x] Task 3.2: 添加空转保护 — 如果回收 0 项且无新 gap，round++ 并在 phaseHistory 记录 "no work available"

### Verification

- [x] 有回收项时进入 TEST 阶段 (SKILL.md 已有: "无 gap + testQueue 非空（含回收项）→ phase=TEST")
- [x] 无回收 + 无 gap 时正确 round++ 并记录 (SKILL.md 已更新决策逻辑含 no_work action)

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: regressionQueue 回收 | E2E | 构造 state.json 含 regressionQueue → 运行 queue-recycler.sh → 验证 testQueue 包含回收项 |
| 2 | AC2: fixQueue retryCount<3 回收 | E2E | 构造 fixQueue 含 retryCount=1 和 retryCount=3 的项 → 运行 → 验证只回收 retryCount<3 的 |
| 3 | AC3: verifyQueue 回收 | E2E | 构造 verifyQueue → 运行 → 验证 testQueue 包含回收项 |
| 4 | AC4: 原 queue 清空已移项 | E2E | 运行后验证 regressionQueue/fixQueue/verifyQueue 中已回收项被移除 |
| 5 | AC5: 回收在扫描之前 | E2E | 完整 SCAN 运行 → 验证 phaseHistory 中 recycle 记录在 scan 记录之前 |
| 6 | AC6: phaseHistory 记录 | E2E | 运行 → 验证 phaseHistory 包含 recycle 记录及正确数量 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore

## Post-Implementation Checklist

- [x] queue-recycler.sh 已创建并可运行
- [x] SKILL.md SCAN 阶段已更新包含回收步骤
- [ ] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
