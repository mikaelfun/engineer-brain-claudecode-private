# Specification: Recipe Architecture 共享指南

**Track ID:** recipe-arch_20260329
**Type:** Chore
**Created:** 2026-03-29
**Status:** Draft
**Issue:** ISS-149

## Summary

从 conductor:verify 的推理驱动+自我演进模式中提取共享架构指南，定义 Recipe Index + LLM 推理层 + 反思演进 的标准三层模式，供 todo/routing/email/patrol/troubleshooting 5个领域引用。

## Context

verify-redesign (ISS-148) 建立了一个强大的 Recipe 模式：
1. Recipe Index (`_index.md`) — 关键词匹配规则 + "无匹配→从零推理" 兜底
2. LLM 推理层 — 读 context → 匹配 recipe → 按 recipe 执行
3. 反思演进 — trigger(retries≥2/遇坑/脚本>10行) → 创建/更新 recipe + 演进日志

架构审查发现 5 个领域可复用此模式（todo/routing/email/patrol/troubleshooting），需要一个共享指南避免各自独立重复实现。

## Acceptance Criteria

- [ ] AC1: 创建 `playbooks/guides/recipe-architecture.md` 定义三层标准架构（Recipe Index / LLM 推理层 / 反思演进）
- [ ] AC2: 指南包含 Recipe Index 标准格式（匹配规则表 + 演进日志表 + 版本号）
- [ ] AC3: 指南包含 LLM 推理层标准流程（读 context → 查 index 匹配 → 有 recipe 按步骤 → 无 recipe 从零推理）
- [ ] AC4: 指南包含反思演进标准触发条件表（retries≥2 / 用户编辑 / 意外结果 / recipe 过时）和操作流程
- [ ] AC5: 指南包含各领域的适配说明（todo/routing/email/patrol/troubleshooting 各自的 context 来源、recipe 粒度、演进信号）
- [ ] AC6: 更新 verification-recipes/_index.md 引用此共享指南
- [ ] AC7: 指南包含"何时不应使用 recipe"的边界说明（安全规则、数据契约、UI token 等应保持硬编码）

## Dependencies

- verify-redesign_20260329 (ISS-148) — 已完成，提供参照实现

## Out of Scope

- 各领域的具体 recipe 创建（由各自的 track 实现）
- 代码修改（此 track 只产出文档指南）

## Technical Notes

- 参照 `playbooks/guides/verification-recipes/_index.md` 和 `.claude/skills/conductor/verify/SKILL.md` 的设计
- 指南应保持简洁（<200 行），避免过度抽象

---

_Generated by Conductor._
