# Implementation Plan: Agent Session SSE Message Filtering

**Track ID:** sse-message-filter_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

纯前端改动。在 CaseAIPanel 的消息渲染层实现消息过滤和合并逻辑，将 verbose 的 tool-call/tool-result/thinking 消息折叠为简洁的状态指示器，同时保留完整数据供展开查看。

## Phase 1: 消息合并逻辑 + 渲染重构

在 CaseAIPanel 中新增消息预处理函数，将原始消息流转为展示友好的合并消息。

### Tasks

- [x] Task 1.1: 创建 `processMessages()` 函数 — 输入 `CaseSessionMessage[]`，输出合并后的展示消息数组。规则：连续的 tool-call/tool-result 合并为一个 "working" 状态项（显示最新工具名 + 已处理工具数），连续 thinking 只保留最后一条，system/completed/failed 始终保留
- [x] Task 1.2: 重构 MessageBubble — 新增 `collapsed` 类型的渲染模式，显示为紧凑的一行（"🔧 Using Bash, Read... (5 tools)" 或 "💭 Thinking..."），带 spinner 动画
- [x] Task 1.3: 添加 "展开/折叠" 交互 — 折叠的消息组可点击展开查看原始详情列表

### Verification

- [x] TypeScript 编译通过：`npx tsc --noEmit`（dashboard/web）
- [ ] 视觉验证：在 Case Detail 页面触发一个操作，确认消息面板显示简洁状态而非 verbose 内容

## Phase 2: 单元测试 + 收尾

### Tasks

- [x] Task 2.1: 为 `processMessages()` 函数编写单元测试 — 覆盖：纯 tool 消息合并、混合消息流、空输入、单条消息、completed/failed 不被折叠
- [x] Task 2.2: 为新的 MessageBubble collapsed 模式编写渲染测试

### Verification

- [x] `npm run test:web` 全部通过
- [ ] Playwright 截图验证 AI Panel 在操作期间的显示效果

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | tool-call 和 tool-result 消息默认折叠为一行简洁状态（如 "🔧 Running Bash..." / "✅ Bash done"） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 连续多条 tool-call/tool-result 合并为一个 "Thinking..." 动画（带工具名轮播或计数） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | thinking 消息（assistant text）也默认折叠，只显示最新一条或 "AI is thinking..." | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 关键状态消息始终显示：system（步骤开始）、completed、failed | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | 用户可以点击展开查看完整的 verbose 详情（可选，不阻挡 MVP） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 消息面板滚动行为保持正常（自动滚到底部） | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] browser-test.mjs 已覆盖新页面/路由（如有 UI 变更）
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
