# Implementation Plan: Fix Issues Page & Agent Monitor State Sync

**Track ID:** issue-session-sync_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Add a lightweight recovery mechanism to the Issues page that detects active backend track-creation sessions on mount and hydrates the local Zustand store. This bridges the gap between backend session state and frontend display, ensuring TrackProgressPanel mounts and recovers.

## Phase 1: Frontend Recovery Hook + Issues Page Integration

Add a `useActiveTrackSessions` query that fetches unified sessions on page load, then for each issue with an active backend session, triggers the existing recovery flow.

### Tasks

- [x] Task 1.1: Create `useActiveTrackSessions` hook in `hooks.ts` — fetch `/sessions/all?type=track-creation` filtered to active sessions, with a moderate refetch interval. Return a `Map<issueId, sessionInfo>` for efficient lookup.

- [x] Task 1.2: Add recovery effect in `IssueRow` component — use the active sessions map to detect when an issue has a backend session but `showProgress` is false. When detected, set `isOptimisticTracking` via `setIssueTracking(issueId, true)` which makes `showProgress = true` → TrackProgressPanel mounts → its built-in `useTrackProgress` recovery fires → messages + pendingQuestion hydrated.

- [x] Task 1.3: Update `AgentMonitor.test.tsx` — add mock for the new hook if needed to prevent test breakage.

### Verification

- [ ] Issues page shows TrackProgressPanel for issues with active backend track-creation sessions
- [ ] Pending questions are recoverable and answerable
- [ ] No regression in existing SSE-driven create-track flow
- [ ] AgentMonitor tests pass

## Final Verification

- [ ] All acceptance criteria met
- [ ] Tests passing (`npm test` in dashboard/web)
- [ ] Manual test: start track creation → refresh Issues page → panel shows with recovery

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Issues page detects active backend track-creation sessions on mount/refresh | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | TrackProgressPanel renders and recovers messages for issues with active backe... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Pending questions from backend are recoverable and answerable on the Issues page | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | No unnecessary API calls for issues without active sessions | API | Call the relevant endpoint → verify response shape and status code |
| 5 | Existing SSE-driven flow continues to work unchanged | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | AgentMonitor.test.tsx continues to pass | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
