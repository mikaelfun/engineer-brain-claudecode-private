# Implementation Plan: Fix Cancel Track Spinner Persists

**Track ID:** cancel-spinner-fix_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Two-pronged fix: (1) Add optimistic cache update in `useCancelTrack` to immediately flip `issue.status` from 'tracking' to 'pending' in TanStack Query cache, and (2) invalidate unified sessions query + add cancel guard in the recovery effect to prevent re-arming after cancel.

## Phase 1: Fix Cancel State Management

### Tasks

- [x] Task 1.1: Add optimistic update to `useCancelTrack` — use `onMutate` to immediately set `issue.status = 'pending'` in the `['issues']` query cache, and also invalidate `['unified-sessions', 'track-creation']` to clear stale backend session data
- [x] Task 1.2: Add cancel guard to ISS-055 recovery effect in `IssueRow` — after `clearTrackMessages(issue.id)` the store already clears `trackingIssues[id]`, but the recovery effect may re-trigger if `hasBackendSession` is still stale; add a condition to skip re-arming when the issue has just been cancelled (use `clearMessages` having cleared `trackingIssues` as the signal, or check store `trackingIssues` explicitly in the effect guard)

### Verification

- [x] TypeScript compiles without errors (`npx tsc --noEmit`)
- [x] Existing unit tests pass (`npm test`)

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Cancel click immediately hides TrackProgressPanel | Interaction | API-seed test issue → start track creation → wait for spinner → click Cancel → verify panel disappears within 1s |
| 2 | Recovery effect does not re-arm after cancel | Interaction | After cancel, wait 3s → verify panel still hidden, no re-appearance |
| 3 | Normal track creation flow still works | Skip | Covered by existing flow, no regression risk from optimistic update |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
