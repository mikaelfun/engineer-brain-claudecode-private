# Implementation Plan: NoteGap 写入 D365 后无成功反馈且内容丢失

**Track ID:** notegap-write-feedback_20260401
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-01
**Status:** [x] Complete

## Overview

分两阶段修复：Phase 1 修复前端反馈机制（Bug #1），Phase 2 修复后端内容传递（Bug #2）。两个 bug 互相独立，可分别验证。

## Phase 1: 写入成功/失败状态反馈

修复写入后卡片立即消失的问题，让用户看到明确的成功/失败反馈。

### Tasks

- [x] Task 1.1: 修改 `useSubmitNote` hook — `onSuccess` 中不立即 `invalidateQueries`，改为手动 `setQueryData` 将状态设为一个"已提交"中间态
- [x] Task 1.2: 修改 `NoteGapCard.tsx` — 新增"已提交成功"状态展示：卡片保留但变为绿色边框 + 成功图标 + "✅ Note 已成功写入 D365" 文字，编辑器和按钮区域隐藏
- [x] Task 1.3: 添加 3 秒后自动淡出逻辑 — 成功状态展示 3 秒后，触发 `invalidateQueries` 让卡片正常消失
- [x] Task 1.4: 修改错误状态展示 — 写入失败时卡片保留，显示红色边框 + 错误信息 + "重试"按钮，不自动消失

### Verification

- [ ] 手动触发 NoteGap 写入，确认成功后卡片保留显示 3 秒再消失
- [ ] 模拟错误场景（断网/无效 case），确认错误状态持久显示

## Phase 2: 修复内容传递丢失

修复 body 通过命令行传递时特殊字符/多行/长文本被截断的问题。

### Tasks

- [x] Task 2.1: 修改 `note-gap-routes.ts` submit 端点 — 将 body 写入临时文件（`{caseDir}/note-body.tmp`），命令改为 `pwsh add-note.ps1 -BodyFile "{tempFile}"`，脚本执行后删除临时文件
- [x] Task 2.2: 修改 `add-note.ps1` — 新增 `-BodyFile` 参数，当提供时从文件读取 body 内容（`Get-Content -Raw`），保持 `-Body` 参数向后兼容
- [x] Task 2.3: 同步修改 API 模式的 body 传递 — 确认 OData API 路径中 `notetext` 字段正确传递多行/特殊字符（JSON 编码天然安全，主要验证）

### Verification

- [ ] 测试包含特殊字符（`$`、`` ` ``、`'`、`"`、中文、换行）的 note 写入
- [ ] 测试超长 body（>2000 字符）写入

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 写入成功后显示明确的成功状态，持续至少 3 秒可见 | Interaction | 创建 note-draft.md 测试文件 → Playwright 打开 CaseDetail → 点击"写入 D365"→ 断言成功状态出现 → 等待 3 秒 → 断言卡片消失 |
| 2 | 写入失败后显示错误信息，不自动消失 | API | Mock submit endpoint 返回 500 → 点击写入 → 断言错误信息持续显示 + 重试按钮可见 |
| 3 | 特殊字符的 note 能完整写入 D365 | Skip | D365 write operation — 安全红线，不自动化。手动验证 |
| 4 | 长内容（>2000 字符）的 note 能完整写入不截断 | Skip | D365 write operation — 安全红线，不自动化。手动验证 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
