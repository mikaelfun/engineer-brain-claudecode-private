# Implementation Plan: Fix Create Track Question Dialog Not Showing

**Track ID:** track-question-fix_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

将 `trackAsync` 中的 `let capturedSessionId` 替换为 shared mutable ref `sessionRef`，通过 `processQueryMessages` 的 `sessionRef` 参数实时写入 `session_id`，让 `canUseTool` 在 AskUserQuestion 触发时能读到正确的 sessionId。同时修复前端 `useSSE.ts` 中的 `sessionId` guard。

## Phase 1: Backend — Shared Mutable Ref Pattern

修改 `dashboard/src/routes/issues.ts` 中的 `trackAsync` 和 `resumeTrackSession`，使用 `sessionRef` 替代 closure timing bug。

### Tasks

- [x] Task 1.1: 修改 `trackAsync`（create-track route）— 将 `let capturedSessionId` 改为 `const sessionRef = { current: undefined as string | undefined }`，`createCanUseTool` getter 改为 `() => sessionRef.current`，传 `sessionRef` 给 `processQueryMessages`
- [x] Task 1.2: 修改 `resumeTrackSession` — 同样传 `sessionRef` 给 `processQueryMessages`（保持一致性，虽然 resume 时 sessionId 已知）

### Verification

- [x] TypeScript 编译通过 (`cd dashboard && npx tsc --noEmit`)

## Phase 2: Frontend — 放宽 sessionId Guard

修改 `useSSE.ts`，当 `sessionId` 为 `undefined` 时仍然设置 `pendingQuestion`（使用空字符串作为 fallback），确保 QuestionForm 可以渲染。

### Tasks

- [x] Task 2.1: 修改 `useSSE.ts` 中 `issue-track-question` handler — 放宽 `if (d.sessionId && d.questions)` 为 `if (d.questions)`，`sessionId` fallback 为空字符串

### Verification

- [x] TypeScript 编译通过 (`cd dashboard/web && npx tsc --noEmit`)

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | `canUseTool` 回调在 AskUserQuestion 触发时能读取到正确的 `sessionId` | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | SSE `issue-track-question` 事件的 `sessionId` 字段不为 `undefined` | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 前端 `TrackProgressPanel` 正确显示 `QuestionForm`，用户可以回答 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 用户回答后 session 正确 resume，track 创建流程继续 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | TypeScript 编译通过，现有单元测试通过 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Phase 3: Post-Implementation Checklist

- [x] TypeScript 编译通过（`cd dashboard && npx tsc --noEmit`）
- [x] 单元测试通过（`cd dashboard && npm test`）
- [x] browser-test 跳过（Bug fix < 3 tasks，按 workflow.md 规则）
- [x] 关联 Issue ISS-027 JSON 状态已更新（done）
- [x] Track metadata.json 已更新（complete）
- [x] tracks.md 状态标记已更新（[x]）

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
