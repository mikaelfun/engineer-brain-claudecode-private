# Implementation Plan: Fix AI Assistant Session 消息流体验

**Track ID:** ai-assistant-session_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

分四步修复 AI Assistant tab 的 session 体验：修复按钮释放逻辑、添加时间戳、改善消息展示、启用交互。

## Phase 1: 修复按钮释放逻辑

修改 CaseAIPanel 中 `isActionDisabled` 的计算逻辑，不再将 paused session 视为"正在运行"。

### Tasks

- [x] Task 1.1: 修改 `CaseAIPanel.tsx` 中 `isActionDisabled` 逻辑，改为仅基于 `isProcessing || hasActiveOperation`，移除 `isLiveRunning`
- [x] Task 1.2: 修改 chat input 的 disabled 逻辑，允许在有 paused session 时发送消息（使用 `activeSessions[0]?.sessionId` 作为 fallback）

### Verification

- [x] TypeScript 编译通过
- [x] 现有单元测试通过（91/91）

## Phase 2: 添加时间戳 + 改善消息展示

### Tasks

- [x] Task 2.1: 在 `SessionMessageList.tsx` 的 `MessageBubble` 中渲染时间戳（HH:MM:SS 格式），CollapsedGroup 也显示时间戳
- [x] Task 2.2: 调整折叠逻辑：CollapsedGroup 默认展开（`useState(true)`），用户可手动折叠

### Verification

- [x] TypeScript 编译通过
- [x] 现有单元测试通过（91/91）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 步骤完成后按钮恢复可点击 | Skip | 涉及 Claude SDK session 触发，安全红线内 |
| 2 | 消息气泡显示时间戳 | Visual | 导航到 Case Detail AI tab → 截图 → 确认消息有时间戳 |
| 3 | 过程消息默认展开 | Visual | 导航到 Case Detail AI tab → 截图 → 确认 tool/thinking 消息可见 |
| 4 | Chat input 在完成后可用 | Visual | 导航到 Case Detail AI tab → 截图 → 确认 input 框可用 |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
