# Implementation Plan: 新增 issue-scanner.sh 扫描 issues/ 目录生成测试

**Track ID:** issue-scanner_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [x] Complete

## Overview

创建 issue-scanner.sh executor，扫描 `issues/*.json`，按状态分类处理：tracked/implemented → 读取关联 track spec 提取 AC → 与 registry 比对输出 gap；done → 检查回归覆盖；pending → SKIP。然后修改 SKILL.md 在 SCAN 阶段更新 Step 4 调用。

## Phase 1: 创建 issue-scanner.sh executor

创建 `tests/executors/issue-scanner.sh`，实现核心扫描逻辑。

### Tasks

- [x] Task 1.1: 创建 issue-scanner.sh 基础框架 — 读取 issues/*.json，解析 id/status/trackId 字段
- [x] Task 1.2: 状态过滤逻辑 — pending → SKIP（输出 ISSUE_SKIP）；tracked/implemented → 进入 AC 提取；done → 进入回归检查
- [x] Task 1.3: AC 提取 — 根据 trackId 找到 conductor/tracks/{trackId}/spec.md，解析 AC 列表
- [x] Task 1.4: Gap 比对 — 将提取的 AC 与 tests/registry/*.yaml 比对，输出未覆盖的 AC 为 ISSUE_GAP
- [x] Task 1.5: 输出格式对齐 — `ISSUE_GAP|{issueId}|{criterion}|{category}`，`ISSUE_SKIP|{issueId}|{reason}`

### Verification

- [x] issue-scanner.sh 可独立运行，正确读取 issues/ 目录
- [x] tracked/implemented issue 正确提取 AC 并输出 gap

## Phase 2: 修改 SKILL.md SCAN 阶段更新 Step 4

在 `.claude/skills/test-loop/SKILL.md` 的 SCAN 阶段更新 Step 4 调用 issue-scanner.sh。

### Tasks

- [x] Task 2.1: 在 SKILL.md SCAN 阶段 Step 4 更新为调用 issue-scanner.sh
- [x] Task 2.2: 将 issue-scanner.sh 输出的 ISSUE_GAP 追加到 state.json gaps（标记 issue-driven）
- [x] Task 2.3: 记录 phaseHistory：`{ phase: "SCAN", action: "issue-scan", gaps: N, regression_gaps: N, skipped: N }`

### Verification

- [x] SKILL.md Step 4 包含调用 issue-scanner.sh
- [x] 描述 ISSUE_GAP 和 ISSUE_REGRESSION_GAP 追加到 state.json gaps

## Phase 3: done issue 回归覆盖检查

处理 done 状态的 issue，检查是否有对应回归测试。

### Tasks

- [x] Task 3.1: done issue 处理 — 根据 trackId 找到 spec AC，检查 registry 中是否有对应测试
- [x] Task 3.2: 未覆盖的 done issue AC 输出为 `ISSUE_REGRESSION_GAP|{issueId}|{criterion}|regression`
- [x] Task 3.3: 已覆盖的 done issue 输出 `ISSUE_COVERED|{issueId}|{testCount}`

### Verification

- [x] done issue 正确检查回归覆盖
- [x] 未覆盖的输出 ISSUE_REGRESSION_GAP，已覆盖的输出 ISSUE_COVERED

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: tracked/implemented issue 提取 AC gap | E2E | 构造 tracked issue + spec → 运行 issue-scanner.sh → 验证输出 ISSUE_GAP |
| 2 | AC2: pending issue 跳过 | E2E | 构造 pending issue（无 trackId）→ 运行 → 验证输出 ISSUE_SKIP |
| 3 | AC3: done issue 回归覆盖检查 | E2E | 构造 done issue + registry 有/无覆盖 → 运行 → 验证 ISSUE_REGRESSION_GAP/ISSUE_COVERED |
| 4 | AC4: gap 追加到 state.json | E2E | 完整 SCAN 运行含 Step 7 → 验证 state.json testQueue 包含 issue gap |
| 5 | AC5: 输出格式一致 | E2E | 运行 → 验证输出格式为 ISSUE_GAP\|{issueId}\|{criterion}\|{category} |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs → restore

## Post-Implementation Checklist

- [x] issue-scanner.sh 已创建并可运行
- [x] SKILL.md SCAN 阶段已更新包含 Step 4 调用 issue-scanner.sh
- [x] done issue 回归覆盖检查已实现
- [ ] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
