# Specification: Workflow 测试采用 Synthetic Data

**Track ID:** synthetic-adoption_20260330
**Type:** Enhancement
**Created:** 2026-03-30
**Status:** Draft

## Summary

让现有 workflow 测试使用 `data_source: synthetic` + profile，替代对 live case 的依赖。保留触发过失败的 seed，实现边缘场景的可复现回归测试。

## Context

当前状况：
- 15 个 synthetic profile 已就绪（normal、compliance-fail-*、edge-*、status-* 等）
- 生成速度 < 1s，不是瓶颈
- **0 个业务测试使用 synthetic data**（仅 2 个验证 synthetic 系统本身的测试）
- 所有 workflow 测试依赖 `cases/active/` 下的 live case，测试稳定性受真实数据变化影响
- `cleanup_synthetic()` 保留最近 5 个，边缘场景数据无法持久化

## Acceptance Criteria

- [ ] AC1: compliance-check 测试使用 synthetic profile（compliance-ok-china-cld、compliance-fail-no-china 等），覆盖合规通过/失败/21v 场景
- [ ] AC2: status-judge 测试使用 synthetic profile（status-pending-customer、status-pending-engineer、status-stale），覆盖不同状态判断场景
- [ ] AC3: inspection-writer 测试使用 synthetic profile（normal、heavy），验证不同数据量下的 inspection 生成
- [ ] AC4: edge case 测试使用 edge-* profile（edge-corrupted-meta、edge-no-entitlement、edge-missing-fields），验证错误恢复能力
- [ ] AC5: 触发失败的 seed 自动记录到 `tests/learnings.yaml`（格式：profile + seed + testId + failure reason），下次回归测试可用固定 seed 复现
- [ ] AC6: `cleanup_synthetic()` 策略调整 — 被 learnings.yaml 引用的 seed 对应的目录不删除（或标记为 pinned）

## Dependencies

- `tests/fixtures/synthetic/generator.sh` — 生成器（已就绪）
- `tests/fixtures/synthetic/profiles.yaml` — 15 个 profile（已就绪）
- `tests/executors/common.sh` — `resolve_case_dir()` + `cleanup_synthetic()`
- `tests/learnings.yaml` — 需扩展 schema 支持 seed 记录

## Out of Scope

- 新增 profile（现有 15 个已够用）
- 修改 generator.js 核心逻辑
- 替换所有 live case 测试（部分测试确实需要真实 D365 数据）

## Technical Notes

- Seed 保留策略：不是保留文件，而是保留 seed 值。同 profile + 同 seed = 确定性输出，可随时重新生成。
- learnings.yaml 新增 `synthetic_seeds` section：
  ```yaml
  synthetic_seeds:
    - profile: edge-corrupted-meta
      seed: 12345
      testId: wf-compliance-edge
      failure: "JSON parse error not caught"
      discoveredAt: "2026-03-30"
  ```
- cleanup_synthetic() 可保持现有逻辑（保留最近 5 个），因为 seed 保留后随时可重新生成。

---

_Generated by Conductor._
