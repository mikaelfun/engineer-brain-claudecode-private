# Implementation Plan: Workflow 测试采用 Synthetic Data

**Track ID:** synthetic-adoption_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

分三个阶段：先扩展 learnings.yaml 支持 seed 记录，再批量创建 synthetic 测试定义，最后验证端到端流程。

## Phase 1: Seed 记录机制

### Tasks

- [x] Task 1.1: 扩展 `tests/learnings.yaml` schema，新增 `synthetic_seeds` section
- [x] Task 1.2: 在 `fix-recorder.sh` 或 test result 写入逻辑中，当 `data_source=synthetic` 的测试失败时，自动记录 profile + seed 到 learnings.yaml
- [x] Task 1.3: 在 `resolve_case_dir()` 中支持 `synthetic_seed` 字段（固定 seed 复现模式）

### Verification

- [ ] 手动创建一个 synthetic 测试 → 失败 → 验证 seed 被记录到 learnings.yaml

## Phase 2: 批量创建 Synthetic 测试定义

### Tasks

- [x] Task 2.1: 创建 compliance-check synthetic 测试（3 个 YAML：compliance-ok、compliance-fail、compliance-21v）
- [x] Task 2.2: 创建 status-judge synthetic 测试（3 个 YAML：pending-customer、pending-engineer、stale）
- [x] Task 2.3: 创建 inspection-writer synthetic 测试（2 个 YAML：normal、heavy）
- [x] Task 2.4: 创建 edge case synthetic 测试（3 个 YAML：corrupted-meta、no-entitlement、missing-fields）

### Verification

- [ ] 所有新建 YAML 通过 schema 校验
- [ ] `resolve_case_dir()` 能正确解析每个定义并生成 case 目录

## Phase 3: 端到端验证

### Tasks

- [x] Task 3.1: 通过 test-loop 的 TEST 阶段运行至少 3 个 synthetic 测试，验证完整流程
- [x] Task 3.2: 验证失败测试的 seed 被自动记录，用记录的 seed 重跑验证可复现

### Verification

- [ ] synthetic 测试在 test-loop 中正常执行（不报 CONTRACT_MISMATCH 或路径错误）
- [ ] learnings.yaml 中有 seed 记录

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | compliance-check synthetic 测试 | E2E | 运行 compliance-ok + compliance-fail profile 测试 → 验证断言覆盖合规通过/失败场景 |
| 2 | status-judge synthetic 测试 | E2E | 运行 3 个 status profile 测试 → 验证不同 actualStatus 判断 |
| 3 | edge case 测试 | E2E | 运行 edge-corrupted-meta → 验证 JSON 错误被捕获而非 crash |
| 4 | seed 记录 | E2E | 制造一个 synthetic 测试失败 → 验证 learnings.yaml 新增 seed 条目 |
| 5 | seed 复现 | E2E | 用记录的 seed 重跑 → 验证输出与首次一致 |
| 6 | cleanup 不删 pinned | E2E | 运行 cleanup_synthetic → 验证被 learnings 引用的 seed 目录保留（或可重新生成） |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor._
