# Implementation Plan: 修复 Verify 第二步（UI Test）报错

**Track ID:** verify-step2-fix_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

根因：`browser-test.mjs` 硬编码 `localhost:5177`，但由于 ISS-018（restart 按钮未杀旧进程导致端口变化），前端可能运行在不同端口。需要让 browser-test.mjs 能动态检测端口，并在 verify 接口中增加服务可用性预检。

## Phase 1: 诊断与端口检测

先复现问题，确认实际报错内容，然后让 browser-test.mjs 支持动态端口。

### Tasks

- [x] Task 1.1: 运行 `node scripts/browser-test.mjs` 复现错误，记录实际输出
- [x] Task 1.2: 修改 `browser-test.mjs` — 支持环境变量 `BROWSER_TEST_URL` 覆盖硬编码的 `http://localhost:5177`，保持默认值向后兼容
- [x] Task 1.3: 修改 `browser-test.mjs` — 在启动测试前添加 HTTP 健康检查（fetch BASE，重试 3 次 × 2s），不可达时输出明确错误信息并退出

### Verification

- [x] `BROWSER_TEST_URL=http://localhost:5173 node scripts/browser-test.mjs` 可正常连接
- [x] 服务未启动时脚本快速失败并输出 "Service not reachable" 而非超时

## Phase 2: Verify API 端点改进

让后端 verify 接口在运行 browser-test.mjs 前检测服务是否可用，并传递正确的 URL。

### Tasks

- [x] Task 2.1: 在 `dashboard/src/routes/issues.ts` 的 verify 端点中，执行第二步前先检测前端服务是否在 `config` 中配置的端口可达
- [x] Task 2.2: 将前端 URL 通过环境变量 `BROWSER_TEST_URL` 传给 `browser-test.mjs`，使用 dashboard 的实际运行端口
- [x] Task 2.3: 服务不可达时返回明确的错误响应 `{ uiTest: { success: false, output: "Frontend service not reachable..." } }` 而非等待超时

### Verification

- [x] 服务运行时 verify 第二步能正常执行
- [x] 服务未运行时 verify 第二步快速返回明确错误而非 180s 超时

## Phase 3: 端到端验证

### Tasks

- [x] Task 3.1: 确保单元测试通过（`npm test`）
- [x] Task 3.2: 手动在 Issues 页面点击 Verify，确认两步都能通过

### Verification

- [x] 完整 Verify 流程（Step 1 + Step 2）成功通过
- [x] Issue 状态从 `in-progress` 变为 `done`

## Final Verification

- [x] All acceptance criteria met
- [x] Tests passing
- [x] Ready for review

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 排查 `browser-test.mjs` 失败的实际原因（读取错误输出 / 截图） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 修复 `browser-test.mjs` 中的问题（端口、登录、页面错误等） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Verify 接口在执行第二步前检测服务是否可用，不可用时返回明确错误信息而非超时 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | Verify 两步测试都能正常通过 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
