# Implementation Plan: Session Active Panel Collapsible

**Track ID:** session-collapse_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [ ] Not Started

## Overview

给 CaseAIPanel 的 Session Active 区域添加折叠/展开功能。单文件修改。

## Phase 1: Add Collapse Toggle

### Tasks

- [ ] Task 1.1: 添加 `isSessionExpanded` state（默认 `true`）
- [ ] Task 1.2: 将 Session Active 指示条改为可点击触发器，点击切换 `isSessionExpanded`，添加 ChevronDown 图标指示展开/折叠方向
- [ ] Task 1.3: 用 `isSessionExpanded` 控制 SSE 消息列表、Chat 输入、Quick Actions 的显示/隐藏
- [ ] Task 1.4: 添加 `transition-transform` 动画到 chevron 图标

### Verification

- [ ] Session active 时点击指示条可折叠/展开
- [ ] 折叠后只显示紧凑指示条
- [ ] 展开后显示完整内容
- [ ] 动画流畅

## Final Verification

- [ ] All acceptance criteria met
- [ ] 无 console 报错
- [ ] 折叠状态下 SSE 消息仍在后台接收（只是不显示）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Session Active 区域可以点击折叠/展开 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 折叠状态下只显示紧凑的指示条（Session Active 标签 + session ID 摘要 + 展开按钮） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 展开状态下显示完整内容（消息列表 + Chat 输入 + Quick Actions） | Interaction | Navigate to page → type in input → submit → verify response appears |
| 4 | 默认状态为展开（保持当前行为） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | 折叠/展开有平滑过渡动画 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
