# Implementation Plan: Test Loop 自愈能力

**Track ID:** testloop-self-heal_20260328
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-28
**Status:** [x] Complete

## Overview

在 test-loop 的 VERIFY/FIX 阶段失败处理路径中，插入 "Self-Heal Check" 步骤。新增 `pattern-detector.sh` 脚本做 failure signature 聚类，识别 systemic failure 后自动跳过受影响测试、注入 learning、创建 framework-level fix item。所有自愈动作写入 self-heal 记录文件，供 supervisor evolution 展示。

## Phase 1: Pattern Detector 脚本

新增 failure pattern 检测脚本，读取 fixQueue + 结果文件，聚类分析。

### Tasks

- [x] Task 1.1: 创建 `tests/executors/pattern-detector.sh` — 输入：round；读取 state.json fixQueue + result files；输出 patterns
- [x] Task 1.2: 实现 failure signature 提取逻辑 — 从 assertions 提取 `timeout:*`、`lock_conflict:*`、`http_status_*:*`、`assertion:*` 归一化签名
- [x] Task 1.3: 实现 systemic pattern 聚类 — 2+ tests 相同 signature + retryCount>=2 = systemic；单 test retryCount>=2 = stuck
- [x] Task 1.4: 输出格式定义 — `PATTERN_DETECT|{pattern-id}|{type}|{signature}|{test-ids}|{action}`

### Verification

- [ ] 手动构造 fixQueue 含 3 个 timeout 测试，运行 pattern-detector.sh 能输出 systemic pattern

## Phase 2: Self-Heal Step 集成到 test-loop

修改 test-loop SKILL.md，在 VERIFY/FIX 失败路径插入 self-heal check。

### Tasks

- [x] Task 2.1: 在 test-loop SKILL.md 的 VERIFY 阶段插入 Self-Heal Check 步骤（Step 3 位置）
- [x] Task 2.2: 实现 systemic 自愈路径 — skip affected tests + create framework fix item + learnings + self-heal record
- [x] Task 2.3: 实现 stuck 自愈路径 — skip test + learnings + self-heal record（不创建 framework fix）
- [x] Task 2.4: 创建 `tests/executors/self-heal-recorder.sh` — 写入 `tests/results/fixes/{pattern-id}-self-heal.md`

### Verification

- [ ] test-loop 在 VERIFY 阶段遇到 systemic timeout pattern 时，能自动跳过 + 创建 framework fix item + 记录 self-heal

## Phase 3: Framework Fix 优先级处理

修改 FIX 阶段支持 framework 类 fix item 优先处理。

### Tasks

- [x] Task 3.1: 扩展 fixQueue item schema — 新增 `category` 和 `priority` 字段（在 self-heal 创建 framework fix 时使用）
- [x] Task 3.2: 修改 FIX 阶段循环逻辑 — 按 priority 排序，framework 类走独立修复路径
- [x] Task 3.3: Framework fix 的 opus agent prompt 模板 — 读取 self-heal record，定位基础设施代码根因

### Verification

- [ ] fixQueue 中 framework item（priority=1）在普通 test item（priority=5）之前被处理

## Phase 4: Evolution 展示集成

确保 supervisor evolution 模式能读取和展示 self-heal 记录。

### Tasks

- [x] Task 4.1: 在 test-supervisor SKILL.md 的 evolution mode 中，增加扫描 `tests/results/fixes/*-self-heal.md` 的逻辑（步骤 5 + 数据来源表）
- [x] Task 4.2: 定义 self-heal record 在 evolution timeline 中的展示格式 — `🧬 SELF-HEALED [{pattern-id}] {signature}` + affected tests + diagnosis + actions

### Verification

- [ ] `/test-supervisor evolution` 能展示 self-heal 记录

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 连续失败 2+ 次自动触发 pattern analysis | E2E | 构造 state.json 含 3 个 retryCount=2 的 timeout 测试 → 模拟 VERIFY 失败 → 验证 pattern-detector.sh 被调用并输出 systemic pattern |
| 2 | systemic failure 识别和聚合 | E2E | 构造 3 个不同 testId 的结果文件，均含 `timeout:poll_for_completion` signature → 运行 pattern-detector.sh → 验证输出 1 个 systemic pattern 含 3 个 affected tests |
| 3 | 自动跳过 + learning 注入 | E2E | 模拟 systemic pattern 触发 → 验证 affected tests 从 queue 移除 → stats.skipped 增加 → learnings.yaml 新增条目 |
| 4 | framework fix item 优先处理 | E2E | 构造 fixQueue 含 1 个 framework(priority=1) + 2 个 test(priority=5) → 模拟 FIX 阶段 → 验证 framework item 先被处理 |
| 5 | self-heal 记录文件生成 | E2E | 触发自愈后 → 验证 `tests/results/fixes/{pattern-id}-self-heal.md` 存在且包含 pattern type、signature、affected tests |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
