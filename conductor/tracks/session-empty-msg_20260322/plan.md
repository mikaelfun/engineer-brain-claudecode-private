# Implementation Plan: Filter Empty System Messages in Agent Monitor

**Track ID:** session-empty-msg_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [ ] Not Started

## Overview

在两层过滤空内容的 system 消息：后端持久化层拦截 + 前端渲染层防御。同时修复 steps.ts 的类型映射不一致问题。

## Phase 1: Backend — 持久化过滤 + 类型一致性

在持久化层添加空内容过滤，并修复 steps.ts 类型映射。

### Tasks

- [x] Task 1.1: 在 `case-session-manager.ts` 的 `appendSessionMessage()` 中添加空内容过滤 — 如果 content 为空字符串或仅包含空白字符，跳过写入
- [x] Task 1.2: 修复 `steps.ts` 中的消息类型映射 — 将原始 `messageType` 替换为 `getPersistedMessageType(messageType)` 调用
- [x] Task 1.3: 在 `case-routes.ts` 的 full-process 和 chat 路由中确认也使用 `getPersistedMessageType()` — 验证一致性

### Verification

- [ ] TypeScript 编译无错误
- [ ] 现有单元测试通过

## Phase 2: Frontend — 渲染层过滤

在 SessionMessageList 组件中过滤空内容的 system 消息。

### Tasks

- [x] Task 2.1: 在 `SessionMessageList.tsx` 中添加过滤逻辑 — 跳过 type 为 'system' 且 content 为空/纯空白的消息
- [x] Task 2.2: 确认 CaseSessionDetail 恢复加载路径也受益于此过滤（AgentMonitor.tsx 中的 recovery 逻辑）

### Verification

- [ ] TypeScript 编译无错误
- [ ] 现有单元测试通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 持久化层过滤空内容消息 | Skip | Backend-only，通过单元测试验证 appendSessionMessage 跳过空内容 |
| 2 | 渲染层过滤空 system 消息 | Visual | 打开 Agent Monitor → 展开 case session → 截图验证无空白 system 行 |
| 3 | steps.ts 类型一致性 | Skip | Backend-only，通过代码审查确认使用 getPersistedMessageType |
| 4 | 有内容的 system 消息仍正常显示 | Visual | 同 #2 截图中确认有实际内容的消息正常渲染 |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
