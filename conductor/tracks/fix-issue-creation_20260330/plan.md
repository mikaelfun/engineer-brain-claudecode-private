# Implementation Plan: Test-loop FIX 阶段发现产品 Bug 自动创建 Issue

**Track ID:** fix-issue-creation_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

在 FIX 阶段的 fix-recorder.sh 完成记录后，增加一个 LLM 推理步骤：读取 fix report + analysis + 修改文件列表，判断是基础设施问题还是产品缺陷。产品缺陷自动创建 Issue JSON，并在 discoveries.json 中建立反向关联。

## Phase 1: Issue Creator Script

创建新脚本 `tests/executors/issue-creator.sh`，负责写入 Issue JSON 文件。

### Tasks

- [x] Task 1.1: 创建 `tests/executors/issue-creator.sh` — 接收参数（testId, round, title, description, priority, fixRef），读取 issues/ 目录最大 ID，生成 ISS-XXX.json 写入 issues/ 目录。包含 discoveredBy/round/testId/fixRef 扩展字段。输出 `ISSUE_CREATED|ISS-XXX|testId`
- [x] Task 1.2: 增加去重逻辑 — 扫描 issues/*.json 中 discoveredBy=test-loop 且 testId 匹配的已有 Issue，存在则跳过并输出 `ISSUE_EXISTS|ISS-XXX|testId`

### Verification

- [x] 手动运行 issue-creator.sh 验证创建的 JSON 格式正确、ID 递增、去重生效

## Phase 2: FIX.md 增加 LLM 推理分类步骤

修改 FIX 阶段流程，在 fix-recorder.sh 之后增加 LLM 推理判断。

### Tasks

- [x] Task 2.1: 修改 `.claude/skills/test-loop/phases/FIX.md` — 在步骤 e（fix-recorder.sh）之后、步骤 f（update state.json）之前，增加新步骤：LLM 推理分类。仅对 `code_bug` 类型的修复触发（env_issue 天然是基础设施问题，跳过）
- [x] Task 2.2: 编写推理 prompt 模板 — Main Agent 读取 `{testId}-analysis.md` + `{testId}-fix.md` + modified files 列表，推理判断：(1) 分类 infrastructure/product，(2) 如果 product → 推理 priority（P0/P1/P2），(3) 生成一句话 title + 描述。推理依据：修改的文件路径（tests/ executor = infra, skills/ playbooks/ dashboard/src/ = product）、错误性质、影响范围
- [x] Task 2.3: 分类为 product bug 时，调用 `bash tests/executors/issue-creator.sh` 创建 Issue

### Verification

- [x] FIX.md 的流程步骤完整连贯，推理步骤位置正确

## Phase 3: discoveries.json 反向关联

在 stats-reporter.sh 中为有 Issue 的 discovery 填充 issueId。

### Tasks

- [x] Task 3.1: 修改 `tests/executors/stats-reporter.sh` — 生成 discoveries.json 时，扫描 issues/*.json 中 `discoveredBy === "test-loop"` 的 Issue，按 testId 匹配，将 issueId 写入 discovery 条目
- [x] Task 3.2: discoveries.json 的 discovery 对象新增可选字段 `issueId: "ISS-XXX" | null`

### Verification

- [x] 运行 stats-reporter.sh 后验证 discoveries.json 中有 issueId 的条目正确关联

## Phase 4: Post-Implementation Checklist

### Tasks

- [x] Task 4.1: 端到端验证 — issue-creator.sh 创建正确 JSON，字段完整，ID 递增
- [x] Task 4.2: 去重验证 — 同一 testId 第二次调用输出 ISSUE_EXISTS 不重复创建

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | FIX 阶段 LLM 推理分类 infrastructure vs product | E2E | 构造两种 fix-analysis.md（一个修改 tests/，一个修改 skills/）→ 模拟 FIX 推理步骤 → 验证分类结果 |
| 2 | product bug 自动创建 Issue JSON | E2E | Backup issues/ → 运行 issue-creator.sh with test params → 验证 ISS-XXX.json 存在且字段正确 → restore |
| 3 | Issue 包含 discoveredBy/round/testId/fixRef 字段 | E2E | 读取创建的 Issue JSON → 断言 4 个扩展字段存在且值正确 |
| 4 | LLM 推理 priority | E2E | 构造不同严重程度的 fix report → 验证 priority 在 P0/P1/P2 范围内 |
| 5 | discoveries.json 新增 issueId 关联 | E2E | 创建 test-loop discovered Issue → 运行 stats-reporter.sh → 验证 discoveries.json 中对应条目有 issueId |
| 6 | 去重：同一 testId 不重复创建 Issue | E2E | 运行 issue-creator.sh 两次相同 testId → 验证只创建一个 Issue，第二次输出 ISSUE_EXISTS |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
