# Implementation Plan: Verify Results Persistent Display

**Track ID:** verify-results-display_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

后端持久化 verify 结果到 issue JSON，前端在 issue 展开详情中内联显示可折叠的测试结果面板。

## Phase 1: 后端持久化 verify 结果

### Tasks

- [x] Task 1.1: 修改 `POST /api/issues/:id/verify` — 将 `verifyResult` 写入 issue JSON（通过 `updateIssue`），包含 `{ unitTest: { success, output }, uiTest: { success, output }, verifiedAt: ISO timestamp }`
- [x] Task 1.2: 更新 issue 类型定义（`IssueVerifyResult` interface + `Issue.verifyResult?`），添加可选字段

### Verification

- [x] TypeScript 编译通过

## Phase 2: 前端 inline 测试结果展示

### Tasks

- [x] Task 2.1: 在 IssueRow 的 Readonly Detail Panel 中添加 `VerifyResultPanel` 组件 — 当 issue 有 `verifyResult` 时显示可折叠的测试结果区域
- [x] Task 2.2: `VerifyResultPanel` 组件实现 — 显示 Step 1 Unit Tests / Step 2 UI Tests 的 PASS/FAIL/SKIPPED badge + 可展开 output + 验证时间

### Verification

- [x] TypeScript 编译通过
- [x] `npm run test:web` 全部通过（50 tests, 4 files）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 后端 verify 接口执行后将 `verifyResult` 写入 issue JSON（含 unitTest/uiTest 的 success + o... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Issue 展开详情面板中，有 verifyResult 的 issue 显示 "Test Results" 可折叠区域 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 折叠区域显示各测试步骤的 PASS/FAIL/SKIPPED 状态 + 可展开的 output | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 刷新页面后测试结果仍可查看 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | TypeScript 编译通过 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 现有测试通过 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
