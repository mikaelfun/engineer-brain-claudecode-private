# Implementation Plan: Fix Verify "Running Tests" Stuck After All Pass

**Track ID:** verify-stuck-fix_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

Add a polling fallback in `VerifyProgressPanel` to detect when the backend verify session has completed/failed but the SSE completion event was missed. This ensures the UI transitions out of "Running Tests..." state reliably.

## Phase 1: Add Polling Fallback to VerifyProgressPanel

When `isActive` is `true` and messages exist (so recovery won't trigger), poll `GET /api/issues/:id/verify-status` every 5 seconds to sync state.

### Tasks

- [x] Task 1.1: In `VerifyProgressPanel.tsx`, add a second `useVerifyStatus` query that polls every 5 seconds when `isActive === true && messages.length > 0`
- [x] Task 1.2: Add a `useEffect` that checks the polled data: if `status === 'completed'` or `status === 'failed'` while `isActive` is still `true`, call `setVerifyActive(issueId, false)` + `setVerifyResult()` / `addVerifyMessage()` accordingly
- [x] Task 1.3: Ensure polling stops once `isActive` becomes `false` (the `enabled` flag should be `isActive && messages.length > 0`)

### Verification

- [x] TypeScript compiles without errors
- [x] Existing unit tests pass (1 pre-existing failure in AgentMonitor.test.tsx, unrelated)

## Phase 2: Unit Tests

### Tasks

- [x] Task 2.1: Add test in `VerifyProgressPanel.test.tsx` covering: polling fallback triggers when `isActive=true` + messages exist + backend returns `status: 'completed'` with result → header transitions to "All Tests Passed"
- [x] Task 2.2: Add test covering: polling stops when `isActive` becomes `false`

### Verification

- [x] `npm run test:web` — all tests pass (6/6 VerifyProgressPanel tests pass)

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
