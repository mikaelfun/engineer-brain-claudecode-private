# Implementation Plan: Case 列表显示 Refresh 时间、Teams 消息截止时间、Refresh/Full Process 按钮

**Track ID:** case-list-actions_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

分两个阶段实现：后端扩展 CaseSummary 数据 → 前端 Dashboard.tsx 增加时间戳显示和操作按钮。

## Phase 1: Backend — 扩展 CaseSummary 数据

后端 GET /api/cases 返回扩展数据，包含 fetchedAt 和 teamsLastMessageTime。

### Tasks

- [x] Task 1.1: 在 types/index.ts 中为 CaseSummary 添加 fetchedAt?: string 和 teamsLastMessageTime?: string 字段
- [x] Task 1.2: 在 services/case-reader.ts 中添加 readTeamsLastMessageTime(caseDir) 辅助函数 — 读取 teams/_chat-index.json，提取各 chat 的 lastMessageTime 取最大值
- [x] Task 1.3: 在 routes/cases.ts 的 GET /api/cases 中，将 fetchedAt 和 teamsLastMessageTime 加入每个 CaseSummary 对象

### Verification

- [ ] npm run test:server 通过
- [ ] curl GET /api/cases 返回包含 fetchedAt 和 teamsLastMessageTime 的 JSON

## Phase 2: Frontend — Dashboard Case Card 增加时间戳和操作按钮

Dashboard.tsx 的 case card 增加时间信息行和 Refresh/Full Process 按钮。

### Tasks

- [x] Task 2.1: 添加 relativeTime 工具函数（ISO 时间 → "5m ago" / "2h ago" / "3d ago"）
- [x] Task 2.2: 在 Dashboard.tsx case card 中增加一行显示 fetchedAt 和 teamsLastMessageTime（使用 relativeTime 格式）
- [x] Task 2.3: 在 Dashboard.tsx case card 中增加 Refresh 和 Full Process 按钮，点击分别触发 POST /api/case/:id/step/data-refresh 和 POST /api/case/:id/process
- [x] Task 2.4: 集成 GET /api/operations/active 数据，有活跃 operation 的 case 按钮禁用 + 显示运行中状态

### Verification

- [ ] npm run test:web 通过
- [ ] UI Screenshot: Dashboard 页面截图确认 card 显示时间戳和按钮

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Card 显示 fetchedAt 相对时间 | Visual | 导航到 Dashboard → 截图 → 确认 card 显示 "Xm ago" 格式时间 |
| 2 | Card 显示 Teams 最后消息时间 | Visual | 导航到 Dashboard → 确认有 Teams 时间标签 |
| 3 | Refresh 按钮可见可点击 | Visual | 导航到 Dashboard → 截图 → 确认按钮存在 |
| 4 | Full Process 按钮可见可点击 | Visual | 同上 |
| 5 | 按钮在 operation 活跃时禁用 | Skip | 需要运行中 operation 才能验证，unit test 覆盖 |
| 6 | 后端返回扩展 CaseSummary | API | curl GET /api/cases → 验证 JSON 包含 fetchedAt 和 teamsLastMessageTime |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
