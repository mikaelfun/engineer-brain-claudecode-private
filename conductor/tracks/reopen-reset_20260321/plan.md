# Implementation Plan: Reopen Issue 应清空旧 Track 信息回到 Pending 状态

**Track ID:** reopen-reset_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [ ] Not Started

## Overview

修改 reopen 逻辑：清除 issue 的 trackId、设 status 为 pending、归档旧 track。涉及后端 reopen 路由 + conductor-reader 归档函数，前端无需改动（已有 `pending && !trackId → Create Track` 分支）。

## Phase 1: 后端 — 修改 Reopen 路由 + 添加归档逻辑

修改 `/api/issues/:id/reopen` 路由，对有 trackId 的 issue：归档旧 track → 清除 trackId → 设 status 为 pending。

### Tasks

- [ ] Task 1.1: 在 `conductor-reader.ts` 中添加 `archiveTrack(trackId)` 函数 — 将 track metadata.status 设为 `"archived"`，tracks.md 状态标记改为 `[A]`
- [ ] Task 1.2: 修改 `dashboard/src/routes/issues.ts` 的 reopen 路由 — 有 trackId 时：调用 archiveTrack → updateIssue 清除 trackId 并设 status 为 pending
- [ ] Task 1.3: 确保 `enrichIssueFromTrack` 对没有 trackId 的 issue 直接返回原 issue（已有逻辑，确认无需改动）

### Verification

- [ ] Reopen 一个有 trackId 的 done issue 后，issue JSON 的 trackId 被清除、status 为 pending
- [ ] 旧 track 的 metadata.json 中 status 变为 "archived"
- [ ] tracks.md 中旧 track 行标记为 `[A]`

## Phase 2: 验证前端行为（无需代码改动）

前端已有正确的分支逻辑：`pending && !trackId` 显示 Create Track。只需确认 reopen 后 API 返回正确数据，前端自动渲染正确按钮。

### Tasks

- [ ] Task 2.1: 确认前端 Issues.tsx 中 `pending && !trackId` 分支会正确显示 Create Track 按钮（代码审查，无需修改）
- [ ] Task 2.2: 确认 `useReopenIssue` hook 的 `onSuccess` 会 invalidate issues query 触发 UI 刷新（代码审查，无需修改）

### Verification

- [ ] Reopen 后前端显示 "Create Track" 按钮而非 "Mark Done" / "Implement"

## Final Verification

- [ ] All acceptance criteria met
- [ ] 旧 track 目录保留不被删除
- [ ] 新 Create Track 能生成独立的新 track
- [ ] Ready for review

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Reopen done/implemented issue 后，issue status 变为 `pending` | Interaction | Navigate to page → perform interaction → verify expected outcome |
| 2 | Reopen 后 issue JSON 中 `trackId` 被清除 | Interaction | Navigate to page → perform interaction → verify expected outcome |
| 3 | Reopen 后 UI 显示 "Create Track" 按钮（而非 Implement/Mark Done） | Interaction | Navigate to page → perform interaction → verify expected outcome |
| 4 | 旧 track 的 metadata.json 中标记 `status: "archived"` 或类似状态 | Skip | Covered by unit tests |
| 5 | tracks.md 中旧 track 的状态标记更新为 `[A]`（archived） | Skip | Covered by unit tests |
| 6 | 新建的 track 与旧 track 独立，不互相影响 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
