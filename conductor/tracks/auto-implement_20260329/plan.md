# Implementation Plan: env.yaml 增加 autoImplementTracked 开关

**Track ID:** auto-implement_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [x] Complete

## Overview

在 env.yaml 添加 `autoImplementTracked` 配置项（默认 false）。修改 SKILL.md SCAN 阶段，当开关开启且发现 tracked+planned issue 时，自动 spawn `conductor:implement` 实现 tracked issue。实现后 issue 状态更新为 implemented，下轮 SCAN 自动生成验证测试。

## Phase 1: env.yaml 添加 autoImplementTracked 配置项

添加配置项并确保配置加载机制支持。

### Tasks

- [x] Task 1.1: 在 `tests/env.yaml` 添加 `autoImplementTracked: false` 配置项
- [x] Task 1.2: 在 env.yaml 的注释中说明此配置的用途和风险
- [x] Task 1.3: 验证现有 env.yaml 加载逻辑能读取新字段（检查 SKILL.md 中的 env 加载步骤）

### Verification

- [x] env.yaml 包含 autoImplementTracked 字段
- [x] 配置值可被 SCAN 阶段读取

## Phase 2: SKILL.md SCAN 阶段添加自动实现逻辑

当 autoImplementTracked=true 且发现 tracked+planned issue 时，spawn conductor:implement。

### Tasks

- [x] Task 2.1: 在 SKILL.md SCAN 阶段（issue-scanner.sh 之后）添加自动实现决策逻辑
- [x] Task 2.2: 检查 autoImplementTracked 值 — false 时 SKIP 并记录 `NOTE_DIRECTIVE|{issueId}|tracked but auto-implement disabled`
- [x] Task 2.3: true 时检查 tracked issue 的 track 是否有 plan.md — 有则 spawn `conductor:implement {trackId}`
- [x] Task 2.4: spawn 后等待实现完成，更新 issue status 为 implemented
- [x] Task 2.5: 添加安全限制 — 每轮 SCAN 最多自动实现 N 个 issue（防止失控，N 可配置，默认 1）

### Verification

- [x] autoImplementTracked=false 时 tracked issue 被跳过并记录 note
- [x] autoImplementTracked=true 时正确 spawn conductor:implement

## Phase 3: 实现后状态流转验证

确保实现完成后 issue 状态正确更新，下轮 SCAN 能发现 implemented issue。

### Tasks

- [x] Task 3.1: 验证 conductor:implement 完成后 issue status 更新为 implemented（由 conductor 流程保证）
- [x] Task 3.2: 验证下轮 SCAN 的 issue-scanner.sh 能扫描到 implemented issue 并提取 AC（issue-scanner.sh 已实现此逻辑）
- [x] Task 3.3: 更新 SKILL.md 文档，清晰描述 autoImplementTracked 功能和工作流

### Verification

- [x] 完整闭环：tracked → auto-implement → implemented → SCAN 提取 AC → 生成测试

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: env.yaml 含配置项 | E2E | 读取 env.yaml → 验证 autoImplementTracked 字段存在且默认 false |
| 2 | AC2: false 时跳过 | E2E | 设 autoImplementTracked=false + tracked issue → 运行 SCAN → 验证跳过并记录 note |
| 3 | AC3: true 时自动实现 | E2E | 设 autoImplementTracked=true + tracked issue with plan → 运行 SCAN → 验证 spawn conductor:implement |
| 4 | AC4: 状态正确更新 | E2E | 自动实现完成 → 验证 issue status=implemented |
| 5 | AC5: SKILL.md 文档 | Skip | 检查 SKILL.md 包含 autoImplementTracked 功能描述 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs → restore
- **Skip** — Documentation verification, no runtime test needed

## Post-Implementation Checklist

- [x] env.yaml 已添加 autoImplementTracked 配置项
- [x] SKILL.md SCAN 阶段已添加自动实现逻辑
- [x] 安全限制（每轮最大实现数）已实现
- [x] SKILL.md 文档已更新描述此功能
- [ ] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
