# Implementation Plan: Case 详情页全屏 AI Assistant 分页

**Track ID:** ai-assistant-tab_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

将 CaseAIPanel 拆分为两种模式：`compact`（侧栏精简版）和 `full`（tab 全宽版）。在 CaseDetail tab 栏新增 AI Assistant tab，精简侧栏保留快捷按钮和跳转入口。两种模式共享同一份 session 状态。

## Phase 1: CaseAIPanel 模式拆分

重构 CaseAIPanel 组件，支持 `mode` prop 控制渲染模式。

### Tasks

- [x] Task 1.1: 给 CaseAIPanel 添加 `mode: 'compact' | 'full'` prop，默认 `'full'`
- [x] Task 1.2: `compact` 模式下只渲染：快捷操作按钮（Full Process / Refresh / Draft Email 等）+ "Open AI Assistant ➜" 跳转按钮 + 当前操作状态指示
- [x] Task 1.3: `full` 模式下渲染完整界面：操作按钮区（横排网格）+ 消息历史流 + 聊天输入框 + Session 管理（session 列表 / 停止按钮 / 清除按钮）
- [x] Task 1.4: `compact` 模式添加 `onOpenFull` callback prop，点击跳转按钮时触发

### Verification

- [x] TypeScript 编译通过
- [x] 现有侧栏渲染不变（传 `mode="compact"` + `onOpenFull`）

## Phase 2: CaseDetail 集成

在 CaseDetail 页面集成新 tab 和精简侧栏。

### Tasks

- [x] Task 2.1: 在 CaseDetail tab 数组添加 `{ id: 'ai', label: 'AI Assistant' }` tab
- [x] Task 2.2: `activeTab === 'ai'` 时渲染 `<CaseAIPanel mode="full" caseNumber={...} />`，占据完整内容区
- [x] Task 2.3: 右侧栏改为 `<CaseAIPanel mode="compact" caseNumber={...} onOpenFull={() => setActiveTab('ai')} />`
- [x] Task 2.4: 小屏（非 xl）底部 panel 同样改为 compact 模式

### Verification

- [x] Tab 切换到 AI Assistant 显示全宽面板
- [x] 侧栏跳转按钮点击切换到 AI tab
- [x] Session 状态在两种模式间一致

## Phase 3: Full 模式布局优化

优化 full 模式下的布局，充分利用宽屏空间。

### Tasks

- [x] Task 3.1: 操作按钮区改为横排网格布局（3-4 列），不再纵向堆叠
- [x] Task 3.2: 消息历史区域使用 flex-grow 占满剩余高度，内部滚动
- [x] Task 3.3: 聊天输入框固定在底部，宽度撑满
- [x] Task 3.4: Active operation / session 状态区域优化为横向信息条

### Verification

- [x] Full 模式下按钮区不超过 2 行
- [x] 消息区域高度自适应，不出现双重滚动条
- [x] 输入框始终可见

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Tab 栏新增 AI Assistant tab | Visual | Navigate to CaseDetail → screenshot → verify tab exists in tab bar |
| 2 | AI Assistant tab 全宽内容 | Visual | Click AI Assistant tab → screenshot → verify full-width layout with buttons + messages + input |
| 3 | 侧栏精简 + 跳转按钮 | Interaction | Navigate to CaseDetail (Inspection tab) → verify sidebar shows compact buttons + "Open AI Assistant" → click → verify activeTab switches to 'ai' |
| 4 | Session 状态共享 | Skip | State sharing via same hooks — covered by unit tests |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
