# Implementation Plan: Issues 页面各分组按修改时间排序

**Track ID:** issue-sort-by-time_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

在 `Issues.tsx` 构建各状态分组后，对每组的 issues 数组按 `updatedAt`（fallback `createdAt`）降序排序。

## Phase 1: 添加排序逻辑

在 Issues.tsx 中各分组构建完成后，添加排序。

### Tasks

- [x] Task 1.1: 在 Issues.tsx 中编写排序函数，按 `updatedAt` 降序排列（fallback `createdAt`）
- [x] Task 1.2: 对每个 issueGroup 的 issues 数组应用排序

### Verification

- [x] TypeScript 编译通过
- [x] 现有单元测试通过（91/91）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 各分组内 issue 按 updatedAt 降序 | Visual | 导航到 Issues 页面 → 截图 → 确认每组内最新修改的 issue 在最前 |
| 2 | 缺少 updatedAt 时 fallback createdAt | Skip | 前端逻辑简单，unit test 覆盖 |
| 3 | 排序在前端完成 | Skip | 代码层面确认，无需运行时验证 |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
