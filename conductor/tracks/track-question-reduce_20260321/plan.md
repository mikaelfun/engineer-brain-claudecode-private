# Implementation Plan: Create Track 交互问题过多

**Track ID:** track-question-reduce_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

修改 `issues.ts` 的 `trackAsync` 函数，在 prompt 中注入 issue JSON 上下文和精简指令，让 conductor:new-track agent 跳过冗余交互问题，直接使用预填信息生成 spec 和 plan。

## Phase 1: Prompt 增强

在 trackAsync 中构建增强 prompt，替换当前简单的 `/conductor:new-track ${id}`。

### Tasks

- [x] Task 1.1: 读取当前 `trackAsync` 函数的 prompt 构建逻辑，理解上下文
- [x] Task 1.2: 创建 `buildTrackPrompt(id, issue)` 函数，生成包含 issue 预填信息和精简指令的 prompt
- [x] Task 1.3: 在 `trackAsync` 中调用 `buildTrackPrompt`，替换原 prompt

### Verification

- [ ] TypeScript 编译通过 (`npx tsc --noEmit`)
- [ ] 后端启动无报错

## Phase 2: 验证 + 收尾

### Tasks

- [x] Task 2.1: 从 WebUI 点击 Create Track 测试，确认交互问题 ≤ 2 个
- [x] Task 2.2: 清理 `processQueryMessages` 中的 debug console.log（ISS-031 遗留）

### Verification

- [ ] 单元测试通过 (`npm test`)

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 从 WebUI 带 ISS-XXX 参数的 Create Track，交互问题 ≤ 2 个（确认 summary + 确认 spec） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 测试相关问题不在 track 创建阶段出现 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | CLI 直接调用 `/conductor:new-track`（无 ISS-XXX）的完整问答流程不受影响 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 生成的 spec.md 和 plan.md 质量不下降（仍包含完整的 acceptance criteria、phases、tasks） | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] 关联 Issue JSON (ISS-034) 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
