# Implementation Plan: Workflow Progress Persistence

**Track ID:** workflow-persist_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [ ] Not Started

## Overview

通过后端维护 workflow state + 前端刷新恢复，实现跨刷新的进度持久化。

## Phase 1: 后端 Workflow State

### Tasks

- [ ] Task 1.1: 扩展 `activeOperations` 数据结构 — 增加 currentStep、completedSteps、stepTimings
- [ ] Task 1.2: 每个步骤开始/结束时更新 workflow state
- [ ] Task 1.3: 新增 API `GET /api/case/:id/workflow-state` 返回当前 workflow 状态

### Verification

- [ ] API 返回正确的实时 workflow 状态

## Phase 2: 前端恢复

### Tasks

- [ ] Task 2.1: CaseAIPanel 挂载时调用 workflow-state API，恢复进度状态
- [ ] Task 2.2: SSE 重连时从后端推送状态快照

### Verification

- [ ] 页面刷新后正在运行的 workflow 进度正确恢复

## Final Verification

- [ ] 刷新后进度状态无丢失
- [ ] 非运行状态的 case 不受影响
- [ ] 无 console 报错

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 页面刷新后，正在运行的 case process 的进度状态可恢复 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 当前正在执行的步骤名称可恢复显示 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 已完成步骤的状态和耗时可恢复显示 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 非运行状态的 case 不受影响 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor._
