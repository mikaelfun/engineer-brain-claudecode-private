# Implementation Plan: Create Track 交互提问突出展示

**Track ID:** track-question-prominent_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

将 TrackProgressPanel 中的内联 QuestionForm 提取为独立的 Modal Dialog 组件，提问内容用更大字号展示，引用内容用 blockquote 样式突出。用户回答后 Dialog 关闭，消息流继续。

## Phase 1: 提取 QuestionDialog 组件

将现有内联 QuestionForm 重构为独立的 Modal Dialog 组件。

### Tasks

- [x] Task 1.1: 创建 `TrackQuestionDialog.tsx` 组件 — Modal overlay + 大字号提问标题 + blockquote 引用内容 + 选项按钮 + 文本输入
- [x] Task 1.2: 修改 `TrackProgressPanel.tsx` — 移除内联 QuestionForm，改为渲染 `TrackQuestionDialog`（当 pendingQuestion 存在时显示）
- [x] Task 1.3: 确保 Dialog 关闭后消息列表中保留 question 记录（现有 message kind 不变）

### Verification

- [x] TypeScript 编译通过
- [x] 现有单元测试通过（67/67）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Create Track 流程中的每个交互提问，以 Modal Dialog 形式展示（覆盖在 TrackProgressPanel 上方） | Interaction | Navigate to page → perform interaction → verify expected outcome |
| 2 | 引用内容（预填 summary、user story 等）在对话框内清晰展示，与普通 SSE 流文本区分 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 用户回答后对话框关闭，SSE 流继续正常展示 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 现有 Track 创建功能不受影响（正常创建 spec + plan） | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Phase 2: Post-Implementation Checklist

- [x] 关联 Issue ISS-049 JSON 状态已更新（→ implemented）
- [x] Track metadata.json 已更新（→ complete）
- [x] tracks.md 状态标记已更新（→ [x]）

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
