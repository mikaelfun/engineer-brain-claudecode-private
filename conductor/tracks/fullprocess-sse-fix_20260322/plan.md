# Implementation Plan: Fix Full Process SSE Messages Empty Content

**Track ID:** fullprocess-sse-fix_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

将 full-process route 的消息处理从简单的 `formatMessageForSSE()` 改为复用 steps route 的 `processStepMessages()` 函数，使两者消息格式完全一致。

## Phase 1: Refactor full-process to use processStepMessages

Replace the inline message loop in `case-routes.ts` full-process handler with a call to `processStepMessages()` from `steps.ts`.

### Tasks

- [x] Task 1.1: Export `processStepMessages` from `steps.ts` (or extract to shared module)
- [x] Task 1.2: Refactor full-process handler in `case-routes.ts` to use `processStepMessages()` instead of inline `formatMessageForSSE()` loop
- [x] Task 1.3: Add `canUseTool` for full-process (deny REPL-only tools like TodoWrite to prevent hangs, deny AskUserQuestion for automated flow)
- [x] Task 1.4: Ensure `sessions-changed` SSE events are broadcast on full-process start/complete/fail (Agent Monitor visibility)

### Verification

- [x] TypeScript compiles: `npx tsc --noEmit`
- [ ] Start dev server, trigger full-process via curl, verify SSE events have non-empty content
- [ ] Verify Agent Monitor shows full-process messages

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Full Process SSE messages contain non-empty content | API | POST /api/case/:id/process → listen SSE → assert content fields are non-empty |
| 2 | Agent Monitor shows full-process messages | Skip | Same SSE stream feeds Agent Monitor — if criterion 1 passes, this follows |
| 3 | Message format consistency between step and full-process | API | Compare SSE event shapes from step vs full-process |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] TypeScript compiles without errors
- [ ] SSE events verified via curl test
- [x] 关联 Issue JSON 状态已更新 (ISS-087)
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
