# Implementation Plan: OneNote Digest Per-Page Parallel LLM Classify + Code Merge

**Track ID:** onenote-digest-parallel_20260418
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-18
**Status:** [ ] Not Started

## Overview

将 `generate_onenote_digest()` 拆为三部分：per-page LLM 调用函数、并行调度、纯代码合并。对齐 Teams 的 `score_and_extract_single_chat()` + `merge_digest_results()` 模式。

## Phase 1: Per-Page LLM Function

新增 `classify_single_page()` 函数，对单个 `_page-*.md` 做 [fact]/[analysis] 分类。

### Tasks

- [ ] Task 1.1: 新增 `classify_single_page(page_path, case_info_head, case_number, base_url, api_key, model)` 函数
  - System prompt: OneNote page analyst，输出 JSON `{relevance, key_facts: [{tag: "fact"|"analysis", text}], summary}`
  - User prompt: case info head + 单页全文 + vision images（复用现有 `extract_images_from_page()`）
  - JSON 解析容错（与 Teams 的 `score_and_extract_single_chat` 相同模式）
  - 失败返回 `{relevance: "low", key_facts: [], summary: ""}`
- [ ] Task 1.2: 编写 system prompt，对齐 `onenote-digest-template.md` 的分类规则（命令输出→fact，假设推断→analysis，判定不清→fact）

### Verification

- [ ] 单页函数可独立调用，返回结构化 JSON

## Phase 2: Parallel Dispatch + Code Merge

替换原有 `generate_onenote_digest()` 的单 prompt 逻辑为并行调度 + 纯代码合并。

### Tasks

- [ ] Task 2.1: 新增 `merge_onenote_results(results, case_number, page_files)` 纯代码合并函数
  - 按 `onenote-digest-template.md` 格式输出：Facts 汇聚 / Analysis 汇聚 / 详细页面（`<details>` 折叠） / Summary
  - 从 per-page 结果中提取所有 `tag=fact` 的 items → `## 事实记录`
  - 从 per-page 结果中提取所有 `tag=analysis` 的 items → `## 分析记录`
  - 每页生成 `<details>` 折叠块，含 Key findings
  - 生成 1-2 句 Summary（从各页 summary 合并）
- [ ] Task 2.2: 重写 `generate_onenote_digest()` 主函数
  - ThreadPoolExecutor(max_workers=3) 并行调用 `classify_single_page()`
  - 收集结果 → 调用 `merge_onenote_results()` → 写 `onenote-digest.md`
  - 写 `_page-relevance.json` 缓存（记录每页的 relevance + mtime）
  - 保留 `--no-vision` 参数传递到 per-page 调用

### Verification

- [ ] 对现有 case 的 OneNote 数据运行新函数，对比输出质量
- [ ] `_page-relevance.json` 正确写入

## Phase 3: Cleanup + Integration

清理废弃代码，确保集成正确。

### Tasks

- [ ] Task 3.1: 在 `onenote-classifier.md` agent 文件头部标记 DEPRECATED
- [ ] Task 3.2: 对 active cases 运行 `generate-digest.py --type onenote` 验证端到端

### Verification

- [ ] 对所有 active cases 运行成功，输出 `onenote-digest.md` 格式正确

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | per-page parallel 架构正常工作 | E2E | 备份 onenote-digest.md → 运行 generate-digest.py --type onenote → 验证输出格式符合模板 → 恢复 |
| 2 | 输出保持现有模板格式 | E2E | 对比新输出与模板结构：Facts/Analysis/Details/Summary 四段均存在 |
| 3 | _page-relevance.json 正确写入 | E2E | 运行后检查 onenote/_page-relevance.json 存在且包含每页 relevance |
| 4 | onenote-classifier agent 标记废弃 | API | 读取 agent 文件验证 DEPRECATED 标记存在 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs → restore
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新为 `implemented`
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor._
