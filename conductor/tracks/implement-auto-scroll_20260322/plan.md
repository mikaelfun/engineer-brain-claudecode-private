# Implementation Plan: WebUI Issue Implement Auto-Scroll

**Track ID:** implement-auto-scroll_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Single-phase change: add a React ref to each IssueRow, and after clicking Implement, call `scrollIntoView` with a small delay to ensure ImplementPanel is mounted.

## Phase 1: Auto-Scroll on Implement Click

### Tasks

- [x] Task 1.1: Add `ref` to IssueRow outer div — use `useRef` + forward ref or callback ref pattern so the parent can access the DOM element
- [x] Task 1.2: Call `scrollIntoView` after Implement click — in the inline onClick handler, after `startImplement.mutate()`, use `setTimeout(() => ref.scrollIntoView({ behavior: 'smooth', block: 'start' }), 100)` to scroll after ImplementPanel mounts
- [x] Task 1.3: Verify no side effects on expand/collapse — ensure the ref and scroll don't interfere with existing row click behavior

### Verification

- [x] TypeScript compiles without errors
- [x] Frontend renders correctly

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 点击 Implement 后页面平滑滚动到该 issue 行 | Skip | Requires real Implement click which triggers D365 agent — covered by manual testing |
| 2 | 滚动后 issue 行和 ImplementPanel 都可见 | Skip | Same as above — requires live agent session |
| 3 | 不影响其他 issue 行的展开/折叠行为 | Interaction | Navigate to Issues page → click issue row to expand → verify expand/collapse still works normally |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 关联 Issue JSON (ISS-073) 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
