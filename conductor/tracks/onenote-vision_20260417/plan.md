# Implementation Plan: OneNote Inline Images + Vision Analysis

**Track ID:** onenote-vision_20260417
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-17
**Status:** [x] Complete

## Overview

三阶段实现：① search-inline.py 复制 assets 图片 → ② generate-digest.py 支持 vision API → ③ WebUI 后端 image API + 前端内联渲染。

## Phase 1: Data Pipeline — search-inline.py 复制 assets

修改 search-inline.py，在复制 `_page-*.md` 时扫描图片引用并复制对应 assets 文件。

### Tasks

- [x] Task 1.1: 在 search-inline.py 的页面复制逻辑（L438-466）后增加 `copy_page_assets()` 函数：扫描已复制的 `_page-*.md` 中的 `![...](./assets/xxx.png)` 引用，从 OneNote Export 源目录复制对应图片到 `{caseDir}/onenote/assets/`
- [x] Task 1.2: 处理路径 resolve — 源图片路径 = `os.path.dirname(r["path"]) + "/assets/" + filename`，目标 = `{caseDir}/onenote/assets/{filename}`。确保 assets 目录自动创建，文件名冲突时用 `{pageSafeName}_{filename}` 去重
- [x] Task 1.3: 更新复制后的 `_page-*.md` 中的图片路径 — 将 `![...](./assets/xxx.png)` 替换为 `![...](./assets/{pageSafeName}_{filename})` 以匹配去重后的文件名
- [x] Task 1.4: 添加 `--skip-assets` CLI 参数（默认 false）供降级场景使用

### Verification

- [ ] 手动运行 search-inline.py 对含图片的 case，确认 `onenote/assets/` 目录生成且图片文件存在
- [ ] 确认 _page-*.md 中的图片路径已更新

## Phase 2: LLM Vision — generate-digest.py 支持图片

修改 generate-digest.py 的 onenote 分支，将图片作为 vision content 传给 LLM API。

### Tasks

- [x] Task 2.1: 新增 `extract_images_from_page(page_path, onenote_dir, max_images=3)` 函数：解析 markdown 中的图片引用，读取对应文件并 base64 编码，返回 `[{"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}]`
- [x] Task 2.2: 修改 `generate_onenote_digest()` 中的 user_prompt 构建逻辑 — 每个页面的内容从纯文本改为 `[{"type": "text", "text": "..."}, {"type": "image_url", ...}, ...]` 格式的 content 数组
- [x] Task 2.3: 修改 `call_llm()` 支持 content 为数组格式（OpenAI vision API 兼容）— messages 中的 user content 可以是 string 或 array
- [x] Task 2.4: 添加 `--max-images-per-page` CLI 参数（默认 3），添加 `--no-vision` 降级开关
- [x] Task 2.5: 更新 system_prompt 增加图片分析指令 — 要求 LLM 从截图中提取 RBAC 配置、错误码、命令输出等关键信息

### Verification

- [ ] 手动运行 generate-digest.py 对含图片的 case，确认 LLM API 请求成功且 digest 内容包含图片分析

## Phase 3: WebUI — 后端 API + 前端内联渲染

后端新增图片服务端点，前端 OnenoteTab 升级为显示各页面内容（可折叠）+ 内联图片。

### Tasks

- [x] Task 3.1: 后端 `cases.ts` 新增 `GET /api/cases/:id/onenote/assets/:filename` — 从 `{caseDir}/onenote/assets/` 读取图片文件，返回 binary 响应（Content-Type: image/png）。增加路径安全校验防止 path traversal
- [x] Task 3.2: 修改后端 `GET /api/cases/:id/onenote` — 除了返回 digest，额外返回 `pages` 数组（各 `_page-*.md` 的 filename + content + imageCount）
- [x] Task 3.3: 前端 `CaseDetail.tsx` 重构 `OnenoteTab` — 顶部显示 digest 摘要，下方展示各 page 卡片（可折叠），每个 page 内的 `![...](./assets/...)` 渲染为 `<img src="/api/cases/${id}/onenote/assets/${filename}" />`
- [x] Task 3.4: 前端 MarkdownContent 组件（或 OnenoteTab 内部）增加自定义 image renderer — 拦截 `./assets/` 开头的图片路径，替换为 API URL
- [x] Task 3.5: 图片展示优化 — 点击放大（lightbox 或 modal），限制默认显示宽度（max-width: 100%），添加 loading 状态

### Verification

- [ ] 打开 WebUI Case 详情页 OneNote tab，确认各 page 卡片可折叠展开
- [ ] 确认图片正确内联渲染，点击可放大
- [ ] 确认 digest 摘要仍然显示在顶部

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | search-inline.py 复制 assets | E2E | 构造含图片引用的测试 page.md + assets/test.png → 运行 search-inline.py → 验证 onenote/assets/ 目录存在且图片已复制 → 验证 _page-*.md 路径已更新 → 清理 |
| 2 | 图片路径正确 resolve | E2E | 同 #1，额外验证多个 page 引用同名图片时去重命名正确 |
| 3 | generate-digest.py vision API | E2E | 备份 onenote/ → 放入含图片的 _page-*.md + assets/ → 运行 generate-digest.py --type onenote → 验证 API 请求包含 image_url content → 验证 digest 输出含图片分析 → 恢复 |
| 4 | WebUI 页面内联渲染 | Interaction | 确保 case 有 onenote 数据 → 打开 CaseDetail → 切换 OneNote tab → 验证 page 卡片可折叠 → 展开验证图片渲染 → 点击图片验证放大 |
| 5 | 后端图片服务端点 | API | curl GET /api/cases/{id}/onenote/assets/{filename} → 验证返回 image/png + 正确内容 → 测试 path traversal 攻击被拒绝 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
