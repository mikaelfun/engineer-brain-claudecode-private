# Implementation Plan: Patrol 后端重构 — 调度+预热+per-case processCaseSession

**Track ID:** patrol-backend-fix_20260327
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-27
**Status:** [~] In Progress

## Overview

重写 `_runPatrol()` 函数：保留后端 TypeScript 调度（case 发现、筛选、进度广播），增加预热阶段（PowerShell 脚本），per-case 处理改用 `processCaseSession()` + `broadcastSDKMessages()` 创建可追踪的独立 session。

## Phase 1: Case 筛选 + 预热阶段

增加 SKILL.md 定义的 case 筛选逻辑和预热步骤。

### Tasks

- [x] Task 1.1: 实现 `filterCasesByLastInspected()` 函数 — 读取每个 case 的 `casehealth-meta.json`，筛选 `lastInspected` 超过 24h 或不存在的 case
- [x] Task 1.2: 实现 `runPatrolWarmup()` 函数 — 并行执行 `check-ir-status-batch.ps1 -SaveMeta` 和 `warm-dtm-token.ps1`，使用 `child_process.execSync`，失败时记录 warning 但不阻断流程
- [x] Task 1.3: 在 `_runPatrol()` 中集成筛选和预热 — discovering 阶段后加入 filtering 和 warming-up 子阶段，SSE 广播新增 `phase: 'filtering'` 和 `phase: 'warming-up'`

### Verification

- [x] 仅 `lastInspected` 超过 24h 的 case 被处理
- [x] 预热脚本执行不阻断 patrol 流程

## Phase 2: Per-case processCaseSession 集成

替换直接 `query()` 调用，改用 `processCaseSession()` + `broadcastSDKMessages()`。

### Tasks

- [x] Task 2.1: 重写 `_runPatrol()` 的 case 处理逻辑 — 每个 case 调用 `processCaseSession(caseNumber, patrolIntent)` 而非直接 `query()`，其中 `patrolIntent` 指向 `casework/SKILL.md`
- [x] Task 2.2: 用 `broadcastSDKMessages()` 消费每个 case 的 AsyncGenerator — 复用 full-process 路由已有的 SSE 广播模式，每个 case session 自动注册到 `sessions` map + `caseIndex`
- [x] Task 2.3: 实现并发控制 — 使用 semaphore 模式限制同时运行 5 个 `processCaseSession()`，而非分批等待（避免短板效应）
- [x] Task 2.4: 集成 `acquireCaseOperationLock()` — 对每个 case 获取操作锁（intent: 'patrol'），防止 patrol 期间用户对同一 case 手动触发 full-process

### Verification

- [x] 每个 patrol case 在 Agent Monitor 显示为独立 session
- [x] Case 详情页可看到 patrol 创建的 session 和 SSE 消息流
- [x] 并发度不超过 5

## Phase 3: 进度广播 + Cancel + 错误隔离

完善进度报告、取消机制和错误处理。

### Tasks

- [x] Task 3.1: 进度广播整合 — 每个 case 完成后更新 `processedCases` 计数器，广播 `patrol-progress` 和 `patrol-case-completed` SSE 事件，确保前端 `patrolStore` 正常更新
- [x] Task 3.2: Cancel 机制 — `cancelPatrol()` 时通过 `abortQuery(caseNumber)` 终止所有正在运行的 patrol case session，释放操作锁
- [x] Task 3.3: 错误隔离 — 单个 case 的 `processCaseSession()` 失败时 catch 错误、记录到结果、释放锁、继续处理下一个 case；`patrolRunning` 标志在 finally 块正确清除
- [x] Task 3.4: 超时保护 — 单个 case 处理超过 15 分钟自动 abort，标记为 timeout 并跳过

### Verification

- [x] Dashboard patrol 进度条实时更新
- [x] Cancel 按钮可终止所有 patrol session
- [x] 单个 case 失败不影响其他 case 处理
- [x] `patrolRunning` 标志在所有场景下正确恢复

## Phase 4: PatrolProgress 类型更新 + 清理旧代码 + 测试

### Tasks

- [x] Task 4.1: 更新 `PatrolProgress` 类型 — 增加 `filtering` / `warming-up` phase，前端 `patrolStore` 同步更新对应文案
- [x] Task 4.2: 清理旧的直接 `query()` patrol 代码 — 移除 `_runPatrol()` 中的旧实现，保持函数签名不变
- [~] Task 4.3: 单元测试 — 为 `filterCasesByLastInspected()` 和 `runPatrolWarmup()` 编写测试
- [ ] Task 4.4: TypeScript 编译零错误 + 全量测试通过

### Verification

- [ ] `npm run test:server` 全部通过
- [ ] `npm run test:web` 全部通过
- [ ] TypeScript 编译零错误

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 每个 patrol case 创建独立 session | API | POST /patrol → GET /sessions/all → 验证每个 case 有独立 session 条目 |
| 2 | Agent Monitor 显示 patrol session | Skip | 依赖真实 patrol 运行 + API 验证覆盖 |
| 3 | Case 筛选只处理需要巡检的 case | Skip | 单元测试覆盖 `filterCasesByLastInspected()` |
| 4 | 预热阶段执行 | Skip | 单元测试覆盖 `runPatrolWarmup()` |
| 5 | Cancel 可终止 patrol | API | POST /patrol → POST /patrol/cancel → 验证 session 状态 |
| 6 | patrolRunning 标志正确恢复 | Skip | 单元测试覆盖 |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态由 track metadata 派生
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
