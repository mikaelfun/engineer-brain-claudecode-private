# Implementation Plan: AI Assistant Chat Queue During Step Execution

**Track ID:** chat-queue_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Frontend-only change: remove the `isStepActive` disable from chat input, add a `queuedMessages` array state, and auto-drain the queue when the step completes. No backend changes needed — the existing `POST /case/:id/chat` API and session resume mechanism are reused as-is.

## Phase 1: Enable Input & Queue Messages

Enable chat input during step execution and queue messages locally.

### Tasks

- [x] Task 1.1: Remove `isStepActive` from chat input/button disabled conditions in `CaseAIPanel.tsx`; update placeholder to "Message AI (queued while busy)..." when step is active
- [x] Task 1.2: Add `queuedMessages` state array and modify `handleChat()` — if `isStepActive`, push to queue with "queued" status and show in chat; otherwise send normally
- [x] Task 1.3: Show queued messages in chat stream with a "⏳ Queued" badge (add `queued` type to message store or use local state overlay)

### Verification

- [ ] TypeScript compiles without errors
- [ ] Can type and "send" messages while step is running — messages appear with queued badge

## Phase 2: Auto-Drain Queue on Step Completion

Auto-send queued messages when the step finishes.

### Tasks

- [x] Task 2.1: Add a `useEffect` that watches for step completion (`selectedSessionStatus` changing from `active` to `paused`/`completed`); when detected, drain `queuedMessages` sequentially via `POST /case/:id/chat`
- [x] Task 2.2: Update queued message status from "queued" → normal as each is sent; handle errors gracefully (mark failed, keep remaining in queue)
- [x] Task 2.3: Add unit tests for queue drain logic

### Verification

- [ ] `npm run test:web` passes
- [ ] Queued messages auto-send after step completes

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Chat input enabled during step execution | Skip | Unit test: verify disabled prop logic |
| 2 | Queued messages shown with badge | Skip | Component test: render with queued messages |
| 3 | Queue auto-drains on step completion | Skip | Unit test: mock status change → verify API calls |
| 4 | Multiple messages sent in order | Skip | Unit test: verify sequential drain |
| 5 | Queue cleared after processing | Skip | Unit test: verify state cleanup |

**Test Type Legend:**
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
