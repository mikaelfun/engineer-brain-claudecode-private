# Implementation Plan: Phase Retrospective — test-loop 阶段回顾机制

**Track ID:** phase-retro_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [x] Complete

## Overview

纯 SKILL.md prompt 工程改动。在 test-loop 的执行流程中插入 Phase Retrospective 步骤，并增强 FIX 阶段对 retroContext 的处理。总共修改 1 个文件：`.claude/skills/test-loop/SKILL.md`。

## Phase 1: 新增 Step 2.2 Phase Retrospective

在 SKILL.md 的 Step 2（更新状态）之后、Step 2.1（续跑判断）之前插入新步骤。

### Tasks

- [x] Task 1.1: 在 SKILL.md Step 2 之后插入 `### Step 2.2: Phase Retrospective（阶段回顾）` 章节
- [x] Task 1.2: 编写 Retrospective 的 Agent 自问清单（4 个问题：输入/输出比例、重复无效工作、运行时 vs 逻辑缺陷、定位具体文件）
- [x] Task 1.3: 编写"发现逻辑 bug"时的三步操作指令（learnings-writer + fixQueue item + self-heal-recorder），含完整 bash 命令模板
- [x] Task 1.4: 编写 retroContext JSON schema 定义（phase/round/anomaly/rootCause/targetFile/targetLine）
- [x] Task 1.5: 添加约束规则：每轮每阶段最多 1 个 retro fix item、不修改代码只创建 item、无异常时零输出

### Verification

- [x] SKILL.md 中 Step 2.2 位于 Step 2 和 Step 2.1 之间，结构完整

## Phase 2: 增强 FIX 阶段 retroContext 处理

修改 SKILL.md 的 FIX 阶段，让 opus agent 能识别并利用 retroContext 定位修复。

### Tasks

- [x] Task 2.1: 在 FIX 阶段的 Framework Fix 路径中，增加对 fixQueue item 含 `retroContext` 字段时的处理逻辑
- [x] Task 2.2: 编写 FIX agent prompt 模板，指导其从 retroContext 中提取 targetFile/targetLine/rootCause 直接定位修复
- [x] Task 2.3: 更新 Step 2.1 续跑判断的注释，说明 Retrospective 可能新增 fixQueue item 影响 next phase 判断

### Verification

- [x] FIX 阶段 Framework Fix 路径能识别 retroContext 字段
- [x] opus agent prompt 包含 retroContext 上下文注入

## Phase 3: 文档与集成

### Tasks

- [x] Task 3.1: 更新 test-supervisor modes/dashboard.md（如需展示 retro fix 状态）— 跳过：dashboard.md 读 state.json 的 phaseHistory，retro_escalate action 已自动包含
- [x] Task 3.2: 在 SKILL.md 的注意事项章节补充 Phase Retrospective 相关说明

### Verification

- [x] SKILL.md 整体结构一致性检查（Step 编号连续、引用正确）

## Post-Implementation Checklist

- [x] SKILL.md 结构完整性验证（Step 编号、引用、flow diagram）
- [x] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
