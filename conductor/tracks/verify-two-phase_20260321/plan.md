# Implementation Plan: Verify Two-Phase — Track Plan + Agent-Driven Execution

**Track ID:** verify-two-phase_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

Two changes: (1) Update conductor:new-track SKILL.md to include Verification Plan in generated
plan.md, and (2) Refactor verify route to use Claude agent for UI tests instead of hardcoded
`node scripts/browser-test.mjs`.

## Phase 1: Plan Template — Add Verification Plan Section

Update the `conductor:new-track` SKILL.md plan template to generate a `## Verification Plan`
section in each new track's plan.md.

### Tasks

- [x] Task 1.1: Add `## Verification Plan` section to plan template in `.claude/skills/conductor/new-track/SKILL.md`
  - Place after the last Phase section, before `## Post-Implementation Checklist`
  - Template format:
    ```markdown
    ## Verification Plan

    | # | Acceptance Criterion | Test Type | Test Steps |
    |---|---------------------|-----------|------------|
    | 1 | {criterion from spec} | Interaction/Visual/API/Skip | {high-level steps} |
    | 2 | ... | ... | ... |

    **Test Type Legend:**
    - **Interaction** — Playwright clicks, form fills, state assertions
    - **Visual** — Navigate + screenshot + visual inspection
    - **API** — curl/fetch endpoint + assert response
    - **Skip** — Backend-only or covered by unit tests
    ```
  - Include instruction to classify based on workflow.md Step 2a rules

### Verification

- [ ] Review updated SKILL.md template
- [ ] Verify the template would produce a useful Verification Plan for a sample Issue

## Phase 2: Verify Route — Agent-Driven UI Tests

Refactor `dashboard/src/routes/issues.ts` verify route to replace `node scripts/browser-test.mjs`
with a Claude agent that reads the Verification Plan and executes it.

### Tasks

- [x] Task 2.1: Refactor `verifyAsync()` Step 2 to use Claude SDK `query()` instead of `exec()`
  - Keep Step 1 (`npm test`) as-is using existing `runStep()` helper
  - Replace Step 2: instead of `exec('node scripts/browser-test.mjs')`, call `query()` with:
    - Prompt that references the track's plan.md Verification Plan
    - Instructions from workflow.md Step 2 (interaction patterns, safety guards)
    - `allowedTools: ['Bash', 'Read', 'Glob', 'Grep']` — no Write/Edit needed
    - `permissionMode: 'acceptEdits'` for Bash commands (Playwright needs shell)
    - `maxTurns: 30`
  - Stream agent messages as SSE events (same pattern as implement route)

- [x] Task 2.2: Build the verification prompt
  - Read plan.md → extract `## Verification Plan` table
  - Read workflow.md → extract Playwright patterns + safety guards
  - Compose prompt:
    ```
    Execute the Verification Plan from plan.md for track {trackId}.

    For each test:
    1. If type=Interaction: generate and run Playwright script via Bash
    2. If type=Visual: navigate + screenshot + report
    3. If type=API: curl/fetch + assert
    4. If type=Skip: note as skipped

    SAFETY: Never click Full Process, Troubleshoot, Draft Email, Execute buttons.

    Report results as: ✅ PASS or ❌ FAIL per criterion, with evidence.
    ```

- [x] Task 2.3: Parse agent results and build VerifyResult
  - After agent completes, extract pass/fail from the final assistant message
  - Map to existing VerifyResult structure: `{ unitTest, uiTest, overall }`
  - `uiTest.passed` = true only if all non-skip criteria passed
  - `uiTest.output` = agent's final summary text

- [x] Task 2.4: Handle fallback when no Verification Plan exists
  - If plan.md has no `## Verification Plan` section (old tracks):
    - Fall back to existing behavior: `node scripts/browser-test.mjs` if exists
    - Otherwise skip UI tests with message "No Verification Plan found, UI tests skipped"
  - This ensures backward compatibility

### Verification

- [ ] Run `npm test` — all unit tests pass (no regressions)
- [ ] Manual test: create a track with new-track, verify plan.md has Verification Plan
- [ ] Manual test: click Verify on an issue with Verification Plan, observe agent execution via SSE

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON (ISS-058) 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
