# Specification: 合并 casework-ar 到 casework — 消除 AR 路由依赖

**Track ID:** merge-ar-casework_20260417
**Type:** Refactor
**Created:** 2026-04-17
**Status:** Draft
**Issue:** ISS-224

## Summary

将 casework-ar 独立 skill（assess-ar、act-ar）的 AR 逻辑合并到主 casework 的 assess/act SKILL.md 中，通过 `isAR` 分支实现统一路由，消除调用方（patrol、dashboard、CLI）需要自行判断 case number 长度来路由到不同 skill 的问题。

## Context

当前架构中 casework-ar 作为独立 skill 存在，但所有调用方（patrol、dashboard 单 case 按钮、CLI `/casework`）都没有实现 case number 长度路由，导致 AR case 全部走了普通 casework 流程。这造成：
- emails 从 AR case 自己拉（空的，邮件挂在 main case 上）
- isAR=false，data-refresh-output.json 错误
- assess 基于 0 封邮件做出错误判断

data-refresh.sh 已完成 auto-detect（case number >= 19 位自动 isAR=true + 截取 mainCase），casework SKILL.md 已有 AR 分路表。剩余工作是让 assess 和 act 的 SKILL.md 内联 AR 分支逻辑。

## Motivation

消除 AR 路由依赖，使所有 case 无论 AR 与否都走统一的 `/casework` 入口，AR 检测下沉到脚本/模板层自动处理。

## Acceptance Criteria

- [ ] AC1: assess SKILL.md 包含 AR 分支（AR scope 提取 + communicationMode 检测 + AR status 判断规则 + AR-specific compliance SAP 检查）
- [ ] AC2: act SKILL.md 包含 AR 分支（按 communicationMode 选收件人 + troubleshooter 限定 AR scope）
- [ ] AC3: casework-ar/ 目录已删除，无残留引用
- [ ] AC4: 端到端验证 — AR case 通过统一 `/casework` 入口能正确检测 AR、拉到 main case emails、走 AR assess/act 路径

## Dependencies

- data-refresh.sh AR auto-detect 已完成（已实现）
- casework SKILL.md 已有 AR 分路表（已实现）

## Out of Scope

- patrol SKILL.md 不需要修改（统一走 casework）
- dashboard 不需要修改
- data-refresh.sh 不需要修改（已完成）
- summarize SKILL.md 不需要修改（已有 AR 规则）

## Technical Notes

### 合并策略
1. **assess**: 在 Step 2 compliance 之后加 AR 分支（Step 2.5: AR scope 提取 + communicationMode 检测），Step 4 LLM prompt 加 AR context 和 AR 判断规则
2. **act**: 在 Step 3 action 执行时根据 `isAR` 分支选择 spawn prompt 模板（AR 版含 scope + communicationMode + 收件人选择逻辑）
3. **删除**: casework-ar/ 整个目录（SKILL.md + assess-ar/SKILL.md + act-ar/SKILL.md）

### 关键 AR 逻辑（从 assess-ar 迁移）
- AR scope 提取（从 notes-ar.md + case-info.md AR 字段）
- communicationMode 检测（从 emails.md To/CC 字段检查工程师参与）
- AR SAP 三层检查（使用 `| AR Support Area Path |` 行）
- AR status 判断（双模式：internal vs customer-facing）

### 关键 AR 逻辑（从 act-ar 迁移）
- spawn prompt 模板含 AR scope + communicationMode
- 收件人按模式选择：internal → case owner / customer-facing → customer + CC owner
- troubleshooter 限定 AR scope

---

_Generated by Conductor. Review and edit as needed._
