# Implementation Plan: 合并 casework-ar 到 casework — 消除 AR 路由依赖

**Track ID:** merge-ar-casework_20260417
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-17
**Status:** [~] In Progress

## Overview

将 assess-ar 和 act-ar 的 AR 逻辑以 `isAR` 分支形式合并到主 assess/act SKILL.md 中，然后删除 casework-ar/ 目录。所有修改都是 SKILL.md 文本变更（LLM 指令），无代码/脚本修改。

## Phase 1: assess SKILL.md 合并 AR 分支

从 `assess-ar/SKILL.md` 提取 AR 特有逻辑，以 `isAR` 条件分支形式合并到主 `assess/SKILL.md`。

### Tasks

- [x] Task 1.1: 在 Step 2 (Compliance gate) 中增加 AR SAP 特化规则 — 当 `isAR=true` 时，SAP 三层检查使用 `| AR Support Area Path |` 行，compliance 额外保存 `arSapPath` 字段
- [x] Task 1.2: 在 Step 2 和 Step 3 之间插入新的 Step 2.5 "AR Enrichment"（仅 `isAR=true` 时执行）— AR scope 提取 + communicationMode 检测 + AR-specific OneNote/Teams 关键词调整
- [x] Task 1.3: 在 Step 4 (主 LLM prompt) 中增加 AR 判断分支 — 当 `isAR=true` 时注入 AR context（scope、communicationMode、caseOwnerEmail）和 AR 双模式 status 判断规则
- [x] Task 1.4: 更新 assess SKILL.md 的输入/输出契约 — 输入增加 `notes-ar.md`（可选），输出的 casework-meta.json 增加 `ar.*` 字段

### Verification

- [ ] assess SKILL.md 包含完整 AR 分支，覆盖 assess-ar 的所有逻辑（scope 提取、communicationMode 检测、AR SAP、AR status judge）

## Phase 2: act SKILL.md 合并 AR 分支

从 `act-ar/SKILL.md` 提取 AR 特有逻辑，以 `isAR` 条件分支形式合并到主 `act/SKILL.md`。

### Tasks

- [x] Task 2.1: 在 Step 2 (模式检测) 后增加 AR context 读取 — 当 `isAR=true` 时从 casework-meta.json 读取 `ar.scope`、`ar.communicationMode`、`ar.caseOwnerEmail`、`mainCaseId`
- [x] Task 2.2: 在 Step 3 (执行 actions) 的 troubleshooter spawn 模板中增加 AR 分支 — AR 版 prompt 包含 scope 限定 + main case ID
- [x] Task 2.3: 在 Step 3 的 email-drafter spawn 模板中增加 AR 分支 — 按 communicationMode 选收件人（internal → case owner / customer-facing → customer + CC owner）
- [x] Task 2.4: 在路由参考表中增加 AR 行 — 按 communicationMode 区分的 AR 典型 actions
- [x] Task 2.5: 更新 act SKILL.md 的输入契约 — 增加 casework-meta.json 的 `ar.*` 字段依赖说明

### Verification

- [ ] act SKILL.md 包含完整 AR 分支，覆盖 act-ar 的所有逻辑（AR context 读取、spawn 模板、收件人选择）

## Phase 3: 删除 casework-ar 目录 + 清理引用

### Tasks

- [x] Task 3.1: 删除 `.claude/skills/casework-ar/` 整个目录（含 SKILL.md、assess-ar/SKILL.md、act-ar/SKILL.md）
- [x] Task 3.2: 确认 casework SKILL.md 的 AR 分路表已与 Phase 1/2 的修改一致（"AR 分路"节已存在，检查无冲突）
- [x] Task 3.3: 验证无残留引用 — grep "casework-ar" 全项目确认只剩 conductor 历史文件和本 track

### Verification

- [ ] casework-ar/ 目录已不存在
- [ ] grep "casework-ar" 仅返回 conductor 历史文件（PRD、旧 plan），无活跃 skill 引用

## Phase 4: 端到端验证 + 收尾

### Tasks

- [ ] Task 4.1: 选取一个真实 AR case（从 `cases/active/` 中找 case number ≥ 19 位的），运行 `/casework {arCaseNumber}` 完整流程，验证 data-refresh 检测 AR → assess 走 AR 分支 → act 走 AR 分支
- [ ] Task 4.2: 检查产出文件 — casework-meta.json 含 `isAR=true`、`ar.scope`、`ar.communicationMode`；emails.md 从 main case 拉取（非空）

### Verification

- [ ] AR case 通过统一 `/casework` 入口完整处理，无报错

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | assess SKILL.md 包含 AR 分支 | E2E | 选取 AR case → 运行 casework → 检查 casework-meta.json 含 ar.scope + ar.communicationMode + AR status 判断 |
| 2 | act SKILL.md 包含 AR 分支 | E2E | 检查 execution-plan.json actions → 验证 act spawn prompt 包含 AR scope + communicationMode |
| 3 | casework-ar/ 目录已删除 | E2E | `ls .claude/skills/casework-ar/` 返回不存在 + `grep -r "casework-ar" .claude/skills/` 无结果 |
| 4 | 端到端 AR case 处理正确 | E2E | Backup AR case data → 运行 `/casework {arCaseNumber}` → 验证 emails.md 非空（从 main case 拉取）+ meta.json isAR=true → restore |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新（注意：不直接写 status，由 track metadata 派生）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
