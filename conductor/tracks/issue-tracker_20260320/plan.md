# Implementation Plan: Dashboard Issue Tracker

**Track ID:** issue-tracker_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

5 个阶段：CLI skill → 数据模型 → 后端 API → 前端页面 → 数据迁移。CLI 先行，确保 Dashboard 挂了也能记录。

## Phase 1: CLI Skill + 数据模型

### Tasks

- [x] Task 1.1: 创建 `issues/` 目录，定义 issue JSON schema
- [x] Task 1.2: 在 `dashboard/src/types/index.ts` 添加 Issue 类型定义
- [x] Task 1.3: 创建 `.claude/skills/issue/SKILL.md` — CLI 入口（`/issue "标题"` 或交互式）

### Verification

- [x] `/issue "test"` 可创建 JSON 文件到 issues/

## Phase 2: 后端存储和 API

### Tasks

- [x] Task 2.1: 实现 `dashboard/src/services/issue-reader.ts` — CRUD 操作
- [x] Task 2.2: 创建 `dashboard/src/routes/issues.ts` — REST API（GET list/detail, POST create, PUT update, DELETE）
- [x] Task 2.3: 注册路由到 app

### Verification

- [x] API 端点可正确操作 issues/ 目录

## Phase 3: 前端页面

### Tasks

- [x] Task 3.1: 创建 Issues 列表页 — 分页、状态/类型/优先级筛选、内联创建表单
- [x] Task 3.2: Issue 行内展开详情 + 编辑（标题、描述、类型、优先级、状态、trackId）
- [x] Task 3.3: 注册路由到 App.tsx，添加顶部导航入口

### Verification

- [x] 可在 WebUI 创建、查看、编辑、筛选 issues

## Phase 4: 数据迁移

### Tasks

- [x] Task 4.1: 将 FEATURE_REQUESTS.md 中现有条目迁移为 issues/ JSON 文件（ISS-001 ~ ISS-010）
- [x] Task 4.2: 将已有 conductor tracks 关联到对应 issue（trackId 字段已设置）

### Verification

- [x] 所有现有问题在 Issues 页面和 CLI 中正确显示

## Final Verification

- [x] CLI `/issue` 可创建 issue
- [x] WebUI Issues 页面 CRUD 正常
- [x] 分页和筛选正常
- [x] CLI 和 WebUI 数据一致
- [x] TypeScript 前后端编译通过，零错误

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | CLI skill `/issue "描述"` 可直接创建 issue（Dashboard 挂了也能记录） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Dashboard 新增 Issues 页面（顶部导航入口） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 支持创建 issue：标题 + 描述 + 类型（bug/feature/refactor/chore）+ 优先级（P0/P1/P2） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | Issue 列表分页显示，支持按状态筛选（pending/tracked/in-progress/done） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | Issue 详情页显示完整描述，关联的 conductor track ID（如有） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 数据存储在 `issues/` 目录（每个 issue 一个 JSON 文件） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 7 | CLI 和 WebUI 读写同一个 `issues/` 目录，数据天然同步 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 8 | 已有 conductor tracks 的问题在 issue 列表中显示 tracked 状态 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor._
