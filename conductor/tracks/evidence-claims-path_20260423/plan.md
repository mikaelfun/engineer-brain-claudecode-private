# Implementation Plan: WebUI Evidence界面 claims.json 路径不匹配

**Track ID:** evidence-claims-path_20260423
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-23
**Status:** [ ] Not Started

## Overview

单行修复：将后端 API 的 claims.json 读取路径从 `join(caseDir, 'claims.json')` 更正为 `join(caseDir, '.casework', 'claims.json')`。

## Phase 1: Fix claims.json path

修正后端 API 路径。

### Tasks

- [x] Task 1.1: 修改 `dashboard/src/routes/cases.ts` 中 `GET /api/cases/:id/claims` 的 claimsPath 为 `join(caseDir, '.casework', 'claims.json')`

### Verification

- [ ] TypeScript 编译通过 (`npx tsc --noEmit`)
- [ ] 现有单元测试通过 (`npm test`)

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | API 正确读取 .casework/claims.json | API | curl GET /api/cases/{有claims的case}/claims → assert 返回 claims 数组非空 |
| 2 | Evidence Tab 正常渲染 | Visual | Playwright 导航到有 claims 的 case → Evidence Tab → 截图验证表格渲染 |
| 3 | 无 claims 时返回 null | API | curl GET /api/cases/{无claims的case}/claims → assert 返回 `{ claims: null }` |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
