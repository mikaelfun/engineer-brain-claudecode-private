# Implementation Plan: Fix step session not visible in Agent Monitor

**Track ID:** step-session-visible_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Add SSE-driven React Query invalidation: broadcast `sessions-changed` SSE event when step sessions start/complete, and have Agent Monitor's `useUnifiedSessions` hook trigger a refetch on this event.

## Phase 1: Backend — Broadcast sessions-changed SSE

### Tasks

- [x] Task 1.1: In `dashboard/src/routes/steps.ts`, broadcast `sessions-changed` SSE event right after the `case-step-started` broadcast and right after the `case-step-completed`/`case-step-failed` broadcasts
- [x] Task 1.2: Verify TypeScript compiles: `npx tsc --noEmit` in `dashboard/`

### Verification

- [x] SSE event `sessions-changed` is broadcast at step start and completion

## Phase 2: Frontend — SSE-triggered React Query invalidation

### Tasks

- [x] Task 2.1: In `dashboard/web/src/hooks/useSSE.ts`, add SSE event listener for `sessions-changed` that calls `queryClient.invalidateQueries` for the sessions query key
- [x] Task 2.2: Verify TypeScript compiles: `npx tsc --noEmit` in `dashboard/web/`

### Verification

- [x] Agent Monitor shows step sessions even for fast-completing steps

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Step sessions appear in Agent Monitor | Skip | Requires live SDK session — manual verification |
| 2 | Session shows correct status lifecycle | Skip | Requires live SDK session — manual verification |
| 3 | Agent Monitor auto-refreshes on session changes | Skip | Backend-only SSE + React Query invalidation logic |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
