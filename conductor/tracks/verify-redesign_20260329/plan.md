# Implementation Plan: Conductor Verify 重新设计 — 推理驱动 + Recipe 自我演进

**Track ID:** verify-redesign_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [x] Complete

## Overview

分 3 个阶段：先建 recipe 基础设施和初始内容，再重写 verify skill 的核心流程，最后更新 workflow.md 文档。

## Phase 1: Recipe 基础设施 + 初始内容

建立 verification-recipes 目录结构，编写 4 个初始 recipe（从现有 learnings.yaml / ERRORS.md / workflow.md 提炼）。

### Tasks

- [x] Task 1.1: 创建 `playbooks/guides/verification-recipes/_index.md` — 匹配规则 + 演进日志表
- [x] Task 1.2: 创建 `playbooks/guides/verification-recipes/web-ui-playwright.md` — Edge + JWT + ports + screenshot 的完整 recipe（从 workflow.md UI Screenshot Verification 和 learnings.yaml 提炼）
- [x] Task 1.3: 创建 `playbooks/guides/verification-recipes/cli-script-output.md` — CLI/shell 脚本输出验证 recipe（determinism + line count + diff）
- [x] Task 1.4: 创建 `playbooks/guides/verification-recipes/api-endpoint.md` — API 端点验证 recipe（curl + JWT auth + response shape assert）
- [x] Task 1.5: 创建 `playbooks/guides/verification-recipes/file-content-check.md` — 文件内容/格式验证 recipe（wc + grep + diff + JSON schema）

### Verification

- [x] 目录结构完整，5 个文件均存在且格式符合设计规范

## Phase 2: 重写 conductor:verify SKILL

创建 `.claude/skills/conductor/verify/SKILL.md`，实现 5 步新流程。

### Tasks

- [x] Task 2.1: 创建 `.claude/skills/conductor/verify/SKILL.md`，定义 5 步流程结构（Step 1: Regression guard / Step 2: 推理验证方案 / Step 3: 执行验证 / Step 4: 汇总报告 / Step 5: 反思提取）
- [x] Task 2.2: Step 2 详细实现 — 读 spec → 提取验收标准 → 逐条推理验证类型 → 查 `_index.md` 匹配 recipe
- [x] Task 2.3: Step 3 详细实现 — 有 recipe 按步骤执行 / 无 recipe 从零推理生成脚本 → 记录执行结果和 evidence
- [x] Task 2.4: Step 5 详细实现 — 反思触发条件判断（重试>=2 / 遇坑 / 脚本>10行 / recipe 过时）→ 创建或更新 recipe 文件 → 更新 `_index.md` 演进日志

### Verification

- [x] SKILL.md 完整定义 5 步流程，每步有明确输入/输出/错误处理

## Phase 3: 文档更新 + 收尾

更新 workflow.md 中的 verify 流程说明，确保文档一致性。

### Tasks

- [x] Task 3.1: 更新 `conductor/workflow.md` Step 2 — 替换为新的推理驱动流程引用，指向 SKILL.md
- [x] Task 3.2: 更新 `conductor/workflow.md` Step 2a — 保留验收标准分类表（E2E/Interaction/Visual/API/Skip）作为 LLM 推理参考，但标注为"参考而非强制"

### Verification

- [x] workflow.md 更新完整，无冲突引用

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: verify 读 spec 推理验证类型 | Skip | 需要运行完整 conductor:verify（LLM 推理），非自动化可测 |
| 2 | AC2: recipes 目录结构 + 4 初始 recipe | E2E | 检查 `playbooks/guides/verification-recipes/` 下 5 个文件存在 → 验证每个 recipe 包含必需 sections（匹配条件 / 前置检查 / 执行步骤 / 常见坑） |
| 3 | AC3: verify 匹配 recipe 执行 | Skip | 需要运行 conductor:verify 对真实 track，非自动化可测 |
| 4 | AC4: 无匹配 recipe 从零推理 | Skip | 需要运行 conductor:verify，非自动化可测 |
| 5 | AC5: 反思提取触发条件 | Skip | 需要运行 conductor:verify 遇到复杂验证，非自动化可测 |
| 6 | AC6: npm test regression guard 保留 | E2E | 读取 SKILL.md → 验证 Step 1 包含 `npm test` 命令 |
| 7 | AC7: 汇总报告 + metadata.json | E2E | 读取 SKILL.md → 验证 Step 4 输出格式定义 + metadata.json 写入逻辑 |
| 8 | AC8: workflow.md 更新 | E2E | Diff workflow.md → 确认 Step 2 指向新 SKILL.md，Step 2a 标注为"参考" |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — LLM 推理验证，无法自动化测试（仅能在真实 verify 运行中验证）

## Post-Implementation Checklist

- [x] 5 个 recipe 文件 + SKILL.md 内容完整且格式一致
- [x] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
