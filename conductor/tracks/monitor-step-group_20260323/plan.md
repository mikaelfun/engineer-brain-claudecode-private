# Implementation Plan: Agent Monitor Step-Grouped Message Display

**Track ID:** monitor-step-group_20260323
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-23
**Status:** [x] Complete

## Overview

在 `SessionMessageList` 中新增 `groupByStep` 模式：将消息按 `step` 字段分组，每组有折叠的 step header（名称 + 状态），默认只展开最后一个 step。`CaseSessionDetail` 传入 `groupByStep={true}` 启用。

## Phase 1: SessionMessageList — Step 分组渲染

### Tasks

- [x] Task 1.1: 在 `SessionMessageList.tsx` 中新增 `groupMessagesByStep()` 函数，将 `CaseSessionMessage[]` 按 `step` 字段分组为 `StepGroup[]`（每组含 stepName、status、messages）。无 step 的消息归入 `__general__` 组。
- [x] Task 1.2: 新增 `StepGroupSection` 组件，渲染单个 step 分组：header 行（step 名称 + 状态 icon + 消息数 + 折叠 chevron）+ 折叠体（内部复用现有 `processMessages()` + 消息渲染逻辑）
- [x] Task 1.3: 在 `SessionMessageList` 中新增 `groupByStep?: boolean` prop，当为 true 时走 step 分组渲染路径，默认 false 保持现有行为
- [x] Task 1.4: 在 `AgentMonitor.tsx` 的 `CaseSessionDetail` 中，传入 `groupByStep={true}`
- [x] Task 1.5: TypeScript 编译验证：`npx tsc --noEmit` in `dashboard/web/`

### Verification

- [x] Agent Monitor case session 展开后显示 step 分组
- [x] 非 case session（implement/verify/track-creation）不受影响

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Messages grouped by step | Skip | Requires live SDK session — manual verification |
| 2 | Step header shows name and status | Skip | Requires live SDK session — manual verification |
| 3 | Step groups collapsible | Skip | Requires live SDK session — manual verification |
| 4 | Non-case sessions unaffected | Skip | No functional change to other session types |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
