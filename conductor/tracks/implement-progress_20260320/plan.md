# Implementation Plan: Issue Implement 实时进度展示

**Track ID:** implement-progress_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

分 4 阶段：后端 session 管理 → 前端 store + SSE 消费 → UI 面板组件 → 测试收尾。参照 case-session-manager 模式但保持轻量（只读进度，不支持 chat/resume）。

## Phase 1: 后端 — Implement Session Manager + 路由改造

后端核心：新建 implement-session-manager.ts，改造 start-implement 路由。

### Tasks

- [x] Task 1.1: 创建 `dashboard/src/agent/implement-session-manager.ts` — 操作锁（`activeImplements: Map<issueId, { issueId, trackId, startedAt }>`）+ 持久化消息存储（`implementMessages: Map<issueId, messages[]>`，上限 100 条）+ `acquireImplementLock` / `releaseImplementLock` / `getImplementMessages` / `clearImplementMessages` / `appendImplementMessage` API
- [x] Task 1.2: 改造 `dashboard/src/routes/issues.ts` 的 `start-implement` 路由 — 引入操作锁（`acquireImplementLock`）、SDK 消息流细化为 thinking/tool-call/tool-result 三种 SSE 事件（复用 `sse-helpers.ts` 的 `getSSEEventType` + `formatMessageForSSE`）、每条消息 `appendImplementMessage` 持久化、完成/失败时 `releaseImplementLock` + 更新 issue 状态
- [x] Task 1.3: 新增 `GET /api/issues/:id/implement-status` 路由 — 返回 `{ active: boolean, messages: [...], startedAt? }` 用于页面刷新后恢复

### Verification

- [x] `npx tsc --noEmit` 通过
- [x] 手动 curl 测试：POST start-implement → 返回 202 + 锁生效 → 重复 POST 返回 409 → GET implement-status 返回消息

## Phase 2: 前端 — Store + SSE 消费

### Tasks

- [x] Task 2.1: 创建 `dashboard/web/src/stores/implementStore.ts` — Zustand store，字段：`implementSessions: Map<issueId, { status, messages[], startedAt }>`, actions：`addMessage`, `setStatus`, `clearSession`
- [x] Task 2.2: 修改 `dashboard/web/src/hooks/useSSE.ts` — 新增 `issue-implement-started` / `issue-implement-progress` / `issue-implement-completed` / `issue-implement-error` 四种事件监听，dispatch 到 implementStore + 相应 query invalidation
- [x] Task 2.3: 新增 `useImplementStatus(issueId)` hook 到 `hooks.ts` — GET `/api/issues/:id/implement-status`，用于页面加载时恢复进行中的 implement 状态到 store

### Verification

- [x] `npx tsc --noEmit`（含 `dashboard/web/`）通过
- [x] 在 console 中确认 SSE 事件能被正确路由到 implementStore

## Phase 3: 前端 — ImplementPanel UI 组件

### Tasks

- [x] Task 3.1: 创建 `dashboard/web/src/components/ImplementPanel.tsx` — 内联面板组件，props：`issueId, trackId, isExpanded`。展示：status 标签（Running/Completed/Failed）+ 消息列表（💭 thinking / 🔧 tool-call / ✅ tool-result 图标映射）+ loading spinner + 自动滚动到底部
- [x] Task 3.2: 修改 `dashboard/web/src/pages/Issues.tsx` — implement 按钮点击后展开 ImplementPanel（`expandedImplementId` state）、有 active implement 时按钮 disabled + spinner、页面加载时调 `useImplementStatus` 恢复 expandedImplementId、完成/失败后保持面板展示最终结果

### Verification

- [x] `npx tsc --noEmit`（含 `dashboard/web/`）通过
- [x] 手动在 dashboard 验证：点击 Implement → 面板展开 → 实时消息流 → 完成后显示结果

## Phase 4: 测试 + 收尾

### Tasks

- [x] Task 4.1: 单元测试 — `implement-session-manager.test.ts`（操作锁 acquire/release、持久化消息 append/get/clear、上限截断）+ `issues.test.ts` 新增 implement-status 和改造后 start-implement 的测试用例
- [x] Task 4.2: `browser-test.mjs` 确认 `/issues` 路由仍通过（无 ErrorBoundary / Maximum update depth）
- [x] Task 4.3: 更新 ISS-015 状态为 done + 更新 track metadata + tracks.md

### Verification

- [x] `npm test` — 所有测试通过
- [x] `npx tsc --noEmit` — 两端类型检查通过

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] browser-test.mjs 已覆盖新页面/路由（如有 UI 变更）
- [x] 关联 Issue JSON 状态已更新（ISS-015 → done）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
