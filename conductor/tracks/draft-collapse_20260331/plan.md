# Implementation Plan: Draft Tab 折叠优化

**Track ID:** draft-collapse_20260331
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-31
**Status:** [x] Complete

## Overview

将 CaseDetail.tsx 中的 DraftsTab 改为只展开最新 draft，历史 draft 折叠显示。复用或参考 DraftsPage.tsx 的 DraftCard 折叠组件。

## Phase 1: Draft Tab 折叠实现

修改 CaseDetail.tsx 的 DraftsTab 组件，实现最新展开 + 历史折叠。

### Tasks

- [x] Task 1.1: 分析 DraftsPage.tsx 的 DraftCard 折叠逻辑，确定复用方案（提取共享组件 or 内联实现）
- [x] Task 1.2: 修改 DraftsTab — 按时间排序 draft，最新的默认展开，其余折叠（带展开/收起 chevron）
- [x] Task 1.3: 折叠态显示 draft 基本信息（draft 类型、生成时间），展开态显示完整内容

### Verification

- [ ] Draft tab 只展开最新 draft
- [ ] 历史 draft 折叠显示，可点击展开

## Phase 2: 测试 & 收尾

### Tasks

- [x] Task 2.1: 编写 DraftsTab 组件单元测试（默认展开最新、折叠历史、展开/收起交互）
- [x] Task 2.2: TypeScript 编译检查 + 现有测试通过

### Verification

- [ ] `npm test` 全部通过
- [ ] `npx tsc --noEmit` 无错误

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 默认只展示最近一次 draft | Interaction | Navigate to case detail draft tab → assert only latest draft expanded → assert others collapsed |
| 2 | 历史 draft 可折叠展开 | Interaction | Click collapsed draft → assert content appears → click again → assert collapsed |
| 3 | 折叠态显示基本信息 | Visual | Navigate to draft tab with 2+ drafts → verify collapsed items show type and time |
| 4 | 交互流畅无闪烁 | Visual | Expand/collapse multiple times → screenshot verify smooth transition |

**Test Type Legend:**
- **E2E** — Backup data -> run actual workflow/script -> verify file outputs + API + UI -> restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Phase 3: DraftsPage 全局页面改造

将 DraftsPage 从按 case 分组改为按时间排序的扁平列表，每个 case 只显示最新 draft。

### Tasks

- [x] Task 3.1: 改造 DraftsPage — 去掉 case 分组，过滤为每个 case 仅保留最新 draft，按绝对时间排序，DraftCard 增加 case number 显示
- [x] Task 3.2: TypeScript 编译检查 + 功能验证

### Verification

- [ ] DraftsPage 不再按 case 分组，显示扁平列表
- [ ] 每个 case 只显示最新 draft
- [ ] 排序按 draft 生成时间 DESC（最新在最上面）
- [ ] 每张卡片显示 case number 并可点击跳转

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
