# Implementation Plan: onenote-export Sub-Page Recursive Export

**Track ID:** onenote-subpage_20260401
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-01
**Status:** [ ] Not Started

## Overview

Single-phase fix: after the `$PageName` regex filter on line 611 of `export-onenote.ps1`, add a forward scan through `$allPagesUnfiltered` to include consecutive sub-pages (higher `pageLevel`) of each matched page.

## Phase 1: Fix Sub-Page Inclusion

### Tasks

- [ ] Task 1.1: In `export-onenote.ps1`, after line 611 (`$allPages = @($allPages | Where-Object { $_.Name -match $PageName })`), add a forward scan: for each matched page, find its index in `$allPagesUnfiltered`, then scan forward collecting all consecutive pages in the same section with `Level > matched.Level`, stopping at `Level <= matched.Level`. Merge these sub-pages into `$allPages`. Add a log line: `"Including N sub-pages of {PageName}"`
- [ ] Task 1.2: Verify existing sub-page directory nesting logic (lines ~655-679) still works correctly with the expanded `$allPages` — no changes needed there, just verify

### Verification

- [ ] Run onenote-export with a OneNote link pointing to a page with known sub-pages → verify sub-pages are exported
- [ ] Run onenote-export with a link to a page with no sub-pages → verify only that page exported
- [ ] Run onenote-export with no link (full section) → verify behavior unchanged

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Link with sub-pages includes all sub-pages | E2E | Run onenote-export with a known link → verify output directory contains parent + sub-pages |
| 2 | Sub-page nesting preserved in directory structure | E2E | Check output directory structure: sub-pages in subdirectories |
| 3 | Link with no sub-pages unchanged | E2E | Run onenote-export with a leaf page link → verify only 1 page exported |
| 4 | Full notebook/section export unchanged | E2E | Run onenote-export without link filter → verify page count matches pre-fix |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor from ISS-194._
