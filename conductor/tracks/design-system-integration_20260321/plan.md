# Implementation Plan: Dashboard Design System Integration — Dual Theme

**Track ID:** design-system-integration_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

分 4 个阶段实施：基础设施搭建 → 布局改造 → 通用组件主题化 → 页面级主题化。每个阶段独立可验证，确保渐进式生效。

## Phase 1: 基础设施 — CSS 变量 + Tailwind + 主题切换

搭建主题系统的底层基础，完成后默认暗色应该已经可见（即使组件还在用旧样式）。

### Tasks

- [x] Task 1.1: 修改 `tailwind.config.js` — 添加 `darkMode: 'class'`，扩展 theme 增加 CSS 变量引用的自定义颜色
- [x] Task 1.2: 修改 `index.css` — 添加 `:root` / `.dark` / `.light` CSS 变量定义（从 design-preview.html 移植），添加字体引用
- [x] Task 1.3: 修改 `index.html` — 添加 `class="dark"` 到 `<html>`，添加 Google Fonts 预加载（Plus Jakarta Sans + JetBrains Mono）
- [x] Task 1.4: 创建 `src/hooks/useTheme.ts` — ThemeProvider hook（dark/light/system 三模式，localStorage 持久化，监听 prefers-color-scheme）

### Verification

- [x] `<html>` 有 `class="dark"`，页面背景变为 `--bg-base` 深色
- [x] 手动切换到 `.light` 后颜色正确变化
- [x] TypeScript 编译通过

## Phase 2: 布局改造 — 侧边栏导航

将 Layout.tsx 从顶部导航改为左侧固定侧边栏，加入主题切换按钮。

### Tasks

- [x] Task 2.1: 重写 `Layout.tsx` — 左侧 220px 固定侧边栏 + 右侧全宽主内容区，侧边栏包含 logo、nav items、底部主题切换 + logout
- [x] Task 2.2: 修改 `LoginPage.tsx` — 适配暗色/亮色主题

### Verification

- [x] 侧边栏正确渲染，导航功能正常
- [x] 主题切换按钮可用，切换后布局颜色正确

## Phase 3: 通用组件主题化

改造所有 common/ 组件和业务组件，替换硬编码颜色。

### Tasks

- [x] Task 3.1: 改造 `common/Card.tsx` — 使用 CSS 变量替换 `bg-white`、`border-gray-200` 等
- [x] Task 3.2: 改造 `common/Tabs.tsx` — 主题化 tab 激活/非激活样式
- [x] Task 3.3: 改造 `common/Loading.tsx` — 主题化加载动画颜色
- [x] Task 3.4: 改造 `common/Badge.tsx` — 使用 accent-* 颜色变量
- [x] Task 3.5: 改造 `common/MarkdownContent.tsx` — prose 暗色适配
- [x] Task 3.6: 改造 `common/ErrorBoundary.tsx` — 主题化错误展示
- [x] Task 3.7: 改造 `SessionBadge.tsx` — 主题化状态指示
- [x] Task 3.8: 改造 `CaseSessionPanel.tsx` — 主题化 session 面板
- [x] Task 3.9: 改造 `CaseAIPanel.tsx` — AI 面板使用 `--bg-inset` 凹陷背景
- [x] Task 3.10: 改造 `TrackProgressPanel.tsx` — 主题化进度面板

### Verification

- [x] 所有组件在暗色/亮色下正确渲染
- [x] TypeScript 编译通过

## Phase 4: 页面级主题化

改造所有页面文件，确保端到端主题一致性。

### Tasks

- [x] Task 4.1: 改造 `Dashboard.tsx` — 统计卡片、表格、搜索栏主题化
- [x] Task 4.2: 改造 `CaseDetail.tsx` — 头部卡片、tab 内容、整体布局主题化
- [x] Task 4.3: 改造 `TodoView.tsx` — Todo 列表主题化
- [x] Task 4.4: 改造 `DraftsPage.tsx` — 草稿列表主题化
- [x] Task 4.5: 改造 `AgentMonitor.tsx` — Agent 监控面板主题化
- [x] Task 4.6: 改造 `SettingsPage.tsx` — 设置表单主题化
- [x] Task 4.7: 改造 `Issues.tsx` — Issues 列表分组主题化
- [x] Task 4.8: 更新 `index.css` 中 prose 表格样式 — 使用 CSS 变量替换硬编码灰色

### Verification

- [x] 每个页面在暗色/亮色下正确渲染
- [x] TypeScript 编译通过

## Phase 5: Post-Implementation Checklist

- [x] TypeScript 编译通过（`cd dashboard && npx tsc --noEmit` + `cd dashboard/web && npx tsc --noEmit`）
- [x] 单元测试通过（`cd dashboard && npm test`）
- [ ] browser-test 覆盖主题切换（主要页面截图）
- [x] 关联 Issue ISS-028 JSON 状态已更新（done）
- [x] Track metadata.json 已更新（complete）
- [x] tracks.md 状态标记已更新（[x]）

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
