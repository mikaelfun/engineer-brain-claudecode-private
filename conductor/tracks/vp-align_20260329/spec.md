# Specification: new-track Verification Plan 与 verify 推理一致性

**Track ID:** vp-align_20260329
**Type:** Bug
**Created:** 2026-03-29
**Status:** Draft
**Issue:** ISS-155

## Summary

修复 new-track 的 Verification Plan 生成仍用规则关键词分类（与 verify SKILL.md 的推理驱动不一致）的问题。

## Context

当前不一致：
- `new-track.md` 用**规则表**分类 AC（关键词→E2E/Interaction/Visual/API/Skip）
- `verify/SKILL.md` 用**推理驱动**分类（LLM 读 spec → 推理 → 查 verification-recipes/_index.md）
- 已有误判实例：`inspection-refactor` 把"casework 快速路径无 LLM"标为 Skip，但用 E2E backup→run→verify 完全可测

## Acceptance Criteria

- [ ] AC1: new-track 的 Verification Plan 生成逻辑改为参考 `verification-recipes/_index.md` 匹配规则
- [ ] AC2: LLM 对每条 AC 推理验证类型（而非关键词查表），与 verify SKILL.md Step 2 的推理方式一致
- [ ] AC3: Skip 类型仅限 D365 写操作（与 new-track.md 已有的 E2E 优先原则一致），需给出 justification
- [ ] AC4: 生成的 Verification Plan 表增加 Recipe 列（匹配到的 verification recipe 或 "from-scratch"）

## Dependencies

- verify-redesign_20260329 (ISS-148) — 已完成

## Out of Scope

- verify SKILL.md 本身的修改（已是推理驱动）
- 现有已完成 tracks 的 Verification Plan 修正

## Technical Notes

- 修改位置：全局 `conductor:new-track` command 文件的 Verification Plan 生成部分
- 不需要创建新的 recipe 文件——直接复用 verification-recipes/_index.md

---

_Generated by Conductor._
