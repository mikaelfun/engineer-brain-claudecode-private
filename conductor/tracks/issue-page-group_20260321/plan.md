# Implementation Plan: Fix Issues Page Grouping with Pagination

**Track ID:** issue-page-group_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

将 Issues 页面从"服务端分页 + 客户端分组"改为"全量获取 + 客户端分组 + 客户端分页"，确保分组在分页之前执行。

## Phase 1: Backend — 支持全量获取

修改后端 API，当不传分页参数时返回全量 issues。

### Tasks

- [x] Task 1.1: 修改 `dashboard/src/routes/issues.ts` — 当 `page` 和 `pageSize` 未传时，返回全量 issues（不 slice），response 格式保持兼容（`totalPages: 1`）
- [x] Task 1.2: 确认 `listIssues()` 无需修改（已返回全量）

### Verification

- [x] `GET /api/issues`（无分页参数）返回所有 issues
- [x] `GET /api/issues?page=1&pageSize=15` 仍正常工作（向后兼容）

## Phase 2: Frontend — 全量获取 + 客户端分页

修改前端：不传分页参数给 API，获取全量 issues 后在客户端先分组再分页。

### Tasks

- [x] Task 2.1: 修改 `dashboard/web/src/api/hooks.ts` — `useIssues` 不传 `page`/`pageSize` 参数（或新增 `useAllIssues` hook）
- [x] Task 2.2: 修改 `dashboard/web/src/pages/Issues.tsx` — 移除服务端分页依赖，改为：全量 issues → search filter → status/type filter → 按 status 分组 → 客户端分页（对分组后的整体列表分页）
- [x] Task 2.3: 保留分页 UI 控件，但改为基于客户端过滤后的数据计算 totalPages

### Verification

- [x] 第一页和第二页的 issues 都正确分组到 pending/tracked/done
- [x] 搜索、status filter、type filter 正常工作
- [x] 分页控件正确显示并可操作

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Issues 页面所有 issues 正确按 pending/tracked/done 分组显示 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 分组在分页之前执行（即分组看到的是全量数据，不受分页影响） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 当 issues 数量多时，仍有分页机制避免页面过长（客户端分页） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 搜索、status filter、type filter 仍然正常工作 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | 页面加载性能不受明显影响（issues 通常 < 100 个） | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Phase 3: Post-Implementation Checklist

- [x] TypeScript 编译通过 (`npx tsc --noEmit`)
- [x] 单元测试已创建并通过（73/73 pass）
- [x] browser-test 跳过（Bug fix < 3 tasks，按 workflow.md 规则）
- [x] 关联 Issue ISS-026 JSON 状态已更新（done）
- [x] Track metadata.json 已更新（complete）
- [x] tracks.md 状态标记已更新（[x]）

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
