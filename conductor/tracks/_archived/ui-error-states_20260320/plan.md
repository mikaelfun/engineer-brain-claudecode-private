# Implementation Plan: UI Error States Fix

**Track ID:** ui-error-states_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [ ] Not Started

## Overview

Two isolated bug fixes targeting error handling gaps. Phase 1 fixes the backend patrol-state 404, Phase 2 fixes the frontend CaseDetail infinite loading. Both are small, focused changes.

## Phase 1: Backend — Patrol State 404 Fix

Fix the `/api/agents/patrol-state` route to return empty state instead of 404 when the patrol state file doesn't exist.

### Tasks

- [ ] Task 1.1: **修复 patrol-state 路由** — 在 `dashboard/src/routes/cases.ts` 中找到 patrol-state GET 路由，当 `casehealth-state.json` 不存在时返回 `200 {}` 而非 404。
- [ ] Task 1.2: **验证** — 确认 API 在无文件时返回 200，有文件时行为不变。

## Phase 2: Frontend — CaseDetail Error State

Add error handling to the CaseDetail page so invalid case IDs show an error message instead of infinite loading.

### Tasks

- [ ] Task 2.1: **添加 error 状态处理** — 在 `CaseDetail.tsx` 中检查 `useCase` hook 的 error 状态，当 API 返回错误时显示 "Case not found" 页面，包含返回 Dashboard 的按钮。
- [ ] Task 2.2: **验证** — 确认 `/case/NONEXISTENT` 显示错误页面，有效 case 正常加载。

## Final Verification

- [ ] All acceptance criteria met
- [ ] No 404 console errors on fresh Dashboard load
- [ ] Invalid case IDs show error state with back button
- [ ] Existing pages unaffected
- [ ] TypeScript compiles clean

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Navigating to `/case/<invalid-id>` shows an error state ("Case not found") in... | Visual | Navigate to page → verify element is visible with correct content |
| 2 | Error state includes a link/button to return to Dashboard | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 3 | `/api/agents/patrol-state` returns 200 with empty state when no patrol state ... | API | Call the relevant endpoint → verify response shape and status code |
| 4 | No 404 console errors on fresh install Dashboard load | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | Existing functionality (valid cases, completed patrols) is not affected | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
