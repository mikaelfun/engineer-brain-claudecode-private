# Implementation Plan: Issue 页面添加重启前后端按钮

**Track ID:** restart-buttons_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

后端新增 restart API（杀进程 + 重启），前端在 Issues 页面添加三个按钮调用 API，断连后自动轮询重连。

## Phase 1: 后端 Restart Service + API

### Tasks

- [x] Task 1.1: 创建 `dashboard/src/services/restart-service.ts` — 实现 restartFrontend()、restartBackend()、restartAll()，通过 shell 命令杀端口进程并 spawn 新进程
- [x] Task 1.2: 创建 `dashboard/src/routes/restart.ts` — POST /api/restart/frontend、/api/restart/backend、/api/restart/all
- [x] Task 1.3: 注册路由到 `dashboard/src/index.ts`（auth middleware + route mount）

### Verification

- [x] TypeScript 后端编译通过

## Phase 2: 前端按钮 + 重连逻辑

### Tasks

- [x] Task 2.1: 在 `dashboard/web/src/api/hooks.ts` 添加 useRestartFrontend、useRestartBackend、useRestartAll mutation hooks
- [x] Task 2.2: 在 Issues 页面 header 区域添加三个重启按钮（FE / BE / All），带 loading 状态 + disabled 防重复
- [x] Task 2.3: 实现断连检测 + 自动轮询 /api/health 重连逻辑，后端恢复后自动 reload

### Verification

- [x] TypeScript 前端编译通过

## Final Verification

- [x] 三个按钮 UI 就绪（FE=Monitor, BE=Server, All=RefreshCw）
- [x] 后端重启后前端自动轮询 /api/health 重连
- [x] 重连期间显示 overlay（Restarting services...）
- [x] TypeScript 前后端编译通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Issues 页面有三个按钮：重启前端、重启后端、重启全部 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 点击后端重启按钮，后端进程重启，API 恢复可用 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 点击前端重启按钮，前端 vite 进程重启，页面自动重连 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 重启全部 = 先后端再前端 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | 按钮有 loading 状态，防止重复点击 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
