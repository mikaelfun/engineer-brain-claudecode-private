# Implementation Plan: WebUI Cron Job Management

**Track ID:** cron-management_20260401
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-01
**Status:** [~] In Progress

## Overview

Add CRUD capabilities for Remote Triggers to the WebUI Agents page. Backend proxies requests to the RemoteTrigger API; frontend adds a create form dialog, delete buttons, and manual trigger buttons to the existing Cron Jobs section.

## Phase 1: Backend API Endpoints

Add proxy routes in `dashboard/src/routes/agents.ts` that call the RemoteTrigger tool API.

### Tasks

- [x] Task 1.1: Add `GET /api/agents/triggers` — list all remote triggers (replaces/supplements cron-jobs.json)
- [x] Task 1.2: Add `POST /api/agents/triggers` — create a new trigger (name, prompt, cron, description)
- [x] Task 1.3: Add `DELETE /api/agents/triggers/:id` — delete a trigger
- [x] Task 1.4: Add `POST /api/agents/triggers/:id/run` — manually trigger execution
- [x] Task 1.5: Define TypeScript types for trigger request/response in `types/index.ts`

### Verification

- [ ] API endpoints respond correctly (curl test: list, create, delete, run)

## Phase 2: Frontend — Cron Job List Enhancement

Upgrade the read-only Cron Jobs section to show richer data from the triggers API and add action buttons.

### Tasks

- [x] Task 2.1: Add `useTriggers` React Query hook in `api/hooks.ts` (fetch from new `/api/agents/triggers`)
- [x] Task 2.2: Update Cron Jobs section in `AgentMonitor.tsx` to use triggers data
- [x] Task 2.3: Add Delete button to each cron job card with confirmation dialog
- [x] Task 2.4: Add "Run Now" button to each cron job card
- [x] Task 2.5: Invalidate/refetch triggers query after create/delete/run mutations

### Verification

- [ ] List shows triggers with status info; delete and run-now buttons work

## Phase 3: Frontend — Create Trigger Dialog

Add a dialog/modal form for creating new triggers with cron expression presets.

### Tasks

- [x] Task 3.1: Create `CreateTriggerDialog` component with form fields (name, prompt, cron expression, description)
- [x] Task 3.2: Add cron expression presets dropdown (every 1h, 3h, 6h, daily 9am, weekdays 9am)
- [x] Task 3.3: Add "New Cron Job" button to Cron Jobs section header
- [x] Task 3.4: Wire form submission to `POST /api/agents/triggers` with optimistic update
- [x] Task 3.5: Add input validation (name required, cron format validation, prompt required)

### Verification

- [ ] Create dialog opens, presets populate cron field, form submits and new job appears in list

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Create cron job via form | API + Interaction | POST /api/agents/triggers with test payload -> assert 201 + job in list -> Playwright: open dialog, fill form, submit, verify card appears |
| 2 | Delete cron job with confirmation | API + Interaction | Create test trigger -> DELETE /api/agents/triggers/:id -> assert removed from list -> Playwright: click delete, confirm, verify card removed |
| 3 | Status display (last run, errors) | API | GET /api/agents/triggers -> assert response includes state fields (lastRunAtMs, lastStatus, consecutiveErrors) |
| 4 | Immediate list update after create/delete | Interaction | Create via dialog -> verify appears without page reload -> delete -> verify removed without reload |
| 5 | Manual trigger execution | API | POST /api/agents/triggers/:id/run -> assert 200 response |

**Test Type Legend:**
- **E2E** — Backup data -> run actual workflow/script -> verify file outputs + API + UI -> restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] API endpoints tested via curl
- [ ] Frontend create/delete/run flows work end-to-end
- [ ] ISS-196.json status updated to `implemented`
- [ ] Track metadata.json updated
- [ ] tracks.md status updated

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
