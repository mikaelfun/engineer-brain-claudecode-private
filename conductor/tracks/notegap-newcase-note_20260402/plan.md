# Implementation Plan: NoteGap 新 Case 日终 Note 生成

**Track ID:** notegap-newcase-note_20260402
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-02
**Status:** [ ] Not Started

## Overview

在 note-gap SKILL.md 中增加"新 Case 检测"分支，使当天新建且无人工 note 的 Case 也能生成日终 note 草稿。改动集中在 Step 3（解析）和 Step 4（判断）两步。

## Phase 1: 修改 SKILL.md 检测逻辑

### Tasks

- [ ] Task 1.1: 在 Step 3 末尾增加"无人工 note"检测逻辑，标记 `isNewCase`
  - 判断条件：notes.md 不存在，或所有 note 都含"系统自动分配"（即过滤后为空）
  - 输出变量：`isNewCase = true/false`、`caseCreatedDate`（从 case-info.md 或 casehealth-meta.json 获取）

- [ ] Task 1.2: 修改 Step 4 判断逻辑，增加 `isNewCase` 分支
  - 原逻辑保持不变（`isNewCase = false` 时走 gapDays 阈值）
  - 新增：`isNewCase = true` 时跳过阈值检查，直接进入 Step 5-7 生成流程
  - 输出提示改为：`"📋 新 Case 检测 — 无历史 Note，生成日终操作记录"`

- [ ] Task 1.3: 增强 Step 6 读取逻辑，为新 Case 补充数据源
  - 除 case-summary.md 外，额外读取 `casehealth-meta.json`：
    - `irSla.status` → "Met IR" / "IR pending"
    - `emails` 数量 → 是否发了邮件
    - `actualStatus` → 当前状态
  - 构建新 Case 的 note 内容模板：
    ```
    YYMMDD--
    - -met IR SLA, initial response sent.
    - -sent first quality response with {topic}.
    - -{其他从 case-summary 提取的排查动作}
    ```
  - 如果 case-summary 无内容（极新 case），至少生成：
    ```
    YYMMDD--
    - -new case received, initial response sent.
    ```

### Verification

- [ ] 读改动后的 SKILL.md，确认逻辑完整性
- [ ] 检查与原有 gap 逻辑无冲突（有人工 note 的 case 行为不变）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 新 Case 无人工 note 触发生成 | E2E | 构造测试 case 目录（notes.md 仅含系统 note）→ 执行 /note-gap → 验证 note-draft.md 生成 |
| 2 | 草稿内容包含 IR/邮件/排查信息 | E2E | 构造 casehealth-meta.json（含 IR status）+ case-summary.md → 执行 /note-gap → 验证草稿内容 |
| 3 | 格式遵循用户偏好 | E2E | 验证 note-draft.md 的 title="fangkun note"、body 含 YYMMDD-- 格式 |
| 4 | 已有人工 note 的 Case 行为不变 | E2E | 构造有人工 note 且 gap < 阈值的 case → 执行 /note-gap → 验证输出 "Note 正常" |

**Test Type Legend:**
- **E2E** — Backup data -> run actual workflow/script -> verify file outputs + API + UI -> restore

## Post-Implementation Checklist

- [ ] SKILL.md 修改完成且逻辑自洽
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
