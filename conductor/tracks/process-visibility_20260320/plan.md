# Implementation Plan: Improve Process Execution Visibility

**Track ID:** process-visibility_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [ ] Not Started

## Overview

从 Backend → Frontend 逐层修复信息丢失，让 SSE 管道传递有意义的操作细节到 UI。

## Phase 1: Backend — 改进 SSE 消息提取

修复信息从 Claude SDK 到 SSE 事件的映射丢失。

### Tasks

- [ ] Task 1.1: 改进 `sse-helpers.ts` 的 `getSSEEventType()` — 完善 SDK 消息类型映射，减少默认降级为 thinking
- [ ] Task 1.2: 改进 `sse-helpers.ts` 的 `formatMessageForSSE()` — 提取 tool arguments 摘要、处理数组/对象 content
- [ ] Task 1.3: 修复 `case-routes.ts` chat route — 使用 `getSSEEventType()` 而非硬编码 thinking
- [ ] Task 1.4: 修复持久化消息类型 — `appendSessionMessage` 使用前端兼容的类型名（`thinking`/`tool-call`/`tool-result` 而非 `assistant`/`tool_use`/`tool_result`）

### Verification

- [ ] 启动 full process，检查 `/api/events` SSE 流中 tool-call 事件包含 toolName 和参数摘要

## Phase 2: Frontend — 修复消息接收和恢复

修复前端接收和恢复消息时的信息丢失。

### Tasks

- [ ] Task 2.1: 修复 `useSSE.ts` / `caseSessionStore.ts` — 非字符串 content 序列化为字符串而非丢弃
- [ ] Task 2.2: 修复持久化消息恢复时的类型映射 — `assistant` → `thinking`、`tool_use` → `tool-call` 等

### Verification

- [ ] 页面刷新后，恢复的消息类型和内容正确显示

## Phase 3: Frontend — 改进 MessageBubble 渲染

让 UI 展示更有意义的操作信息。

### Tasks

- [ ] Task 3.1: Tool-call 气泡显示工具名 + 参数摘要（如 `Bash: pwsh scripts/get-case-info.ps1 -CaseNumber 123`）
- [ ] Task 3.2: Step-started 系统消息增加视觉分隔样式
- [ ] Task 3.3: 确保 content 非空时总是显示（移除不必要的截断或隐藏逻辑）

### Verification

- [ ] Full process 执行时能看到具体操作步骤和工具调用详情

## Final Verification

- [ ] All acceptance criteria met
- [ ] Full process: 可看到 step 切换、tool 调用细节、tool 结果摘要
- [ ] Chat: 可区分 thinking / tool-call / tool-result
- [ ] 页面刷新后消息正确恢复
- [ ] 无 console 报错

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Tool-call 消息显示工具名称和关键参数摘要（如 `Bash: pwsh scripts/get-case-info.ps1`） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Tool-result 消息显示结果摘要（截取前 300 字符，非空） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Step 切换有明显的视觉分隔（如 "📋 data-refresh started"） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | 非字符串 content（数组/对象）被正确序列化而非丢弃 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | Chat route 也能区分 thinking/tool-call/tool-result | Interaction | Navigate to page → type in input → submit → verify response appears |
| 6 | 页面刷新后恢复的持久化消息类型正确渲染 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
