# Implementation Plan: Patrol Case SSE Shows Full Casework Process

**Track ID:** patrol-case-sse_20260331
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-31
**Status:** [x] Complete

## Overview

Extract `broadcastSDKMessages` from `case-routes.ts` into a shared utility, then replace the patrol loop's manual `getSSEEventType`+`formatMessageForSSE` approach with the shared broadcaster. This gives patrol cases the same deep-parsed, content-rich SSE messages as single-case processing.

## Phase 1: Extract broadcastSDKMessages to shared module

Extract the `broadcastSDKMessages` function from `case-routes.ts` into `dashboard/src/utils/sdk-message-broadcaster.ts` so it can be reused by both case-routes and patrol.

### Tasks

- [x] Task 1.1: Create `dashboard/src/utils/sdk-message-broadcaster.ts` — move `broadcastSDKMessages`, `summarizeToolInput`, and `ToolCallRecord` type from `case-routes.ts`
- [x] Task 1.2: Update `case-routes.ts` to import from the new shared module instead of using local definitions
- [x] Task 1.3: Verify single-case processing still works (TypeScript compile + existing tests pass)

### Verification

- [x] `npx tsc --noEmit` passes in `dashboard/`
- [x] `npm test` passes (no regression)

## Phase 2: Replace patrol message loop with shared broadcaster

Replace the patrol loop's manual message broadcasting (lines 1214-1237 in `_runPatrol`) with the shared `broadcastSDKMessages` function.

### Tasks

- [x] Task 2.1: Update `_runPatrol` in `case-session-manager.ts` to use `broadcastSDKMessages` instead of the manual `getSSEEventType`+`formatMessageForSSE` loop. Pass `step: 'patrol'` as the step name.
- [x] Task 2.2: Ensure patrol still captures `sdkSessionId` from the broadcast result (needed for session tracking and abort)
- [x] Task 2.3: Verify the patrol timeout/abort logic still works correctly with the refactored message consumption

### Verification

- [x] `npx tsc --noEmit` passes
- [x] `npm test` passes (no regression)

## Phase 3: Frontend event compatibility

Ensure the frontend correctly renders patrol case messages with the new `case-step-progress` event type.

### Tasks

- [x] Task 3.1: Verify `useSSE.ts` already handles `case-step-progress` events for patrol cases (it should, since it's the same event type as single-case). Confirm `caseNumber` and `sessionId` from patrol events are correctly routed to `caseSessionStore`.
- [x] Task 3.2: If needed, ensure `case-step-started` and `case-step-completed` events are also emitted for each patrol case (so step-grouped display works in Agent Monitor)

### Verification

- [x] `npx tsc --noEmit` passes in `dashboard/web/`
- [x] `npm test` passes

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Patrol SSE messages contain actual thinking/tool content | API | Start patrol via `POST /api/patrol` → listen to SSE stream → verify `case-step-progress` events have non-empty `content` and `toolName` fields for each case |
| 2 | Patrol uses `case-step-progress` event type | API | Start patrol → verify SSE events for case messages use `case-step-progress` type, not `case-session-thinking` |
| 3 | Agent Monitor shows same detail for patrol cases | Interaction | Start patrol → open Agent Monitor → expand a patrol case session → verify messages show thinking text and tool call summaries |
| 4 | Patrol step name preserved | API | Start patrol → verify SSE `case-step-progress` events include `step: 'patrol'` field |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
