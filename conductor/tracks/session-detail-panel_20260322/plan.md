# Implementation Plan: Agent Monitor Session Detail Panel

**Track ID:** session-detail-panel_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Extract the message display components from CaseAIPanel into shared components, then enhance each session row in AgentMonitor with an expandable detail panel showing live SSE messages, chat input (case sessions), and terminate button. Restructure the session list to separate completed sessions into a default-collapsed group.

## Phase 1: Extract Shared Message Components

Extract `ProcessedMessageList`, `MessageBubble`, `CollapsedGroup`, and `processMessages()` from `CaseAIPanel.tsx` into shared components that both CaseAIPanel and AgentMonitor can use.

### Tasks

- [x] Task 1.1: Create `components/session/SessionMessageList.tsx` — extract `ProcessedMessageList`, `MessageBubble`, `CollapsedGroup`, and `processMessages()` from `CaseAIPanel.tsx` into standalone shared components
- [x] Task 1.2: Update `CaseAIPanel.tsx` to import from the new shared location instead of inline definitions
- [x] Task 1.3: Verify CaseAIPanel still works correctly after refactor (TypeScript compiles, existing tests pass)

### Verification

- [x] TypeScript compiles without errors
- [x] Existing CaseAIPanel tests still pass

## Phase 2: AgentMonitor — Expandable Session Detail

Add expand/collapse per session row, showing the SSE message stream for case sessions.

### Tasks

- [x] Task 2.1: Add `expandedSessionId` state to AgentMonitor — clicking a session row toggles its detail panel open/closed
- [x] Task 2.2: Create `SessionDetailPanel` component within AgentMonitor — when expanded, renders `SessionMessageList` for case sessions, shows simplified status text for non-case sessions
- [x] Task 2.3: Wire case session messages — use `useCaseSessionStore` messages keyed by `caseNumber` (from session.context for case type); add message recovery from `GET /case/:id/messages` on expand
- [x] Task 2.4: Add visual expand indicator to `SessionRow` — chevron icon that rotates on expand, subtle highlight on expanded row

### Verification

- [x] TypeScript compiles without errors
- [x] Clicking a session row expands/collapses detail panel
- [x] Case session detail shows live SSE messages

## Phase 3: Session Interaction — Chat & Terminate

Add chat input for case sessions and terminate button for active sessions.

### Tasks

- [x] Task 3.1: Add chat input box inside `SessionDetailPanel` for case sessions — text input + send button, only shown when session is active/paused; sends `POST /case/:id/chat` with session ID
- [x] Task 3.2: Add "Stop" button for active sessions — shown next to session row for active/paused case sessions; calls `DELETE /case/:id/session`; updates local state on success
- [x] Task 3.3: Add optimistic user message on chat send — immediately append `type: 'user'` message to local store, matching CaseAIPanel pattern

### Verification

- [x] TypeScript compiles without errors
- [x] Chat input sends message and SSE response appears in panel
- [x] Stop button terminates session

## Phase 4: Completed Sessions — Default Collapsed Group

Restructure session grouping so completed sessions are separated and collapsed by default.

### Tasks

- [x] Task 4.1: Split sessions into active/recent group and completed group — active/paused/failed sessions stay in current type-grouped view; completed sessions move to a separate "Completed Sessions" card at the bottom
- [x] Task 4.2: Make completed sessions card collapsed by default — only show count badge and toggle chevron; expanding reveals the completed sessions list grouped by type

### Verification

- [x] TypeScript compiles without errors
- [x] Completed sessions appear in collapsed section at bottom
- [x] Active sessions remain in main view

## Phase 5: Post-Implementation Checklist

- [x] Unit test for SessionMessageList shared component
- [x] Unit test for SessionDetailPanel expand/collapse
- [x] ISS-048 JSON status updated
- [x] Track metadata.json updated
- [x] tracks.md status mark updated

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
