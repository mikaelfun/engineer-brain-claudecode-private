# Implementation Plan: Test Loop 架构优化 — 批量处理 + subagent 隔离

**Track ID:** testloop-batch_20260328
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-28
**Status:** [x] Completed

## Overview

三阶段重构：先改 SKILL.md 核心循环逻辑，再给 executor 加 progress 日志，最后增强 health-check.sh 读取 progress。

## Phase 1: SKILL.md 批量处理重构

重写 TEST/FIX/VERIFY 阶段，从单 item 处理改为批量遍历 + subagent 隔离。

### Tasks

- [x] Task 1.1: 修改 SKILL.md 第 367 行注意事项，从「每轮只执行一个阶段的一个动作」改为「遍历当前 phase 的所有 queue items，每个 item spawn 对应 agent」
- [x] Task 1.2: 重写 TEST 阶段（Step 1）— 添加 `for each test in testQueue` 循环，每个 test spawn agent，agent 返回后立即更新 state.json
- [x] Task 1.3: 重写 FIX 阶段（Step 1）— 添加 `for each fix in fixQueue` 循环，每个 fix 先运行 fix-analyzer.sh 再 spawn opus agent，完成后立即更新 state.json
- [x] Task 1.4: 重写 VERIFY 阶段（Step 1）— 添加 `for each verify in verifyQueue` 循环 + regressionQueue 处理，每个完成后立即更新 state.json
- [x] Task 1.5: 更新 Step 0 说明，明确 env/learnings/safety 只读一次，agent prompt 引用文件路径

### Verification

- [x] SKILL.md 语法和逻辑一致性检查

## Phase 2: Executor Progress 日志

为每个 executor 添加 `write_progress()` 函数，在关键步骤写入 `.progress-{testId}.json`。

### Tasks

- [x] Task 2.1: 在 common.sh 中添加 `write_progress()` 和 `clear_progress()` 公用函数
- [x] Task 2.2: 在 e2e-runner.sh 关键步骤（启动、等待、验证）调用 write_progress
- [x] Task 2.3: 在 api-runner.sh 关键步骤调用 write_progress
- [x] Task 2.4: 在 verify-rerun.sh 关键步骤调用 write_progress
- [x] Task 2.5: 确保 executor 完成（正常/异常）后清理 progress 文件

### Verification

- [x] 手动运行 e2e-runner.sh，确认 .progress 文件生成和清理

## Phase 3: Health-check 增强

health-check.sh 增加读取 `.progress-*.json` 展示当前正在执行的测试。

### Tasks

- [x] Task 3.1: health-check.sh Node.js 部分增加扫描 `results/.progress-*.json` 的逻辑
- [x] Task 3.2: 输出 JSON 增加 `inProgress` 字段（当前正在执行的测试列表 + 步骤 + 耗时）
- [x] Task 3.3: 如果有 progress 文件但 elapsed_s > 阈值，加入 warnings

### Verification

- [x] 手动创建 .progress 文件，运行 health-check.sh 确认输出包含 inProgress 字段

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | SKILL.md TEST 阶段批量遍历 | Skip | SKILL.md 是 prompt 文档，验证为人工 review |
| 2 | 每个 agent 完成后立即更新 state.json | Skip | 需运行实际 test-loop session 验证 |
| 3 | Executor 写 .progress 文件 | E2E | 创建 mock test definition → 运行 e2e-runner.sh → 验证 .progress 文件生成 → 验证完成后清理 |
| 4 | health-check.sh 读取 .progress | E2E | 手动创建 .progress-mock.json → 运行 health-check.sh → 验证 JSON 输出包含 inProgress |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Skip** — SKILL.md 是 prompt 指令文档，需实际 session 运行验证

## Post-Implementation Checklist

- [x] 关联 Issue JSON 状态已更新为 `implemented`
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
