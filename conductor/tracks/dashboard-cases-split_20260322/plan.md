# Implementation Plan: Cases 列表独立页面 + Dashboard 统计可视化

**Track ID:** dashboard-cases-split_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [ ] Not Started

## Overview

从 Dashboard.tsx 提取 Case 列表到独立的 CasesPage.tsx，Dashboard 改为统计概览。新增 `/cases` 路由和侧栏导航项。统计卡片用纯 CSS 实现（数字 + 进度条 + grid），不引入图表库。

## Phase 1: Case 列表页面提取

从 Dashboard.tsx 提取 Case 列表相关逻辑到新的 CasesPage.tsx。

### Tasks

- [x] Task 1.1: 创建 `dashboard/web/src/pages/CasesPage.tsx`，从 Dashboard.tsx 提取 stat filter cards + case list + sort + case card 渲染 + 操作按钮（Refresh/Process）
- [x] Task 1.2: 在 `App.tsx` 添加 `/cases` 路由指向 CasesPage
- [x] Task 1.3: 在 `Layout.tsx` 侧栏 navItems 添加 "Cases" 项（位于 Dashboard 下方，icon: FolderOpen），使用 `/cases` 路径

### Verification

- [ ] `/cases` 页面显示完整 Case 列表（含过滤卡片、排序、Case 卡片）
- [ ] 侧栏 "Cases" 导航项可点击并高亮

## Phase 2: Dashboard 统计可视化改版

将 Dashboard 页面改为统计概览，移除 Case 列表，添加可视化统计卡片。

### Tasks

- [x] Task 2.1: 重写 Dashboard.tsx — 移除 Case 列表和排序逻辑，保留 `useCases()` 用于计算统计数据
- [x] Task 2.2: 添加统计概览卡片 — 第一行：Total Active（大数字）、SLA At Risk（红色高亮数字+进度条）、Needs My Action（黄色）、Awaiting Others（绿色），点击跳转到 `/cases?filter=xxx`
- [x] Task 2.3: 添加 Case 分布卡片 — 按 Severity 分组的水平条形图（纯 CSS div 宽度百分比）、按 actualStatus 分组的数字列表
- [x] Task 2.4: 添加响应指标卡片 — 平均 daysSinceLastContact（数字高亮）、最长未联系 Case（链接到详情）、IR SLA met/not-met 比例条
- [x] Task 2.5: 添加巡检状态卡片 — 最近巡检时间、Start Patrol 按钮（从原 Dashboard 保留）

### Verification

- [ ] Dashboard 页面不再显示 Case 列表
- [ ] 统计卡片正确显示数字和分布
- [ ] 点击统计卡片跳转到 `/cases?filter=xxx`

## Phase 3: 联调优化

确保两个页面联动正常，处理边界情况。

### Tasks

- [x] Task 3.1: CasesPage 支持 URL query 参数 `?filter=sla-at-risk` 等，Dashboard 卡片点击传参跳转
- [x] Task 3.2: 空数据状态处理 — 无 Case 时 Dashboard 显示空态卡片，CasesPage 显示空态提示
- [x] Task 3.3: TypeScript 编译检查 + build 验证

### Verification

- [ ] Dashboard 卡片点击跳转到 Cases 页面并自动过滤
- [ ] 空数据时两个页面都正常显示
- [ ] Build 通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 侧栏新增 Cases 导航项 | Visual | Navigate to Dashboard → screenshot → verify sidebar has "Cases" nav item |
| 2 | Case 列表页面包含全部 Case 功能 | Visual | Navigate to /cases → screenshot → verify filter cards + case list + sort |
| 3 | Dashboard 移除 Case 列表，改为统计卡片 | Visual | Navigate to / → screenshot → verify no case list, only stat cards |
| 4 | Dashboard 统计卡片内容 | Visual | Navigate to / → screenshot → verify severity distribution, SLA summary, response metrics |
| 5 | 路由正确 | Interaction | Click sidebar "Cases" → verify URL is /cases → click "Dashboard" → verify URL is / |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
