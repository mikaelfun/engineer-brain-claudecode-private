# Implementation Plan: CaseDetail AI Panel — More Single-Step Operations

**Track ID:** ai-panel-ops_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

Expand CaseAIPanel from 3 buttons to 9 (Full Process + 8 step routes), organized in collapsible groups. Pure frontend change — no backend modifications needed.

## Phase 1: Expand Action Types and Button Data

Modify CaseAIPanel.tsx to define all 9 actions with icons, colors, and descriptions.

### Tasks

- [x] Task 1.1: Expand `AIAction` type union to include all step names: `'process' | 'data-refresh' | 'compliance-check' | 'status-judge' | 'teams-search' | 'troubleshoot' | 'draft-email' | 'inspection' | 'generate-kb'`
- [x] Task 1.2: Define action groups with metadata — group into 3 categories:
  - **Data**: Data Refresh (🔄), Teams Search (💬)
  - **Analysis**: Compliance Check (✅), Status Judge (🔍), Troubleshoot (🔧)
  - **Output**: Draft Email (📧), Inspection (🩺), Generate KB (📚)
- [x] Task 1.3: Add lucide-react icons for new actions: `RefreshCw`, `MessageSquare`, `ShieldCheck`, `GitBranch`, `FileText`, `BookOpen`

### Verification

- [x] TypeScript compiles without errors
- [x] All 9 actions defined with complete metadata

## Phase 2: Redesign Button Layout

Replace the fixed 3-column grid with a grouped layout that scales well.

### Tasks

- [x] Task 2.1: Keep "Full Process" as a full-width primary button at the top (prominent, blue)
- [x] Task 2.2: Add collapsible "Quick Actions" section below with 3 groups rendered as labeled rows
- [x] Task 2.3: Each group label (Data / Analysis / Output) is a subtle header, buttons are compact inline chips
- [x] Task 2.4: Draft Email retains its split-button dropdown for email type selection
- [x] Task 2.5: All buttons call the existing `handleAction()` which routes to `/case/:id/step/:step`

### Verification

- [x] UI renders correctly with all 9 buttons visible
- [x] Groups are visually distinct
- [x] Buttons scale correctly on narrow screens

## Phase 3: Polish and Edge Cases

### Tasks

- [x] Task 3.1: Add tooltips (title attribute) showing each action's description
- [x] Task 3.2: Ensure operation lock disables all buttons correctly (existing `isActionDisabled`)
- [x] Task 3.3: Quick Actions section starts expanded by default, collapsible via chevron
- [x] Task 3.4: TypeScript type-check passes for dashboard/web/

### Verification

- [x] `npx tsc --noEmit` passes in dashboard/web/
- [x] All buttons trigger correct API calls
- [x] Operation lock works across all action types

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | All 8 existing step routes are accessible as individual buttons in CaseAIPanel | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Buttons are visually grouped by category: Data (refresh, teams-search), Analy... | Visual | Navigate to page → verify items are correctly grouped/sorted |
| 3 | Full Process button remains prominently placed as the primary action | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 4 | Each button shows its description on hover (tooltip) | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 5 | Existing operation lock mechanism works correctly (only one action at a time) | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | Real-time SSE message display works for all step types (already supported) | Visual | Navigate to page → verify element is visible with correct content |
| 7 | Draft Email retains its split-button with email type dropdown | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 8 | UI remains clean and not overwhelming despite more buttons | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
