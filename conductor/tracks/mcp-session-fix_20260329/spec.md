# Specification: Dashboard case-session-manager spawn session 未传 mcpServers

**Track ID:** mcp-session-fix_20260329
**Type:** Bug
**Created:** 2026-03-29
**Status:** Draft
**Issue:** ISS-134

## Summary

case-session-manager.js 的 createSession() 只配了 allowedTools 但没传 mcpServers 参数，导致 spawned Claude session 无法访问 Teams/ICM/Mail MCP server。所有依赖 MCP 的 E2E 测试场景均失败。

## Context

Dashboard 通过 Claude Agent SDK 的 session API spawn per-case session。当前 `case-session-manager.ts` 的 `createSession()` 调用中配置了 `allowedTools` 列表但遗漏了 `mcpServers` 参数。这意味着 spawned session 虽然"允许"调用 MCP 工具，但实际没有 MCP server 连接，导致工具调用失败（timeout/auth_issue）。源码位置：`dashboard/src/agent/case-session-manager.ts`。

## User Story

As a developer, I want spawned Claude sessions to have MCP server connections so that case automation can access Teams, ICM, and Mail services.

## Acceptance Criteria

- [ ] AC1: createSession() 传递 mcpServers 配置
- [ ] AC2: spawned session 可以访问 Teams/ICM/Mail MCP
- [ ] AC3: 不影响现有 session 功能
- [ ] AC4: 单元测试覆盖 mcpServers 参数传递

## Dependencies

- 依赖 Claude Agent SDK 的 session API（mcpServers 参数格式）
- 依赖项目 `.mcp.json` 配置文件
- 依赖 dashboard 构建流程（TypeScript → JavaScript）

## Out of Scope

- 不修改 MCP server 本身的配置
- 不修改 allowedTools 列表
- 不添加新的 MCP server

## Technical Notes

- 源码位置：`dashboard/src/agent/case-session-manager.ts`
- 编译产物：`dashboard/dist/agent/case-session-manager.js`
- 需要从 `.mcp.json` 或项目配置读取 MCP server 列表
- Claude Agent SDK mcpServers 参数格式需要确认（可能是 `{ name: string, url: string }[]`）

---

_Generated by Conductor. Review and edit as needed._
