# Implementation Plan: SDK Session 进程清理

**Track ID:** session-cleanup_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

为每个 SDK query() 调用注入 AbortController，维护活跃操作注册表，在 session end / cancel / shutdown 时调用 abort() 清理子进程树。

## Phase 1: AbortController 注册表 + query() 注入

在 case-session-manager.ts 中引入 AbortController 生命周期管理

### Tasks

- [x] Task 1.1: 创建 activeQueries Map<string, AbortController>（key=caseNumber 或 sessionId），添加 registerQuery / abortQuery / abortAllQueries helper
- [x] Task 1.2: processCaseSession — 创建 AbortController 传入 query() options，注册到 activeQueries，query 结束后自动清理
- [x] Task 1.3: chatCaseSession — 同样注入 AbortController
- [x] Task 1.4: executeTodoAction standalone query — 注入 AbortController
- [x] Task 1.5: patrol _runPatrol per-case query — 注入 AbortController，用 caseNumber 作 key 注册

### Verification

- [x] TypeScript 编译通过 `npx tsc --noEmit`
- [ ] 手动验证：启动一个 step → endSession → 确认子进程被终止

## Phase 2: 终止路径接入 abort

让 endSession / cancel / shutdown 实际调用 abort

### Tasks

- [x] Task 2.1: endSession() — 调用 abortQuery(sessionId) 终止底层进程
- [x] Task 2.2: step cancel (routes/steps.ts) — 通过 abortQuery 终止 query，不再依赖消息循环 break
- [x] Task 2.3: patrol cancelPatrol() — abort 当前批次所有 patrol session
- [x] Task 2.4: index.ts — 添加 process.on('SIGINT'/'SIGTERM') handler，调用 abortAllQueries() + stopFileWatcher() + process.exit()
- [x] Task 2.5: restart-service.ts restartBackend()/restartAll() — exit 前调用 abortAllQueries()

### Verification

- [x] TypeScript 编译通过 `npx tsc --noEmit`
- [ ] 手动测试：backend restart 后无孤儿 node 进程

## Phase 3: 测试与收尾

### Tasks

- [x] Task 3.1: 编写单元测试 — 验证 registerQuery/abortQuery/abortAllQueries 逻辑
- [x] Task 3.2: 提交代码

### Verification

- [x] npm test 全部通过 (121/121)

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | query() 调用传入 AbortController | Skip | 单元测试覆盖 |
| 2 | endSession 终止底层进程 | Skip | 后端逻辑，单元测试覆盖 |
| 3 | step cancel abort SDK query | Skip | 后端逻辑，单元测试覆盖 |
| 4 | patrol cancel abort 当前批次 | Skip | 后端逻辑，单元测试覆盖 |
| 5 | SIGINT/SIGTERM handler | Skip | 后端逻辑，手动验证 |
| 6 | restart 先 abort 再退出 | Skip | 后端逻辑，手动验证 |

**Test Type Legend:**
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
