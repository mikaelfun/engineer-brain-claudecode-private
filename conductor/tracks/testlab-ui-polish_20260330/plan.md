# Implementation Plan: TestLab UI 美化 — Design A Mission Control

**Track ID:** testlab-ui-polish_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

纯视觉重构，不改变数据流和组件结构。分 3 个阶段：动画基础 → 核心组件美化 → 主题支持。参考 HTML mockup `dashboard/testlab-v2-design-A.html` 的样式。

## Phase 1: 动画 + 基础样式升级

升级 keyframes、注入全局 CSS 变量覆盖、glassmorphism 基础。

### Tasks

- [x] Task 1.1: 扩展 TESTLAB_KEYFRAMES — 新增 `stage-glow`、`float`、`connector-flow`、`shimmer-border`、`pulse-ring` 动画，保留现有动画
- [x] Task 1.2: 提取 glassmorphism 样式工具函数 — `glassStyle()`、`glassCardStyle()` 返回 CSSProperties 对象，包含 backdrop-filter + 半透明背景 + 微弱边框
- [x] Task 1.3: 升级 TestLabHeader — 激活时顶部 shimmer 动画条（linear-gradient 移动），大号 Cycle 显示（20px font），更醒目的 Start/Stop 按钮样式

### Verification

- [ ] 页面加载无 console 错误
- [ ] Header 在 running 状态下顶部有动画条

## Phase 2: Pipeline Hero + Reasoning 面板重构

核心视觉升级：大号 stage card + 发光效果 + 推理时间线。

### Tasks

- [x] Task 2.1: 重构 StagePipeline — stage card 升级为大号卡片（padding 26px，icon 36px），弹性宽度（flex: 1, min-width 120px, max-width 220px），活跃阶段 scale(1.06) + glow 动画
- [x] Task 2.2: Connector 线条升级 — 三种状态：`lit`（绿色渐变 done）、`flowing`（蓝色/amber 流动动画 active）、`off`（暗灰 pending），弹性宽度
- [x] Task 2.3: 当前测试标签 — 活跃 stage 下方浮动显示当前正在执行的测试名称
- [x] Task 2.4: 重构 ReasoningNarrative — 展开时使用竖向时间线布局（左侧圆点 + 连接线），每步有 icon + label + 结论文本
- [x] Task 2.5: Self-heal 横幅 — 独立的 amber 背景横幅卡片，显示在推理面板下方，含标题、描述、修复动作

### Verification

- [ ] Pipeline 5 个 stage 等宽分布无两侧空白
- [ ] 活跃 stage 有明显发光效果
- [ ] Reasoning 展开/折叠工作正常

## Phase 3: 主题支持 + 收尾

浅色主题 CSS 变量覆盖 + TypeScript 编译验证。

### Tasks

- [x] Task 3.1: 浅色主题 CSS 变量 — 在 useInjectKeyframes 中注入 `[data-theme="light"]` 选择器覆盖所有颜色变量，glassmorphism 调整为白色半透明
- [x] Task 3.2: 主题切换机制 — 在 TestLabHeader 右侧添加 ☀️/🌙 切换按钮，操作 `document.documentElement.dataset.theme`
- [x] Task 3.3: TypeScript 编译验证 — `npx tsc --noEmit` 无错误，`npm test` 通过
- [ ] Task 3.4: 清理 HTML mockup 文件 — 删除 `dashboard/testlab-v2-design-A.html`（用户要求保留，跳过）

### Verification

- [ ] 浅色/深色模式切换流畅
- [ ] tsc 编译无错误
- [ ] 现有测试全部通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Pipeline 弹性全宽布局 | Visual | 启动 Dashboard → 导航到 TestLab → 截图验证 pipeline 无两侧空白 |
| 2 | Stage card 大图标 + 发光动画 | Visual | 确保有测试数据 → 截图验证活跃 stage 有发光边框和 scale-up |
| 3 | Glassmorphism 卡片效果 | Visual | 截图验证卡片有半透明 + 模糊背景效果 |
| 4 | Header shimmer 动画 | Visual | 确保 runner running 状态 → 验证 header 顶部有动画条 |
| 5 | Self-heal amber 横幅 | E2E | 写入 supervisor.json selfHealEvent → API 读取 → 验证 UI 渲染 amber 横幅 |
| 6 | 深色/浅色主题切换 | Interaction | 导航到 TestLab → 点击主题切换按钮 → 截图验证浅色模式 → 再切回深色 |
| 7 | TypeScript 编译通过 | E2E | 运行 tsc --noEmit → 验证 0 errors |
| 8 | 现有测试通过 | E2E | 运行 npm test → 验证 0 failures |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
