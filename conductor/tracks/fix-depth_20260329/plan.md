# Implementation Plan: fix-analyzer.sh 增加根因追溯，减少误判 env issue

**Track ID:** fix-depth_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [x] Complete

## Overview

修改 fix-analyzer.sh，在 timeout/auth_issue/service_down 被标记为 IS_ENV_ISSUE=true 之前，插入 Root Cause Trace 步骤。沿调用链追溯（test result → API endpoint → handler → session config → MCP config），如果发现是配置缺失则降级为 config_missing (code_bug)。

## Phase 1: 新增 failure type "config_missing"

在 fix-analyzer.sh 的分类器中添加新的 failure type。

### Tasks

- [x] Task 1.1: 在 fix-analyzer.sh 的 FAILURE_TYPE 枚举中添加 `config_missing`
- [x] Task 1.2: config_missing 归类为 code_bug（IS_ENV_ISSUE=false），可生成修复建议
- [x] Task 1.3: analysis.md 模板添加 config_missing 对应的输出格式

### Verification

- [x] config_missing 出现在 fix-analyzer.sh 的 failure type 列表中
- [x] analysis.md 正确输出 config_missing 类型

## Phase 2: 添加 Root Cause Trace 逻辑

当 IS_ENV_ISSUE 初始判定为 true 时，执行追溯检查。

### Tasks

- [x] Task 2.1: 从 test result 提取 API endpoint（解析 test output 中的 URL/route）
- [x] Task 2.2: 根据 API endpoint 找到对应的 route handler 文件（在 dashboard/src/ 中搜索）
- [x] Task 2.3: 检查 handler 是否涉及 session spawn（grep createSession/spawnSession 等关键词）
- [x] Task 2.4: 如果涉及 session spawn，检查 mcpServers/allowedTools 等配置是否完整
- [x] Task 2.5: 如果发现配置缺失 → 设 IS_ENV_ISSUE=false, FAILURE_TYPE=config_missing

### Verification

- [x] timeout 因配置缺失 → 正确降级为 config_missing
- [x] 真正的临时环境问题 → 仍为 env_issue

## Phase 3: analysis.md trace 路径输出

在 analysis.md 中记录完整的 trace 路径，便于诊断。

### Tasks

- [x] Task 3.1: analysis.md 添加 "Root Cause Trace" section
- [x] Task 3.2: 记录 trace 路径：`test → endpoint → handler → config → verdict`
- [x] Task 3.3: 如果 trace 无法完成（找不到 handler 等），记录 "trace incomplete" 并保留原始 env_issue 判定

### Verification

- [x] analysis.md 包含 Root Cause Trace section
- [x] trace 路径清晰可读

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: 不再直接标记 env issue | E2E | 构造 timeout test result（因 MCP 配置缺失）→ 运行 fix-analyzer.sh → 验证 IS_ENV_ISSUE=false |
| 2 | AC2: Root Cause Trace 检查配置 | E2E | 构造含 API endpoint 的 test result → 运行 → 验证 trace 检查了 handler 和 session config |
| 3 | AC3: config_missing 正确归类 | E2E | 构造配置缺失场景 → 运行 → 验证 FAILURE_TYPE=config_missing |
| 4 | AC4: 真正的 env issue 仍标记 | E2E | 构造真实环境问题（无对应 handler/config）→ 运行 → 验证 IS_ENV_ISSUE=true |
| 5 | AC5: analysis.md 含 trace | E2E | 运行 → 验证 analysis.md 包含 "Root Cause Trace" section 和路径 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs → restore

## Post-Implementation Checklist

- [x] fix-analyzer.sh 已修改，包含 config_missing type 和 Root Cause Trace
- [x] analysis.md 模板已更新
- [ ] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
