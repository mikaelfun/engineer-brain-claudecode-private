# Implementation Plan: Track 创建流程完善 — 实时进度 + Plan 展示

**Track ID:** track-creation-progress_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

4 阶段实现：后端类型+路由+API → 前端 Store/SSE/Hooks → UI 组件+页面集成 → 测试更新。

## Phase 1: Backend — 类型 + 路由 + API + 状态缓存

### Tasks

- [x] Task 1.1: 修改 `dashboard/src/types/index.ts` — IssueStatus 添加 `'tracking'`
- [x] Task 1.2: 创建 `dashboard/src/services/issue-track-state.ts` — 内存缓存进度消息（IssueTrackStateManager: start/addMessage/finish/getState）
- [x] Task 1.3: 修改 `dashboard/src/routes/issues.ts` create-track 路由 — 初始状态改为 `tracking`；成功后设 `tracked` + 读 plan.md；失败回退 `pending`；广播 tool-call + thinking 两种进度事件
- [x] Task 1.4: 新增 `GET /:id/track-plan` API — 读取 conductor/tracks/{trackId}/plan.md 内容
- [x] Task 1.5: 新增 `GET /:id/track-progress` API — 返回缓存的进度消息数组 + isActive 状态

### Verification

- [x] TypeScript 后端编译通过

## Phase 2: Frontend — Store + SSE + Hooks

### Tasks

- [x] Task 2.1: 创建 `dashboard/web/src/stores/issueTrackStore.ts` — Zustand store（per-issue messages + activeTracking + loadMessages 批量恢复）
- [x] Task 2.2: 修改 `dashboard/web/src/hooks/useSSE.ts` — 添加 issue-track-started/progress/completed/error 4 个 SSE 事件监听
- [x] Task 2.3: 修改 `dashboard/web/src/api/hooks.ts` — 添加 useTrackPlan + useTrackProgress hooks

### Verification

- [x] TypeScript 前端编译通过

## Phase 3: Frontend — TrackProgressPanel + Issues 集成

### Tasks

- [x] Task 3.1: 创建 `dashboard/web/src/components/TrackProgressPanel.tsx` — 消息流展示（started/thinking/tool-call/completed/error）+ PlanSummary 折叠展示 + 页面刷新恢复 + 错误重试
- [x] Task 3.2: 修改 `dashboard/web/src/pages/Issues.tsx` — 添加 tracking 状态到类型/颜色映射；提取 IssueRow 组件订阅 issueTrackStore；tracking 状态按钮禁用；展示 TrackProgressPanel

### Verification

- [x] TypeScript 前端编译通过

## Phase 4: 测试更新

### Tasks

- [x] Task 4.1: 重写 `dashboard/src/routes/issues.test.ts` — 添加 sseManager/issueTrackState/Claude SDK/fs mocks；测试 tracking 状态流转、重复点击防护、track-plan API、track-progress API（39 个测试全部通过）

### Verification

- [x] 全部 51 个单元测试通过（2 个测试文件）
- [x] TypeScript 前后端编译零错误

## Final Verification

- [x] tracking 状态按钮显示 "Creating Track..." spinner
- [x] SSE 进度事件正确广播并在前端展示
- [x] agent 完成后 plan 概要可折叠查看
- [x] 失败回退 pending + 显示错误 + 重试按钮
- [x] 页面刷新恢复进度面板

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 新增 `tracking` 中间状态到 IssueStatus 类型 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | create-track 路由：pending → tracking（执行中）→ tracked（成功）/ pending（失败） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | agent 执行期间广播 tool-call + thinking 两种 SSE 进度事件 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | agent 完成后 SSE 事件附带 plan.md 概要（前 2000 字符） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | 新增 GET /api/issues/:id/track-plan 和 track-progress API | API | Call the relevant endpoint → verify response shape and status code |
| 6 | 内存缓存进度消息，支持页面刷新恢复 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 7 | 前端 Zustand store + SSE 监听 + TrackProgressPanel 组件 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 8 | tracking 状态下按钮禁用（防重复点击） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 9 | TypeScript 前后端编译通过 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 10 | 所有 51 个单元测试通过 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor (retroactive). All tasks were implemented in session and marked complete retroactively._
