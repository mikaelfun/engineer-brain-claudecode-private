# Specification: Case 页面返回按钮导航到 Cases 列表

**Track ID:** case-back-nav_20260404
**Type:** Bug
**Created:** 2026-04-04
**Status:** Draft

## Summary

Case 详情页的返回按钮和错误状态页的重试按钮导航到 `/`（Dashboard），应导航到 `/cases`（Cases 列表页）。

## Problem Description

- **当前行为：** 在 Case 详情页点击左上角返回箭头，页面跳转到 Dashboard 首页（`/`）
- **期望行为：** 应跳转到 Cases 列表页（`/cases`）
- **根因：** `CaseDetail.tsx` 中 `navigate('/')` 硬编码为根路由

**受影响代码位置：**
1. `dashboard/web/src/pages/CaseDetail.tsx` 第 80 行：返回按钮 `onClick={() => navigate('/')}`
2. `dashboard/web/src/pages/CaseDetail.tsx` 第 48 行：错误状态 `onRetry={() => navigate('/')}`

## Acceptance Criteria

- [ ] Case 详情页返回按钮点击后导航到 `/cases`
- [ ] Case 详情页错误状态重试按钮导航到 `/cases`

## Dependencies

None

## Out of Scope

- 浏览器原生后退按钮行为
- Breadcrumb 导航

## Technical Notes

两处 `navigate('/')` 改为 `navigate('/cases')` 即可。

---

_Generated by Conductor. Review and edit as needed._
