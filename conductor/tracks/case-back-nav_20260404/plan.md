# Implementation Plan: Case 页面返回按钮导航到 Cases 列表

**Track ID:** case-back-nav_20260404
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-04
**Status:** [ ] Not Started

## Overview

修改 CaseDetail.tsx 中两处 `navigate('/')` 为 `navigate('/cases')`。

## Phase 1: Fix Navigation

修改返回按钮和错误状态的导航目标。

### Tasks

- [ ] Task 1.1: 修改 CaseDetail.tsx 第 80 行返回按钮 `navigate('/')` → `navigate('/cases')`
- [ ] Task 1.2: 修改 CaseDetail.tsx 第 48 行错误状态 `navigate('/')` → `navigate('/cases')`

### Verification

- [ ] TypeScript 编译通过
- [ ] 页面可正常加载

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 返回按钮导航到 /cases | Interaction | Playwright 打开 Case 详情页 → 点击返回按钮 → 验证 URL 为 /cases |
| 2 | 错误状态导航到 /cases | API | 检查代码中 onRetry 回调指向 /cases |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
