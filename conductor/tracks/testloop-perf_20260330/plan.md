# Implementation Plan: test-loop/supervisor 性能优化

**Track ID:** testloop-perf_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [~] In Progress

## Overview

分 3 个阶段：先改脚本层（trend-analyzer + spec-scanner），再改 SKILL/Agent 定义层（state-update + runner），最后验证。各阶段互相独立，可并行但建议按顺序确保稳定。

## Phase 1: 脚本层优化（trend-analyzer + spec-scanner）

修改 executor 脚本，不影响 SKILL 层逻辑。

### Tasks

- [x] Task 1.1: `trend-analyzer.sh` 增加 `conclusions` 预计算对象 — 在现有输出 JSON 末尾新增 `conclusions: { plateau, regressionRate, fixSuccessRate, scanPrecision, efficiencyWarning }` 字段，保留原有字段向后兼容
- [x] Task 1.2: `spec-scanner.sh` 增加 AC hash cache + delta 检测 — 新增 `tests/.cache/spec-hashes.json`，首次运行全量扫描并写 cache，后续只扫 `git diff --name-only` 变更的 tracks + cache miss 的 tracks
- [x] Task 1.3: 验证脚本输出兼容性 — 手动运行 `trend-analyzer.sh 3` 和 `spec-scanner.sh` 确认输出格式正确，health-check.sh / pre-flight.sh 不受影响

### Verification

- [x] trend-analyzer.sh 输出包含 `conclusions` 字段且原有字段不变
- [x] spec-scanner.sh 首次运行生成 cache，第二次运行仅扫描变更 tracks（输出行数明显减少）

## Phase 2: SKILL/Agent 定义层精简

修改 test-loop 和 supervisor 的定义文件。

### Tasks

- [x] Task 2.1: 精简 `state-update.md` Phase Retrospective — 删除 Step 2.2 中的跨轮重复浪费检测、Recipe Evolution Check（Fix + Gen 两处）；保留熔断检查（100% 同一错误 → earlyExit 标记到 roundJourney）；目标从 ~156 行降到 ~60 行
- [x] Task 2.2: 精简 `test-supervisor-runner.md` Step 2 Strategic Review — 改为读取 `conclusions` JSON 直接使用，不再对原始趋势数据推理；保留 Escalation Reasoning（Question 7）和 Scan Strategy Selection（Question 6）
- [x] Task 2.3: 精简 `test-supervisor-runner.md` Step 4C — 删除 Scan Recipe Evolution Check 整段；Step 4B Meta-analysis 保留不变
- [x] Task 2.4: 精简 `test-supervisor-runner.md` Step 1 Self-check — 增加快速路径：gate=pass 且 autoRepaired=false 且 health=healthy 时跳过 4 个推理问题，直接进入 Step 2

### Verification

- [x] state-update.md 行数 ≤ 70 行，仅保留 state update + 熔断检查 + continuation check
- [x] test-supervisor-runner.md Step 4C 不存在 Recipe Evolution 内容
- [x] Step 1 在正常情况下跳过 git log/diff（观察 tool call 数量减少）

## Phase 3: 集成验证 + 收尾

### Tasks

- [x] Task 3.1: 运行 `npm test` 确认无回归
- [ ] Task 3.2: 运行一轮 `/test-supervisor run` 观察实际表现 — 确认 supervisor tool calls 减少、test-loop 每 phase 不再输出 recipe 建议
- [x] Task 3.3: 更新 ISS-169 状态 + track metadata

### Verification

- [x] npm test 全部通过
- [ ] 一轮 supervisor run 正常完成，无错误

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | AC1: Recipe Evolution 移除 + 元数据保留 | E2E | Grep state-update.md 确认无 "Recipe Evolution" 段；grep test-supervisor-runner.md 确认无 "Step 4C"/"Recipe Evolution"；读 phaseHistory schema 确认 recipe_used 等字段仍在定义中 |
| 2 | AC2: trend-analyzer.sh 预计算结论 | E2E | 运行 `bash tests/executors/trend-analyzer.sh 3` → 验证输出 JSON 包含 `conclusions` 对象 + 原有字段保留 |
| 3 | AC3: Retrospective 精简为熔断检查 | E2E | 统计 state-update.md 行数 ≤ 70；grep 确认保留 "earlyExit" 和 "100%" 关键词；确认无 "跨轮" 和 "recipe" 内容 |
| 4 | AC4: Self-check 快速路径 | E2E | 读 test-supervisor-runner.md 确认 Step 1 有 fast-path 条件判断（gate=pass + autoRepaired=false + health=healthy → skip） |
| 5 | AC5: spec-scanner delta 检测 | E2E | 删除 tests/.cache/spec-hashes.json → 运行 spec-scanner.sh（全量）→ 确认 cache 生成 → 再次运行（无 git diff）→ 确认输出显著减少 |
| 6 | AC6: npm test 不回归 | E2E | 运行 `npm test` → 全部通过 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
