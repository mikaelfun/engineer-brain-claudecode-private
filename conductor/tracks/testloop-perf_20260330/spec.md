# Specification: test-loop/supervisor 性能优化

**Track ID:** testloop-perf_20260330
**Type:** Refactor
**Created:** 2026-03-30
**Status:** Draft
**Issue:** ISS-169

## Summary

精简 test-loop 和 test-supervisor 的推理开销：砍 Recipe Evolution 自动推理（保留元数据）、精简 Supervisor Strategic Review（预计算+快速路径）、SCAN delta 检测、test-loop Retrospective 瘦身。预计 32min→22-25min（节省 25-30%）。

## Context

基于实际性能数据分析，supervisor 推理占总耗时 50%（~16min/32min），Recipe Evolution 每轮消耗 ~900 tokens 但从未产出被采纳的 recipe。test-loop 的 Phase Retrospective 与 supervisor 的 Meta-analysis 存在重叠检测，且"跨轮重复浪费"检测在 test-loop 单轮内无法获取跨轮数据。

## Acceptance Criteria

- [ ] AC1: Recipe Evolution Check 从 test-loop `state-update.md`（2处：Fix Recipe + Gen Recipe）和 supervisor `test-supervisor-runner.md`（Step 4C）中移除；phaseHistory 元数据标记（recipe_used, fix_step_count, yaml_lines, gap_source）保留不变
- [ ] AC2: `trend-analyzer.sh` 输出预计算结论 JSON（plateau: yes/no, regression_rate, fix_success_rate, scan_precision, efficiency_warning），supervisor Step 2 直接使用结论而非对原始数据推理
- [ ] AC3: test-loop `state-update.md` 的 Phase Retrospective 精简为熔断检查（仅检测 100% 同一错误→earlyExit），砍掉跨轮重复浪费检测和 Recipe Evolution，目标 ~20 行
- [ ] AC4: supervisor Step 1 Self-check 增加快速路径：gate=pass 且 pre-flight 无异常信号时跳过 4 个推理问题（git log/diff），直接进入 Step 2
- [ ] AC5: spec-scanner.sh 增加 AC hash cache，只扫描 git diff 变更的 conductor tracks（cache 文件存 tests/.cache/spec-hashes.json）
- [ ] AC6: 现有 `npm test` 全部通过，health-check.sh / pre-flight.sh 输出格式兼容

## Dependencies

- `tests/executors/trend-analyzer.sh` — 需要修改输出格式
- `tests/executors/spec-scanner.sh` — 需要增加 delta 检测
- `.claude/skills/test-loop/phases/state-update.md` — 精简 Retrospective
- `.claude/agents/test-supervisor-runner.md` — 精简 Strategic Review + Self-check
- Dashboard modes (`modes/trends.md`, `modes/health.md`) — 可能需适配 trend-analyzer 新输出

## Out of Scope

- FIX 并行化（复杂度高，单独 track）
- 新增 recipe 或 recipe 架构变更
- Dashboard UI 变更
- test-loop 核心 5-phase 执行逻辑变更

## Technical Notes

- trend-analyzer.sh 新输出格式需向后兼容（保留原有字段，新增 `conclusions` 对象）
- spec-scanner.sh 的 hash cache 需处理首次运行（无 cache = 全量扫描）
- state-update.md 精简后需确保 earlyExit 信号能被 supervisor 正确识别（通过 state.json 的 roundJourney 传递）

---

_Generated by Conductor. Review and edit as needed._
