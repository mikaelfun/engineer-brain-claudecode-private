# Specification: verify.md 桥接到 SKILL.md — 消除双实现

**Track ID:** verify-bridge_20260329
**Type:** Bug
**Created:** 2026-03-29
**Status:** Draft
**Issue:** ISS-156

## Summary

将 verify.md（全局 command）从包含完整旧实现改为桥接模式，只做 pre-flight 和模式检测，然后指向 SKILL.md 执行。

## Context

当前并存的两个实现：
- `verify.md`（全局 command）：旧的两步实现（npm test + Playwright UI tests），metadata 格式 `{ unitTest, uiTest, verifiedAt }`
- `.claude/skills/conductor/verify/SKILL.md`：新的五步推理驱动实现，metadata 格式 `{ regression, criteria: [{id, result, evidence, recipe}] }`

两者 metadata.json 输出格式不兼容，Dashboard 解析 verification 字段时可能出问题。

## Acceptance Criteria

- [ ] AC1: verify.md 保留 Pre-flight Checks、Mode Detection（verify/mark-done/reopen/interactive）、Track Resolution 逻辑
- [ ] AC2: verify.md 的 Mode: Verify 部分改为"读取并执行 `.claude/skills/conductor/verify/SKILL.md` 的完整 5 步流程"
- [ ] AC3: verify.md 的 Mode: Mark Done 和 Mode: Reopen 保留原有逻辑（这些不需要推理驱动）
- [ ] AC4: 删除 verify.md 中旧的 Step 3（Run Unit Tests）、Step 4（Run UI Tests）、Step 5（Write Verification Result）的完整实现代码
- [ ] AC5: Dashboard `conductor-reader.ts` 的 verification 解析兼容新旧两种格式（向后兼容已有 tracks）
- [ ] AC6: npm test 回归通过

## Dependencies

- verify-redesign_20260329 (ISS-148) — 已完成，SKILL.md 已就绪

## Out of Scope

- SKILL.md 本身的修改
- 已有 tracks 的 metadata.json 迁移（向后兼容即可）

## Technical Notes

- verify.md 是全局 command（symlink 自 `~/.claude/commands/conductor/`），修改需注意路径
- conductor-reader.ts 需要判断 verification 格式：有 `criteria[]` → 新格式，有 `result.unitTest` → 旧格式
- Mark Done 和 Reopen 模式不经过 SKILL.md，直接在 verify.md 中处理

---

_Generated by Conductor._
