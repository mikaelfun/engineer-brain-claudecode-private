# Implementation Plan: Fix issues.test.ts Mock Alignment with ISS-047

**Track ID:** test-mock-fix_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

Update 3 failing tests in `dashboard/src/routes/issues.test.ts` to align with the ISS-047
refactor where issue status is derived from track metadata via `enrichIssueFromTrack` instead
of being directly written via `updateIssue`. Only the test file needs changes — no route code changes.

## Phase 1: Fix Failing Tests

Fix all 3 failing tests by updating mocks and assertions.

### Tasks

- [x] Task 1.1: Fix "transitions tracked → in-progress" test (~line 404-417)
  - Configure `mockEnrichIssueFromTrack` to return `{ ...issue, status: 'in-progress' }` when called after track metadata update
  - Assert `mockUpdateTrackMetadata` was called with `('some-track', { status: 'in_progress' })` instead of `mockUpdateIssue`
  - Keep expecting `json.issue.status === 'in-progress'`

- [x] Task 1.2: Fix "transitions done → pending, keeps trackId" test (~line 497-510)
  - Route behavior for issues with trackId: calls `updateTrackMetadata(trackId, { verification: undefined })` + `updateTracksMdStatus(trackId, '[~]')`
  - Status re-derives to 'implemented' (track still complete, verification cleared)
  - Update `mockEnrichIssueFromTrack` to return `{ ...issue, status: 'implemented' }` for the final `getIssue` call
  - Assert `mockUpdateTrackMetadata` called with verification: undefined, NOT `mockUpdateIssue`
  - Update expected status from 'pending' to 'implemented'

- [x] Task 1.3: Fix "acquires lock and starts implementation when available" test (~line 1022-1034)
  - Configure `mockEnrichIssueFromTrack` to return issue with status 'tracked' (for guard) then 'in-progress' (for response)
  - Assert `mockUpdateTrackMetadata` was called with `('some-track', { status: 'in_progress' })` instead of `mockUpdateIssue`

### Verification

- [x] Run `npm test` in dashboard/ — all 3 previously failing tests now pass
- [x] No regressions in other tests

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Test "transitions tracked → in-progress" passes: mock enrichIssueFromTrack to... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Test "transitions done → pending, keeps trackId" passes: update assertion to ... | Interaction | Navigate to page → perform interaction → verify expected outcome |
| 3 | Test "acquires lock and starts implementation when available" passes: assert ... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | All existing passing tests continue to pass (no regressions) | Skip | Covered by unit tests |
| 5 | `npm test` in dashboard/ passes clean | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已修改并通过
- [x] 关联 Issue JSON (ISS-052) 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
