# Implementation Plan: Teams digest per-chat 并行生成

**Track ID:** digest-per-chat_20260418
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-18
**Status:** [ ] Not Started

## Overview

重构 `generate-digest.py` 的 teams digest 生成逻辑：从"拼所有 chat → 截断 → 单次 LLM" 改为 "per-chat 独立 LLM call → 并行 → 纯代码合并"。

## Phase 1: Per-chat LLM call + 并行

重构 `generate_teams_digest()` 函数。

### Tasks

- [ ] Task 1.1: 新增 `score_and_extract_single_chat()` 函数 — 接收单个 chat 文件完整内容 + case-info head，调 LLM 返回 `{"relevance": "high|low", "reason": "...", "key_facts": [...], "timeline_entries": [...]}`
- [ ] Task 1.2: 新增 `generate_teams_digest_parallel()` 函数 — 用 `ThreadPoolExecutor(max_workers=5)` 并行调 `score_and_extract_single_chat()`，收集所有结果
- [ ] Task 1.3: 新增 `merge_digest_results()` 函数 — 纯代码合并：按日期排序 key_facts，生成 timeline，拼成 teams-digest.md 格式 + _relevance.json
- [ ] Task 1.4: 替换 `generate_teams_digest()` 内部逻辑 — 调用新的并行函数，保持外部接口（参数、输出文件路径）不变
- [ ] Task 1.5: 移除 chat 文件的 `max_chars=3000` 截断 — `read_file` 不再截断 chat 内容（或设一个足够大的上限如 50000）

### Verification

- [ ] 运行 `python3 generate-digest.py --type teams --case-dir ../data/cases/active/2602130040001034 --case-number 2602130040001034` 验证输出包含 4/17 对话

## Phase 2: Vision 图片适配

确保 per-chat 模式下图片正确传入各自 chat 的 LLM call。

### Tasks

- [ ] Task 2.1: 在 `score_and_extract_single_chat()` 中集成 `extract_images_from_page()` — 如果 chat 文件引用了图片，构建 vision content blocks
- [ ] Task 2.2: 验证无图片 chat 走纯文本路径，有图片 chat 走 vision 路径

### Verification

- [ ] 对有图片和无图片的 chat 分别测试，确认输出正确

## Phase 3: 兼容性验证

确保新逻辑与下游 assess 完全兼容。

### Tasks

- [ ] Task 3.1: 验证输出 `teams-digest.md` 格式与现有 assess SKILL.md 的读取逻辑兼容
- [ ] Task 3.2: 验证 `_relevance.json` 格式与现有 schema 兼容（chats.{name}.relevance/reason/chatIds）
- [ ] Task 3.3: 对 3 个活跃 case 各运行一次 digest 生成，对比新旧输出质量

### Verification

- [ ] 3 个 case 的 digest 输出格式正确，assess 能正常读取

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | 每个 chat 完整输入不截断 | E2E | 用 2602130040001034 的 3404 字符 chat → 运行 generate-digest.py → 验证 digest 包含 4/17 "sure" 消息 |
| 2 | 并行执行 | E2E | 用 3+ chat 的 case → 记录执行时间 → 验证总耗时 < 串行耗时的 50% |
| 3 | 输出格式兼容 | E2E | 运行 generate-digest.py → diff teams-digest.md 格式（Key Facts / Timeline / Low-Relevance 三节均在） → 验证 _relevance.json schema |
| 4 | low relevance 不提取 facts | E2E | 构造一个仅含 case number 的 chat → 运行 → 验证 relevance=low 且 key_facts=[] |
| 5 | OneNote 不受影响 | E2E | 运行 --type onenote → 验证输出不变 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
