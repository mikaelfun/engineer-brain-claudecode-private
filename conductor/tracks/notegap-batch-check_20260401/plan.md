# Implementation Plan: Daily Ops NoteGap 批量检测 + 生成

**Track ID:** notegap-batch-check_20260401
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-01
**Status:** [x] Complete

## Overview

新增后端 batch check endpoint + 前端 Check All 按钮。Draft 生成在 Node.js 中实现（规则化逻辑，无需 LLM），保持与 `/note-gap` skill 相同的输出格式。

## Phase 1: 后端批量检测 + 生成

### Tasks

- [x] Task 1.1: 在 `note-gap-routes.ts` 新增 `POST /` batch check endpoint — 扫描所有活跃 case 的 `notes.md`，解析最新 note 时间，对比 threshold 判断 gap
- [x] Task 1.2: 对有 gap 的 case，从 `case-summary.md` 提取 `lastNoteDate` 之后的进展条目，按 `/note-gap` skill 的 YYMMDD-- 规则生成 `note-draft.md`
- [x] Task 1.3: 返回检测结果 `{ checked: number, gaps: number, generated: number, details: [...] }`

### Verification

- [ ] 准备测试 case（有/无 notes.md，有/无 gap），调用 API 验证结果正确

## Phase 2: 前端 Check All 按钮

### Tasks

- [x] Task 2.1: 在 `hooks.ts` 新增 `useCheckAllNoteGaps` mutation hook（POST `/api/note-gaps`）
- [x] Task 2.2: 在 `LaborEstimatePage.tsx` Note Gaps 区域添加 "Check All" 按钮，替换 TODO 注释
- [x] Task 2.3: 添加进度状态展示 — checking 时显示 spinner + "检测中..."，完成后显示结果摘要（如 "检测 15 cases，发现 3 个 gap"）

### Verification

- [ ] 点击 Check All，确认进度展示 + gaps 列表刷新

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Check All 按钮显示在 Note Gaps 区域 | Interaction | Playwright 打开 Daily Ops → 断言 Check All 按钮可见 |
| 2 | 扫描所有活跃 Case 检测 gap | E2E | Backup → 构造 2 个 case（1 有 gap、1 无 gap） → POST /api/note-gaps → 验证返回 gaps=1 → verify note-draft.md 生成 → restore |
| 3 | 生成的 draft 格式正确 | E2E | 构造 case-summary.md + notes.md → POST /api/note-gaps → 读取 note-draft.md → 验证 YAML 格式 + YYMMDD 格式 + title="fangkun note" → restore |
| 4 | 检测过程中显示进度 | Interaction | Playwright 点击 Check All → 断言 spinner 出现 → 等待完成 → 断言结果摘要显示 |
| 5 | 完成后自动刷新 gaps 列表 | Interaction | 构造 note-draft.md 测试文件 → 点击 Check All → 等待完成 → 断言新 gap 卡片出现 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
