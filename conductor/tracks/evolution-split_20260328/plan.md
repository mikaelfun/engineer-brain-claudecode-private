# Implementation Plan: Evolution 概念拆分 — Discoveries + Evolution

**Track ID:** evolution-split_20260328
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-28
**Status:** [ ] Not Started

## Overview

拆分 test-supervisor SKILL.md 中的 evolution mode 为两个独立命令，同时扩展 stats-reporter.sh 自动生成 discoveries.json。改动集中在 2 个文件，无执行逻辑变更。

## Phase 1: stats-reporter.sh 生成 discoveries.json

扩展 stats-reporter.sh，在每轮结束时扫描 fixes/ 目录，聚合生成 discoveries.json。

### Tasks

- [ ] Task 1.1: 在 stats-reporter.sh 末尾新增 discoveries.json 生成逻辑 — 扫描 fixes/ 目录，按 testId 聚合 analysis/fix/verify/regression 文件，输出结构化 JSON
- [ ] Task 1.2: discoveries.json schema — `{ discoveries: [{ testId, foundRound, severity, rootCause, fixFile, verifiedRound, status }] }`，status 枚举：verified/retry-needed/diagnosed/unaddressed/regression

### Verification

- [ ] 手动运行 `bash tests/executors/stats-reporter.sh 0`，确认 `tests/discoveries.json` 生成且格式正确

## Phase 2: SKILL.md 拆分 discoveries + evolution

重写 test-supervisor SKILL.md 的 Mode 7，新增 Mode 8（discoveries 原有内容迁移），原 Mode 8 auto-heal 顺延。

### Tasks

- [ ] Task 2.1: 新增 Mode 7 `discoveries` — 从 discoveries.json + fixes/ 目录展示项目质量发现，输出模板包含 Impact Summary + Discovery Timeline
- [ ] Task 2.2: 重写 Mode 8 `evolution` — 仅保留 evolution.json（Framework Changes）+ self-heal records + Capability Growth metrics，去掉 per-test lifecycle 和 round discovery
- [ ] Task 2.3: 原 Mode 8 `auto-heal` 改为 Mode 9，更新所有内部引用
- [ ] Task 2.4: 更新参数解析部分 — mode 列表新增 `discoveries`，编号更新
- [ ] Task 2.5: 更新文件依赖表 — 新增 `tests/discoveries.json` 行

### Verification

- [ ] 检查 SKILL.md 参数解析、mode 编号、文件依赖表一致性

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | discoveries.json 自动生成 | E2E | 确保 fixes/ 目录有测试数据 → 运行 stats-reporter.sh → 验证 discoveries.json 存在且 schema 正确 |
| 2 | discoveries 命令输出格式 | Skip | 需要 test-supervisor session 执行，人工验证输出格式 |
| 3 | evolution 命令不再含 per-test lifecycle | Skip | 需要 test-supervisor session 执行，人工验证输出只含 framework changes + self-heal |
| 4 | auto-heal 功能不受影响 | Skip | Mode 编号顺延，逻辑不变，人工确认引用一致 |

**Test Type Legend:**
- **E2E** — Backup data -> run actual workflow/script -> verify file outputs + API + UI -> restore
- **Skip** — 需 supervisor session 执行，人工验证

## Post-Implementation Checklist

- [ ] stats-reporter.sh discoveries.json 生成逻辑已添加
- [ ] SKILL.md mode 编号和参数解析一致
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
