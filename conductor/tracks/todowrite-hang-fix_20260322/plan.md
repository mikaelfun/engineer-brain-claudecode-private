# Implementation Plan: Fix data-refresh hang on TodoWrite tool call

**Track ID:** todowrite-hang-fix_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Add a blocklist of REPL-only tool names to `createStepCanUseTool` in `steps.ts`, so that any attempt by the agent to call `TodoWrite`, `TaskCreate`, etc. is immediately denied with a message telling it to skip task tracking and proceed with actual work. This is a small, targeted fix in one file.

## Phase 1: Add REPL-only tool blocklist to canUseTool

Block REPL-only tools that hang in headless SDK mode.

### Tasks

- [x] Task 1.1: Add a `REPL_ONLY_TOOLS` constant set in `dashboard/src/routes/steps.ts` containing: `TodoWrite`, `TodoRead`, `TaskCreate`, `TaskUpdate`, `TaskList`, `TaskGet`, `EnterPlanMode`, `ExitPlanMode`
- [x] Task 1.2: In `createStepCanUseTool`, add a check before the existing `AskUserQuestion` handler: if `toolName` is in `REPL_ONLY_TOOLS`, return `{ behavior: 'deny', message: 'Task tracking tools are not available in automated SDK sessions. Skip task management and proceed with the actual work.' }`
- [x] Task 1.3: Verify TypeScript compiles: `npx tsc --noEmit` in `dashboard/`

### Verification

- [ ] Manually trigger data-refresh via Dashboard step API — should complete without hanging

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | REPL-only tools are denied in SDK step sessions | Skip | Backend-only logic change, covered by unit test |
| 2 | Denied tools return helpful skip message | Skip | Verified by unit test assertion on deny message |
| 3 | data-refresh completes without hanging | Skip | Requires live SDK session — manual verification |
| 4 | AskUserQuestion interception unchanged | Skip | Existing unit tests cover this |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建/更新并通过
- [x] 关联 Issue JSON 状态已更新
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
