# Implementation Plan: Patrol new-case mode IR-first 路径修复

**Track ID:** ir-first-routing_20260424
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-24
**Status:** [ ] Not Started

## Overview

修改 assess SKILL.md 的决策规则，使 "phone call IR + 无工程师邮件" 场景正确触发 IR-first 路径。同时更新路由表注释和邮件去重规则的说明。

## Phase 1: Assess 决策规则修复

修改 `.claude/skills/casework/act/assess/SKILL.md` 中的 LLM prompt 决策规则。

### Tasks

- [ ] Task 1.1: 在决策规则第 5 条前新增/修改规则，明确 "phone call IR + 无工程师邮件" 场景：
  - 条件：Case 有 phone call / notes 记录的 IR activity，但 emails.md 中无工程师 outbound 邮件
  - 输出：actualStatus=pending-engineer, actions=[troubleshooter + email-drafter(initial-response)]
  - 确保此规则优先级高于规则 #2（已有 IR → troubleshooter only）
- [ ] Task 1.2: 更新邮件去重规则说明，明确 "phone call IR 不等于已发 IR 邮件"
- [ ] Task 1.3: 更新路由参考表注释，在 `pending-engineer（新 case）` 行补充 "含 phone call IR 场景"

### Verification

- [ ] 阅读修改后的 assess SKILL.md，确认决策规则覆盖 phone call IR 场景

## Phase 2: Act 路由表和测试 fixture 更新

更新路由参考表和测试数据，确保 IR-first 路径对 phone call IR 场景生效。

### Tasks

- [ ] Task 2.1: 在 `.claude/skills/casework/act/SKILL.md` 路由参考表中补充 phone call IR 场景行
- [ ] Task 2.2: 在 `act/tests/fixtures/` 新增 `plan-ir-first-phonecall.json` 测试 fixture，模拟 phone call IR + 无工程师邮件的 execution-plan
- [ ] Task 2.3: 在 `act/tests/test_read_plan.sh` 中新增测试用例，验证 phone call IR fixture 的 IR_FIRST=1

### Verification

- [ ] 运行 `bash .claude/skills/casework/act/tests/test_read_plan.sh` 全部通过
- [ ] 新增的 phone call IR fixture 被正确解析为 IR_FIRST=1

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Assess 决策规则覆盖 phone call IR 场景 | E2E | 构造 phone call IR + 无工程师邮件的 case 数据 → 调用 assess → 验证 execution-plan.json 包含 troubleshooter + email-drafter(initial-response) |
| 2 | read-plan.sh IR_FIRST=1 | E2E | 用 plan-ir-first-phonecall.json fixture → 运行 read-plan.sh → 验证输出 IR_FIRST=1 |
| 3 | Act 路由表文档准确 | Visual | 阅读 act SKILL.md 路由参考表，确认 phone call IR 场景有对应行 |
| 4 | 邮件去重规则不阻止 IR 邮件 | E2E | 构造有 phone call 但无 outbound 邮件的 emails.md → 运行 assess → 验证不触发 "已发 IR" 去重 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 关联 Issue ISS-247 状态已更新为 `implemented`
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
