# Implementation Plan: Case Detail Header Redesign

**Track ID:** case-header-redesign_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

分三阶段：先扩展后端数据、再重构前端 header 布局、最后添加健康分数。

## Phase 1: Backend — 扩展数据可用性

确保所有需要的字段在 API 中可用。

### Tasks

- [x] Task 1.1: `case-reader.ts` — 解析 case-info.md 中的 Snapshot 时间行（`> Snapshot: ...`），作为 `fetchedAt` 返回
- [x] Task 1.2: `types/index.ts` — 更新 `CaseInfo` 添加 `fetchedAt?: string`；更新 `CaseHealthMeta` 添加 `actualStatus`、`daysSinceLastContact`、`statusJudgedAt`、`compliance` 等已有 JSON 字段
- [x] Task 1.3: `cases.ts` — GET `/cases/:id` 响应中补充 `modifiedAt`（case 目录下文件的最新 mtime）

### Verification

- [x] `/api/cases/:id` 返回 `fetchedAt` 和 `modifiedAt`
- [x] `/api/cases/:id/meta` TypeScript 类型与实际 JSON 一致

## Phase 2: Frontend — Header 布局重构

将当前分散的信息整合到紧凑的 header 卡片。

### Tasks

- [x] Task 2.1: 重构 header 区域 — Title 大字加粗占满宽度，元数据行紧凑排列（Case号 · Severity · Status · Owner · Age · 创建/更新/拉取时间）
- [x] Task 2.2: 添加产品路径行 — SAP 字段带文件夹图标独立显示
- [x] Task 2.3: 整合 SLA 指示器到 header 底部 — 移除当前独立的 SLA 区块，合并到 header 卡片内
- [x] Task 2.4: 时间格式化 — 创建/更新/拉取时间使用紧凑格式（M/D HH:MM）

### Verification

- [x] Header 信息密度接近参考截图
- [x] 所有关键信息一目了然

## Phase 3: 健康分数

添加右上角的健康分数圆圈。

### Tasks

- [x] Task 3.1: 实现健康分数计算逻辑（基于 SLA + daysSinceLastContact + inspection 状态）
- [x] Task 3.2: 创建 HealthScoreBadge 组件 — 圆形数字，颜色按分数段变化（绿/黄/红）
- [x] Task 3.3: 集成到 header 右上角

### Verification

- [x] 健康分数正确计算并显示
- [x] 颜色随分数变化

## Final Verification

- [x] All acceptance criteria met
- [x] Header 信息完整且紧凑
- [x] 响应式布局在不同屏幕宽度正常
- [x] 无 console 报错

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Header 显示完整 title（大字加粗） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | 元数据行显示：Case号 · SR号(如有) · Severity · Status · Owner · 已开天数 · 创建时间 · 更新时间 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | 产品路径（SAP）显示为独立行，带文件夹图标 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | SLA 状态整合到 header 底部（IR/FWR/FDR 合并显示） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | Header 右侧显示健康分数圆圈（基于 SLA 状态和 daysSinceLastContact 计算） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | 拉取时间显示（从 case-info.md Snapshot 行解析） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 7 | CaseHealthMeta 类型更新，声明 actualStatus/daysSinceLastContact 等已有字段 | Visual | Navigate to page → screenshot → verify visual matches spec |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
