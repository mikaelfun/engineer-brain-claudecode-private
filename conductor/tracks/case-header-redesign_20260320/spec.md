# Specification: Case Detail Header Redesign

**Track ID:** case-header-redesign_20260320
**Type:** Refactor
**Created:** 2026-03-20
**Status:** Draft

## Summary

重新设计 Case Detail 页面的 header 区域，参考截图中的信息密度和布局，将当前分散在 header + SLA 指示器 + Info tab 中的关键信息整合到一个紧凑的 header 卡片中。

## Context

当前 case detail header 信息过少且分散：
- Header 只显示：case号、severity、status、title、customer | assignedTo | age | sap
- SLA 指示器单独一行
- 大量有用信息（创建时间、更新时间、产品路径、SLA 详情等）藏在 Info tab 里

### 参考截图设计要素

```
┌─────────────────────────────────────────────────────────────────────┐
│ ■ 标题（大字加粗，完整 case title）                          [78] 健康分 │
│ Case号 · SR号 · SEV B · 活跃 · Owner · 已开N天                  🤖 AI │
│ · 创建时间 · 更新时间 · 拉取时间                        ⏱️ 📋 ↻ 按钮  │
│ 📁 Azure/21Vianet Mooncake/21Vianet China Intune          🗑️ 删除  │
│ ✅ SLA: Met 0                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 数据可用性分析

| 字段 | 来源 | 状态 |
|---|---|---|
| Case 号 | `caseInfo.caseNumber` | ✅ 已有 |
| Title | `caseInfo.title` | ✅ 已有 |
| Severity | `caseInfo.severity` | ✅ 已有 |
| Status | `caseInfo.status` | ✅ 已有 |
| Owner | `caseInfo.assignedTo` | ✅ 已有 |
| 已开天数 | `caseInfo.caseAge` | ✅ 已有 |
| 创建时间 | `caseInfo.createdOn` | ✅ 已有 |
| 产品路径 SAP | `caseInfo.sap` | ✅ 已有（当前在 Info tab） |
| SLA 状态 | `meta.irSla/fwr/fdr` | ✅ 已有 |
| actualStatus | `meta.actualStatus` | ✅ JSON 中有，类型未声明 |
| daysSinceLastContact | `meta.daysSinceLastContact` | ✅ JSON 中有，类型未声明 |
| 更新时间 | case-info.md 中无 | ⚠️ 需从文件 mtime 获取 |
| 拉取时间 | case-info.md Snapshot 行 | ⚠️ 需新增解析 |
| 健康分数 | 无 | ❌ 需设计算法 |
| SR 号 | case-info.md 中可能有 | ⚠️ 需确认 |

## Acceptance Criteria

- [ ] Header 显示完整 title（大字加粗）
- [ ] 元数据行显示：Case号 · SR号(如有) · Severity · Status · Owner · 已开天数 · 创建时间 · 更新时间
- [ ] 产品路径（SAP）显示为独立行，带文件夹图标
- [ ] SLA 状态整合到 header 底部（IR/FWR/FDR 合并显示）
- [ ] Header 右侧显示健康分数圆圈（基于 SLA 状态和 daysSinceLastContact 计算）
- [ ] 拉取时间显示（从 case-info.md Snapshot 行解析）
- [ ] CaseHealthMeta 类型更新，声明 actualStatus/daysSinceLastContact 等已有字段

## Dependencies

None — 所有数据已存在或可从现有文件解析

## Out of Scope

- 改变 tab 结构和 tab 内容
- 修改 CaseAIPanel
- 改变 case-info.md 的生成逻辑
- Dashboard 列表页的 UI 改动

## Technical Notes

### 健康分数算法（建议）
```
baseScore = 100
- IR SLA breached: -30
- FWR breached: -20
- FDR breached: -20
- daysSinceLastContact > 3: -10
- daysSinceLastContact > 7: -20 (额外)
- 无 inspection: -10
clamp(0, 100)
```

### 需修改的文件
**Backend:**
1. `dashboard/src/services/case-reader.ts` — 解析 Snapshot 时间、Modified On(如有)
2. `dashboard/src/types/index.ts` — 更新 CaseHealthMeta 类型声明
3. `dashboard/src/routes/cases.ts` — 可选：合并 meta 到 case detail 响应

**Frontend:**
4. `dashboard/web/src/pages/CaseDetail.tsx` — 重构 header 区域
5. `dashboard/web/src/components/common/Badge.tsx` — 可能需新增 HealthScoreBadge

---

_Generated by Conductor. Review and edit as needed._
