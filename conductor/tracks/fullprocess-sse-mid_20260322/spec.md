# Specification: Fix Full Process SSE Intermediate Messages Not Shown

**Track ID:** fullprocess-sse-mid_20260322
**Type:** Bug
**Created:** 2026-03-22
**Status:** Draft

## Summary

AI Assistant 面板在执行 Full Process 时只显示 start/completed 系统消息，中间过程（tool 调用、thinking）不显示。

## Problem Description

1. 后端 `broadcastSDKMessages()` 正确发送带 `sessionId` 的 `case-step-progress` 事件
2. 但 `case-step-started` 在 SDK session 启动前发送，**没有 sessionId**
3. 前端 `useSSE.ts` 只在 `case-step-started` 时设置 `activeSessionId`
4. `case-step-progress` 监听器不更新 `activeSessionId`
5. 消息存入了 `messages[caseNumber]` timeline，但 session 绑定断裂

## Acceptance Criteria

- [x] `case-step-progress` 监听器在收到 sessionId 且当前无 activeSession 时自动设置
- [x] Full Process 执行时 AI Assistant 面板实时显示 tool 调用和 thinking 消息

## Technical Notes

修改 `useSSE.ts` 中 `case-step-progress` 事件处理，添加 activeSession 自动发现。

---

_Generated by Conductor._
