# Implementation Plan: Cancel Track Creation Button

**Track ID:** cancel-track-creation_20260322
**Created:** 2026-03-22
**Status:** [x] Complete

## Phase 1: Backend Cancel Support

### Tasks

- [x] Task 1.1: Add `cancel()` method to IssueTrackStateManager + cancellation flag check
- [x] Task 1.2: Add `POST /api/issues/:id/cancel-track` route
- [x] Task 1.3: Guard `finalizeTrackCreation` and `processQueryMessages` against cancelled state

## Phase 2: Frontend Cancel Button

### Tasks

- [x] Task 2.1: Add `useCancelTrack()` hook in hooks.ts
- [x] Task 2.2: Add Cancel button to TrackProgressPanel (with `onCancel` prop)
- [x] Task 2.3: Wire Cancel in Issues.tsx (pass onCancel prop)
- [x] Task 2.4: TypeScript compile + test pass

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Cancel button visible in TrackProgressPanel header during active creation | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 2 | Cancel button hidden after completion/error or when a question is pending | Interaction | Navigate to relevant page → locate element → click → verify state change |
| 3 | Clicking Cancel calls backend endpoint to stop tracking + reset issue to pending | API | Call the relevant endpoint → verify response shape and status code |
| 4 | Backend sets a cancellation flag so the running agent's output is discarded | Interaction | Start operation → click cancel/stop → verify operation ends and state reverts |
| 5 | SSE broadcasts cancellation event to update all connected clients | Interaction | Start operation → click cancel/stop → verify operation ends and state reverts |
| 6 | After cancel, the panel shows "Cancelled" state with option to retry | Interaction | Start operation → click cancel/stop → verify operation ends and state reverts |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor._
