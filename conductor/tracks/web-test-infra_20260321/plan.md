# Implementation Plan: Frontend Test Infrastructure

**Track ID:** web-test-infra_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

三阶段：先装依赖+配置 Vitest，再建共享 mock 工具+示例测试，最后集成到根命令+更新文档。

## Phase 1: Vitest + jsdom 配置

安装依赖，创建前端独立的 Vitest 配置文件，确保空测试套件能跑通。

### Tasks

- [x] Task 1.1: **安装依赖** — 在 dashboard/web/ 安装 vitest、@vitest/coverage-v8、jsdom、@testing-library/react、@testing-library/jest-dom、@testing-library/user-event 为 devDependencies
- [x] Task 1.2: **创建 vitest.config.ts** — 在 dashboard/web/ 创建独立配置：environment: 'jsdom'、globals: true、include: ['src/**/*.test.{ts,tsx}']、setupFiles: ['./src/test-utils/setup.ts']
- [x] Task 1.3: **创建 setup.ts** — dashboard/web/src/test-utils/setup.ts，导入 @testing-library/jest-dom/vitest 扩展 matchers
- [x] Task 1.4: **添加 test scripts** — 在 dashboard/web/package.json 添加 "test": "vitest run"、"test:watch": "vitest"、"test:coverage": "vitest run --coverage"

### Verification

- [x] `cd dashboard/web && npx vitest run` 无报错退出（0 tests, 0 failures）

## Phase 2: 共享 Mock 工具 + 示例测试

建立 test-utils/ 工具集，写 1 个组件测试 + 1 个 hook 测试作为模板。

### Tasks

- [x] Task 2.1: **创建 render helper** — dashboard/web/src/test-utils/render.tsx，封装 QueryClientProvider + MemoryRouter + 自定义 render 函数，导出 screen、userEvent 等常用工具
- [x] Task 2.2: **创建 mock helpers** — dashboard/web/src/test-utils/mocks.ts，提供 createMockCase()、createMockQueryClient() 等工厂函数
- [x] Task 2.3: **创建 test-utils/index.ts** — 统一导出所有 test-utils
- [x] Task 2.4: **组件测试示例** — dashboard/web/src/components/SessionBadge.test.tsx，测试不同 session 状态下的渲染输出（connected/disconnected/无 session）
- [x] Task 2.5: **Hook 测试示例** — dashboard/web/src/hooks/useSSE.test.ts，mock EventSource 测试连接/消息/断开行为

### Verification

- [x] `cd dashboard/web && npx vitest run` 通过 2+ 测试
- [x] 测试中使用了 test-utils/ 的 render helper 和 mock 工厂

## Phase 3: 集成 + 文档

统一根命令，更新 workflow.md 前端测试约定。

### Tasks

- [x] Task 3.1: **统一 test 命令** — 修改 dashboard/package.json 的 "test" 脚本，使其同时跑后端和前端测试（如 vitest run --workspace 或 npm --prefix web test && vitest run）
- [x] Task 3.2: **更新 workflow.md** — 在 conductor/workflow.md 的 Mock Patterns 部分补充前端测试约定：jsdom 环境、render helper 用法、React 组件 mock 模式、hook 测试模式
- [x] Task 3.3: **TypeScript 编译检查** — 确保 dashboard/web/ 的 tsconfig.json 包含 test-utils 和 .test.tsx 文件的类型

### Verification

- [x] `cd dashboard && npm test` 同时跑通后端 3 个测试 + 前端 2+ 测试
- [x] workflow.md 包含前端测试 mock patterns 段落

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | **安装依赖** — 在 dashboard/web/ 安装 vitest、@vitest/coverage-v8、jsdom、@testing-libr... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | **创建 vitest.config.ts** — 在 dashboard/web/ 创建独立配置：environment: 'jsdom'、global... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | **创建 setup.ts** — dashboard/web/src/test-utils/setup.ts，导入 @testing-library/j... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 4 | **添加 test scripts** — 在 dashboard/web/package.json 添加 "test": "vitest run"、"t... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | **创建 render helper** — dashboard/web/src/test-utils/render.tsx，封装 QueryClient... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 6 | **创建 mock helpers** — dashboard/web/src/test-utils/mocks.ts，提供 createMockCase... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 7 | **创建 test-utils/index.ts** — 统一导出所有 test-utils | Visual | Navigate to page → screenshot → verify visual matches spec |
| 8 | **组件测试示例** — dashboard/web/src/components/SessionBadge.test.tsx，测试不同 session ... | Visual | Navigate to page → screenshot → verify visual appearance |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [x] 单元测试文件已创建并通过
- [x] 关联 Issue JSON 状态已更新（如有）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
