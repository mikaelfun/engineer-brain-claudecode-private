# Implementation Plan: test-loop 创建的 Issue 自动生成 Conductor Track

**Track ID:** auto-track-issues_20260330
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-30
**Status:** [x] Complete

## Overview

在 `issue-creator.sh` 的 `ISSUE_CREATED` 输出之前，增加 auto-track 逻辑：创建 conductor track 目录 + spec.md + metadata.json，回写 issue JSON，追加 tracks.md。所有操作在同一个 Node.js 进程中完成（性能 + 原子性）。

## Phase 1: 实现 auto-track 逻辑

在 `issue-creator.sh` 中增加 auto-track 功能。

### Tasks

- [x] Task 1.1: 在 issue JSON 写入成功后，新增 Node.js 代码块：读取 fixRef 文件内容 → 提取 AC → 生成 spec.md
- [x] Task 1.2: 生成 metadata.json（minimal：id, title, type, status=pending, created, updated）
- [x] Task 1.3: 回写 issue JSON — 设置 `trackId` 和 `status: "tracked"`
- [x] Task 1.4: 追加 tracks.md — 新增一行 `| [ ] | {trackId} | {title} | {date} | {date} |`
- [x] Task 1.5: 处理 fixRef 为空/文件不存在的 fallback — 用 title+description 构造 AC

### Verification

- [x] 运行 `issue-creator.sh` 带 fixRef 参数 → 验证 track 目录、spec.md、metadata.json、issue JSON 更新、tracks.md 追加
- [x] 运行 `issue-creator.sh` 不带 fixRef → 验证 fallback AC 生成

## Phase 2: 验证兼容性

确保 issue-scanner.sh 和去重逻辑正常工作。

### Tasks

- [x] Task 2.1: 验证 `ISSUE_EXISTS` 路径不触发 auto-track（仅 `ISSUE_CREATED` 路径触发）
- [x] Task 2.2: 手动运行 `issue-scanner.sh` → 验证新创建的 tracked issue 被正确扫描，AC 被提取

### Verification

- [x] 创建两次相同 testId 的 issue → 第二次应返回 `ISSUE_EXISTS` 且不创建新 track
- [x] 运行 `issue-scanner.sh` → 新 issue 不再出现为 `ISSUE_SKIP|pending`（出现为 tracked 且 ACs 被检测）

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | issue-creator.sh 创建 Issue 后自动生成 track | E2E | ✅ 验证通过 — ISS-177 auto-created track |
| 2 | spec.md AC 从 fixRef 提取 | E2E | ✅ 验证通过 — 4 ACs extracted from fix.md |
| 3 | Issue JSON 回写 trackId 和 status | E2E | ✅ 验证通过 — trackId=testloop-iss-177_20260330, status=tracked |
| 4 | tracks.md 追加行 | E2E | ✅ 验证通过 — new row appended |
| 5 | fixRef 为空时 fallback | E2E | ✅ 验证通过 — ISS-178 used title+description as ACs |
| 6 | ISSUE_EXISTS 不创建新 track | E2E | ✅ 验证通过 — duplicate testId returned ISSUE_EXISTS |
| 7 | issue-scanner 检测 tracked issue | E2E | ✅ 验证通过 — ISS-177 processed as tracked (all ACs covered, no SKIP|pending) |

## Post-Implementation Checklist

- [x] 实现代码已完成并通过 E2E 测试
- [x] 关联 Issue JSON 状态已更新（由 track metadata 派生）
- [x] Track metadata.json 已更新
- [x] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
