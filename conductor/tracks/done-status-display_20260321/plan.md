# Implementation Plan: Done Issue 展示实现状态和验证状态

**Track ID:** done-status-display_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

纯前端修改 Issues.tsx 的 IssueRow 组件，在 Done issue 的 collapsed 行添加验证方式徽章和 track 进度，并在展开详情区域增强 Manual/Auto-Verified 显示。

## Phase 1: Done Issue Collapsed 行增强

在 Done issue 的 collapsed 状态（row header）添加验证方式指示和 track 进度信息。

### Tasks

- [x] Task 1.1: 在 IssueRow 的 row header badges 区域，当 issue.status === 'done' 时，添加验证方式徽章 — 有 verifyResult 显示 "Auto-Verified"（绿色/红色按 overall 结果），无则显示 "Manual"（灰色）
- [x] Task 1.2: 在 Done issue collapsed 行的 metadata 区域（trackId 旁边），利用已有 planData 显示 track 任务完成进度（如 "5/5 tasks ✓"），仅在有 trackId 且 planData 可用时显示
- [x] Task 1.3: 在 Done issue 展开详情中，当无 verifyResult 时显示 "Manually marked as done" 提示卡片（与现有 VerifyResultPanel 互补位置）

### Verification

- [ ] Done issue collapsed 行正确显示 Manual/Auto-Verified 徽章
- [ ] 展开详情中 Manual 和 Auto-Verified 的显示互不干扰

## Final Verification

- [ ] All acceptance criteria met
- [ ] TypeScript 编译无错误
- [ ] Manual/Auto-Verified 在双主题（light/dark）下视觉正确

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Done issue row (collapsed) 显示验证方式徽章："Manual" 或 "Auto-Verified" | Interaction | Navigate to page → find collapsible element → click to toggle → verify expanded/collapsed state |
| 2 | Auto-Verified 徽章显示总体结果颜色（全通过=绿色，部分失败=红色） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Done issue 展开详情时，对于人工 Mark Done 的 issue，显示 "Manually marked as done" 提示 | Interaction | Navigate to page → perform interaction → verify expected outcome |
| 4 | Done issue 展开详情时，对于自动验证的 issue，显示 `VerifyResultPanel`（已有组件，确保渲染正确） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 5 | Track 完成状态（Plan tasks 进度）在 Done issue 的 collapsed 行中有简要展示（如 "5/5 tasks"） | Interaction | Navigate to page → find collapsible element → click to toggle → verify expanded/collapsed state |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
