# Implementation Plan: Dashboard Case 列表优化

**Track ID:** case-list-opt_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

三处小改动集中在 `Dashboard.tsx` 的 Case 卡片渲染逻辑，移除 FWR 徽章 + 突出客户名。单 Phase 即可完成。

## Phase 1: Dashboard Case Card UI 调整

### Tasks

- [x] Task 1.1: 移除 FWR SLA 徽章 — 删除 Dashboard.tsx 中 FWR `<div className="text-center">` 块（lines 295-298）
- [x] Task 1.2: 突出客户名显示 — 将 `{c.customer}` 包裹在 `<span>` 中，添加 `font-semibold` + `color: var(--accent-cyan)` 样式
- [x] Task 1.3: 验证构建通过 — `npm run build` 无报错

### Verification

- [ ] Case 卡片不显示 FWR
- [ ] 客户名带颜色加粗
- [ ] 构建通过

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | Case 卡片不再显示 FWR SLA 徽章 | Visual | Navigate to Dashboard → screenshot → verify no "FWR" label visible in case cards |
| 2 | 客户名称使用正确的 Customer 字段 | Skip | Backend-only data field, no UI change needed — already confirmed correct |
| 3 | 客户名称使用 accent 颜色 + font-semibold 加粗 | Visual | Navigate to Dashboard → screenshot → verify customer name is visually distinct with color and bold styling |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Post-Implementation Checklist

- [ ] 关联 Issue JSON 状态已更新
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
