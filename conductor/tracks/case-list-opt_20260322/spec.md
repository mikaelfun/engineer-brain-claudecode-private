# Specification: Dashboard Case 列表优化：隐藏 FWR、客户名突出显示

**Track ID:** case-list-opt_20260322
**Type:** Bug
**Created:** 2026-03-22
**Status:** Draft

## Summary

Dashboard Case 列表的三个 UI 优化：移除 FWR SLA 指标显示、确认客户名使用正确的 Customer 字段、客户名字体突出并带颜色方便快速识别。

## Context

Dashboard Case 列表页（`Dashboard.tsx`）以卡片形式展示活跃 Case，每张卡包含 Case 编号、标题、客户名、SLA 状态等信息。当前存在三个问题需要修复。

## Problem Description

1. **FWR 不需要显示**：Case 卡片右侧显示了 IR 和 FWR 两个 SLA 徽章，但 FWR（First Within Response）对日常工作不重要，需要移除以简化界面。
2. **客户名称字段确认**：当前 `c.customer` 来源于 `case-info.md` 中 `| Customer | ... |` 行，由 `fetch-case-snapshot.ps1` 从 D365 `_customerid_value@OData.Community.Display.V1.FormattedValue` 写入。数据源正确，但需确认前端渲染无误。
3. **客户名称不够突出**：当前客户名使用 `text-secondary` 灰色文字，与 assignedTo、caseAge 混在一起，难以快速识别。需要用独立颜色 + 加粗字体突出显示。

## Acceptance Criteria

- [ ] Case 卡片不再显示 FWR SLA 徽章（IR 和 Health Score 保留）
- [ ] 客户名称（c.customer）使用正确的 Customer 字段（已确认正确，保持不变）
- [ ] 客户名称使用 accent 颜色（如 `--accent-blue` 或 `--accent-cyan`）+ `font-semibold` 加粗，在列表中一眼可见

## Dependencies

None

## Out of Scope

- 修改后端 API 或 case-info.md 解析逻辑
- 修改 FWR 数据本身（只是前端不显示）
- 其他 Case 列表结构变更

## Technical Notes

修改范围仅限 `dashboard/web/src/pages/Dashboard.tsx`：
- 删除 lines 295-298（FWR `<div>` 块）
- 将 line 231 的 `{c.customer}` 包裹在 `<span>` 中，添加 `font-semibold` + accent 颜色样式

---

_Generated by Conductor. Review and edit as needed._
