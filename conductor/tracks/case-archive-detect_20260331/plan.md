# Implementation Plan: Patrol/Casework Case Archive & Transfer Detection

**Track ID:** case-archive-detect_20260331
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-31
**Status:** [x] Complete

## Overview

分 3 个阶段实现：先创建 D365 closed case 查询脚本，然后在 patrol 中集成归档检测逻辑，最后在 casework 中加入单 case 检测。

## Phase 1: D365 Closed Case 查询脚本

创建查询 D365 已关闭 case 的 PowerShell 脚本，复用 `list-active-cases.ps1` 的 OData/FetchXML 模式。

### Tasks

- [x] Task 1.1: 创建 `list-closed-cases.ps1`，使用 FetchXML 查询 `statecode ne 0`（Resolved + Cancelled），按 `msdfm_assignedto` 或 `ownerid` 过滤当前工程师，支持 `-OutputJson` 输出
- [x] Task 1.2: 创建 `detect-case-status.ps1` — 归档检测主脚本，接受 case number 列表，对比 active list 和 closed list，输出每个 case 的判定结果（active / archived / transferred），支持 closure 邮件辅证（读 emails.md 最后一封工程师发出的邮件，检查是否包含 closure 关键词如 "Problem Solved"、"resolved"、"closure" 等）

### Verification

- [ ] 手动运行 `list-closed-cases.ps1 -OutputJson`，确认返回 JSON 格式的 closed cases
- [ ] 手动运行 `detect-case-status.ps1`，验证对已知 archived case 的判定正确

## Phase 2: Patrol 集成归档检测

在 patrol 流程中插入归档检测步骤，在获取 active case 列表后、开始 casework 前执行。

### Tasks

- [x] Task 2.1: 在 patrol SKILL.md 步骤 2 和步骤 3 之间插入新步骤 "2.5: 归档检测"：获取 active list 后，扫描 `cases/active/` 目录，找出不在 active list 中的 case numbers，调用 `detect-case-status.ps1` 判定
- [x] Task 2.2: 实现文件归档逻辑：确保 `archived/` 和 `transfer/` 目录存在，使用 `mv` 移动 case 目录，移动前记录日志到 `cases/archive-log.jsonl`（JSON Lines 格式：`{"timestamp","caseNumber","reason","from","to"}`）
- [x] Task 2.3: 归档后的 case 从 patrol 的待处理列表中排除，不再 spawn casework

### Verification

- [ ] 模拟场景：在 `cases/active/` 中创建一个假 case 目录（不在 D365 active list 中），运行 patrol 检测逻辑，验证能正确识别并移动

## Phase 3: Casework 单 Case 检测

在 casework 流程中加入当前 case 的归档/转移检测，data-refresh 后立即检查。

### Tasks

- [x] Task 3.1: 在 casework SKILL.md 的 data-refresh 步骤完成后、compliance-check 之前，增加 "归档检测" 步骤：调用 `detect-case-status.ps1` 检查当前 case
- [x] Task 3.2: 如果检测到 case 已 archived/transferred：记录日志，移动目录，输出提示信息，提前终止 casework 流程（不继续 compliance/status/troubleshoot 等后续步骤）

### Verification

- [ ] 确认 casework 对 active case 正常执行不受影响
- [ ] 确认 casework 对已归档 case 能正确检测并提前终止

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | list-closed-cases.ps1 查询 D365 closed cases | E2E | 运行脚本 -OutputJson → 验证返回 JSON 数组，包含 ticketnumber/statecode/statuscode 字段 |
| 2 | patrol 对比本地 active/ 与 D365 active list | E2E | Backup active/ → 创建假 case 目录 → 运行 patrol 归档检测逻辑 → 验证假 case 被识别 → restore |
| 3 | closed case 分流：archived vs transfer | E2E | 构造测试数据：一个在 closed list 中的 case + 一个两边都找不到的 case → 运行 detect-case-status.ps1 → 验证分别判定为 archived 和 transferred |
| 4 | closure 邮件辅证 | E2E | 构造包含 closure 关键词的 emails.md → 运行检测 → 验证辅证结果出现在输出中 |
| 5 | 自动移动 case 目录 | E2E | Backup → 构造待归档 case → 运行归档逻辑 → 验证目录已从 active/ 移入 archived/ 或 transfer/ → restore |
| 6 | casework 单 case 检测与提前终止 | Skip | 需运行完整 casework（D365 交互），跳过自动化 |
| 7 | 移动前记录日志 | E2E | 运行归档逻辑 → 验证 archive-log.jsonl 包含正确的 timestamp/caseNumber/reason/from/to 字段 |

**Test Type Legend:**
- **E2E** — Backup data → run actual workflow/script → verify file outputs + API + UI → restore
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — D365 write/execute operations only (must justify)

## Post-Implementation Checklist

- [ ] 单元测试文件已创建/更新并通过
- [ ] 关联 Issue JSON 状态已更新为 `implemented`（非 `done`，需 verify 后才可标 `done`）
- [ ] Track metadata.json 已更新
- [ ] tracks.md 状态标记已更新

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
