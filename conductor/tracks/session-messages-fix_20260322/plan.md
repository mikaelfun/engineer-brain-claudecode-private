# Implementation Plan: Fix Agent Monitor Session Messages Not Displaying

**Track ID:** session-messages-fix_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Fix three backend issues preventing AgentMonitor session detail panel from showing messages: content extraction, type mapping, and chat route persistence.

## Phase 1: Fix formatMessageForSSE Content Extraction

Fix the `formatMessageForSSE` function to properly extract text from SDK structured content (array of content blocks).

### Tasks

- [x] Task 1.1: Update `formatMessageForSSE` in `sse-helpers.ts` — handle `content` as array of `{type: 'text', text: string}` blocks; extract and join text; also add a `getPersistedMessageType` helper that maps SDK types to frontend types

### Verification

- [x] TypeScript compiles without errors

## Phase 2: Fix Persistence and Chat Route

Fix the full-process route to use correct frontend types for persistence, and add persistence + correct event dispatch to the chat route.

### Tasks

- [x] Task 2.1: Fix full-process route in `case-routes.ts` — use the new `getPersistedMessageType()` for the `type` field in `appendSessionMessage` calls; use the extracted content from `formatMessageForSSE` properly
- [x] Task 2.2: Fix chat route in `case-routes.ts` — use `getSSEEventType(message)` instead of hardcoded `case-session-thinking`; add `appendSessionMessage()` calls to persist chat messages

### Verification

- [x] TypeScript compiles without errors
- [x] Existing tests still pass

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | `formatMessageForSSE` correctly extracts text from SDK structured content (ar... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | Persisted messages use frontend-compatible types (`thinking`, `tool-call`, `t... | Visual | Navigate to page → screenshot → verify visual matches spec |
| 3 | Chat route persists messages via `appendSessionMessage()` | Interaction | Navigate to page → type in input → submit → verify response appears |
| 4 | Chat route uses `getSSEEventType()` for correct event dispatch | Interaction | Navigate to page → type in input → submit → verify response appears |
| 5 | TypeScript compiles without errors | Skip | Covered by unit tests |
| 6 | Existing tests still pass | Skip | Covered by unit tests |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

## Phase 3: Post-Implementation Checklist

- [x] ISS-051 JSON status updated
- [x] Track metadata.json updated
- [x] tracks.md status mark updated

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
