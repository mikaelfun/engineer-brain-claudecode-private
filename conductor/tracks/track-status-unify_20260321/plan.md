# Implementation Plan: Issue 与 Track 状态统一

**Track ID:** track-status-unify_20260321
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

分 3 个阶段：先扩展类型和 conductor-reader，然后加派生逻辑到 GET 路由，最后改写操作去掉 issue JSON 双写。

## Phase 1: 类型扩展 + conductor-reader 增强

扩展 TrackMetadata 类型，让 conductor-reader 支持读写新字段。

### Tasks

- [x] Task 1.1: 在 `dashboard/src/types/index.ts` 中扩展 `TrackMetadata` 接口，新增 `issueId?: string` 和 `verification?: TrackVerification`，新增 `TrackVerification` 接口
- [x] Task 1.2: 在 `dashboard/src/services/conductor-reader.ts` 中新增 `deriveIssueStatus(track: TrackMetadata): { status: IssueStatus; verifyResult?: IssueVerifyResult }` 函数，实现 track → issue 状态映射
- [x] Task 1.3: 在 `dashboard/src/services/conductor-reader.ts` 中新增 `enrichIssueFromTrack(issue: Issue): Issue` 函数，对有 trackId 的 issue 从 track metadata 派生 status 和 verifyResult

### Verification

- [x] TypeScript 编译通过
- [x] deriveIssueStatus 映射逻辑覆盖所有 track status + verification 组合

## Phase 2: GET 路由 — 读取时派生

修改 issues.ts 的 GET 路由，返回 issue 时自动从 track metadata 派生状态。

### Tasks

- [x] Task 2.1: 修改 `GET /api/issues` 路由，listIssues 结果中有 trackId 的 issue 调用 `enrichIssueFromTrack()` 填充
- [x] Task 2.2: 修改 `GET /api/issues/:id` 路由，同样对有 trackId 的 issue 调用 `enrichIssueFromTrack()`

### Verification

- [x] GET 接口返回的 issue status 与 track metadata 一致
- [x] 无 trackId 的 issue 不受影响

## Phase 3: 写操作 — 单写 track metadata

改造各写操作端点，只写 track metadata，不再双写 issue JSON 的 status。

### Tasks

- [x] Task 3.1: 修改 `POST /:id/create-track` (finalizeTrackCreation)，track 创建完成后在 track metadata 中写入 `issueId`，issue JSON 只写 `trackId`（不再写 status='tracked'，因为会从 track 派生）
- [x] Task 3.2: 修改 `POST /:id/start-implement`，只调 `updateTrackMetadata(trackId, { status: 'in_progress' })`，不再 `updateIssue(id, { status: 'in-progress' })`
- [x] Task 3.3: 修改 implement 完成回调（implementAsync 内），只调 `updateTrackMetadata(trackId, { status: 'complete' })`，不再 `updateIssue(id, { status: 'implemented' })`
- [x] Task 3.4: 修改 `POST /:id/verify`，验证结果写入 `updateTrackMetadata(trackId, { verification: { status, method: 'auto', result } })`，不再 `updateIssue(id, { verifyResult })`
- [x] Task 3.5: 修改 `POST /:id/mark-done`，调 `updateTrackMetadata(trackId, { verification: { status: 'skipped', method: 'manual' } })` + `updateTracksMdStatus(trackId, '[x]')`，不再双写 issue status
- [x] Task 3.6: 修改 `POST /:id/reopen`，清 track metadata 的 verification 字段（`updateTrackMetadata(trackId, { verification: undefined })`），恢复 track status 为之前的值（complete → 实际看 tasks），不再写 issue status

### Verification

- [x] implement/verify/mark-done/reopen 后 GET 返回正确的派生状态
- [x] issue JSON 中有 trackId 的 issue 不再出现 status 双写

## Final Verification

- [x] All acceptance criteria met
- [x] TypeScript 编译无错误
- [x] 前端页面行为不变（status badge, verify result, action buttons 正常）
- [x] 无 trackId 的 issue 全流程正常

## Verification Plan

| # | Acceptance Criterion | Test Type | Test Steps |
|---|---------------------|-----------|------------|
| 1 | TrackMetadata 类型新增 `issueId?: string` 和 `verification?: TrackVerification` 字段 | Visual | Navigate to page → screenshot → verify visual matches spec |
| 2 | `GET /api/issues` 返回的 issue，有 trackId 时其 status 和 verifyResult 从 track metada... | API | Call the relevant endpoint → verify response shape and status code |
| 3 | `POST /:id/start-implement` 只写 track metadata（status: in_progress），不写 issue s... | API | Call the relevant endpoint → verify response shape and status code |
| 4 | `POST /:id/verify` 完成后把 verifyResult 写入 track metadata.verification，不写 issue ... | API | Call the relevant endpoint → verify response shape and status code |
| 5 | `POST /:id/mark-done` 写 track metadata（verification: { status: 'skipped', met... | API | Call the relevant endpoint → verify response shape and status code |
| 6 | `POST /:id/reopen` 清 track metadata 的 verification 字段，不写 issue status | API | Call the relevant endpoint → verify response shape and status code |
| 7 | `POST /:id/create-track` 完成后在 track metadata 写入 issueId | API | Call the relevant endpoint → verify response shape and status code |
| 8 | 前端无感知变化 — 返回的 issue 对象结构不变（status + verifyResult 由后端派生填充） | Visual | Navigate to page → screenshot → verify visual matches spec |
| 9 | 无 trackId 的 issue（pending/tracking）仍用 issue JSON 自身的 status | Skip | Covered by unit tests |

**Test Type Legend:**
- **Interaction** — Playwright clicks, form fills, state assertions
- **Visual** — Navigate + screenshot + visual inspection
- **API** — curl/fetch endpoint + assert response
- **Skip** — Backend-only or covered by unit tests

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
