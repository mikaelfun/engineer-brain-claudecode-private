# VM Azure Files 挂载与连接 — 排查速查

**来源数**: 1 (AW) | **条目**: 65 | **21V**: 全部
**最后更新**: 2026-04-24

## 症状速查

| # | 症状 | 根因 | 方案 | 分数 | 来源 |
|---|------|------|------|------|------|
| 1 | Azure Files SMB mount via UNC path fails with STATUS_OBJECT_NAME_NOT_FOUND instead of prompting for  | Azure Files Storage service bug (starting 8/14/2023) changed SMB Session Setup e | Provide credentials explicitly: (1) Use net use command with credential paramete | 🔵 7.5 | AW |
| 2 | Cannot mount Azure File Share from on-premises or different Azure region. Mount fails on Windows Ser | Mounting Azure File Share outside hosting region requires SMB 3.0 with encryptio | Upgrade to OS supporting SMB 3.0+: Windows Server 2012+, Windows 8.1+, Linux ker | 🔵 7.5 | AW |
| 3 | Access Denied error when mounting Azure File Share using storage account key after SMB security sett | Storage account SMB security settings configured to allow only Kerberos, disabli | Update SMB security settings: PowerShell Update-AzStorageFileServiceProperty -Sm | 🔵 7.5 | AW |
| 4 | Azure Files mount fails with private endpoint configured; DNS resolves storage account to public IP  | Private DNS zone (privatelink.file.core.windows.net) not associated with the pri | 1) Verify private DNS zone (privatelink.file.core.windows.net) is associated wit | 🔵 7.5 | AW |
| 5 | Azure Files AD DS Kerberos authentication fails when mounting using privatelink subdomain URL (*.pri | Kerberos authentication requires the standard FQDN (*.file.core.windows.net) for | Always mount using the standard FQDN: net use * \\<storage-account>.file.core.wi | 🔵 7.5 | AW |
| 6 | Azure SMB file share mounted on Linux VM fails with unknown error 524 and CIFS STATUS_LOGON_FAILURE; | Stale connection to storage account - newer connection attempts reuse an already | Add nosharesock option to FSTAB entry: //sa.file.core.windows.net/share /mnt/dir | 🔵 7.5 | AW |
| 7 | Azure File Share mounted via PowerShell (New-PSDrive) is accessible in PowerShell session but return | New-PSDrive command was used without the -Persist option, creating a session-onl | Mount with -Persist flag: New-PSDrive -Name Z -PSProvider FileSystem -Root '\\<s | 🔵 7.5 | AW |
| 8 | Mount of Azure File Share on RHEL 8.2 fails with: mount: bad option; for several filesystems (e.g. n | cifs-utils RPM package is not installed by default on RHEL 8.2, so /sbin/mount.c | Install cifs-utils package: yum install cifs-utils. Verify installation: ls -l / | 🔵 7.5 | AW |
| 9 | Azure File Share mount error 1219: Multiple connections to a server or shared resource by the same u | Open file handle on a previously mounted file share that needs to be closed | Wait for auto-resolution (handle timeout). Check for open file handles and close | 🔵 7.5 | AW |
| 10 | System error 64 when mounting Azure File Share: The specified network name is no longer available. P | Proxy or NAT device in customer local network blocking connection from on-premis | Add Storage Account IP/range in proxy or NAT device. Run ipconfig /flushdns and  | 🔵 7.5 | AW |
| 11 | System error 64 mounting Azure File Share; SMB negotiate met with TCP RST from storage IP but TTL is | Client-side network element (firewall) intercepting and sending TCP RST with dif | Confirm suspicious TTL in Wireshark (Windows TTL=128; 64 indicates interception) | 🔵 7.5 | AW |
| 12 | Azure Files AD DS auth mount fails with Kerberos STATUS_ACCOUNT_DISABLED; klist fails with 0xc000018 | Storage account computer object in Active Directory is disabled or accidentally  | Open AD Users and Computers, locate and enable the storage account computer obje | 🔵 7.5 | AW |
| 13 | Cannot mount Azure file share on macOS via Finder when storage account key contains '/' special char | macOS Finder does not properly handle forward slash '/' character in storage acc | Mount via Terminal in 2-stage process: (1) mkdir mount_point, (2) mount_smbfs // | 🔵 7.5 | AW |
| 14 | Cannot mount/map Azure File Share drive to macOS. Connection fails when using Finder and storage acc | macOS Finder does not properly handle the forward slash (/) character in the sto | Mount in 2-stage process via Terminal: 1. Create mount point (mkdir share_name). | 🔵 7.5 | AW |
| 15 | Azure Files AAD Kerberos mount prompts for credentials with error: The SAM database on the Windows S | Storage account AD object is missing or disabled in Active Directory. | 1) Run Get-AzStorageAccountADObject to verify object exists. 2) If missing, rejo | 🔵 7.5 | AW |
| 16 | Access Denied when trying to update NTFS permissions on Azure File Share via identity-based authenti | Customer is using cached AD credentials instead of Storage Account Access Key wh | 1) Drop existing mount. 2) Clear cached credentials via Windows Credential Manag | 🔵 7.5 | AW |
| 17 | System error 5 'Access is denied' when mounting Azure File Share due to SMB cipher order mismatch (W | Windows 11/Server 2022 default SMB cipher order (e.g. AES-128-CCM first) conflic | Change cipher order on client: Set-SmbClientConfiguration -EncryptionCiphers 'AE | 🔵 7.5 | AW |
| 18 | Access denied (0xc0000022) when mounting Azure File Share - XDS log shows 'Account does not list SMB | Client attempting to connect with SMB version not in storage account allowed SMB | Configure client SMB version via registry: HKLM\SYSTEM\CurrentControlSet\Service | 🔵 7.5 | AW |
| 19 | Access denied when mounting Azure File Share with Kerberos - wrong encryption type (e.g. RC4 when on | Kerberos ticket encrypted with unsupported encryption type for the storage accou | Use correct encryption type or allow required type at storage account level. See | 🔵 7.5 | AW |
| 20 | Error 1396 'The target account name is incorrect' / KRB_AP_ERR_MODIFIED when mounting Azure File Sha | Storage account identity password in AD does not match either kerb1 or kerb2 key | 1) Verify: Test-AzStorageAccountADObjectPasswordIsKerbKey. 2) If mismatch, rotat | 🔵 7.5 | AW |
| 21 | Error 1396 with AAD DS storage account - mount fails after SRP automatic Kerberos key rotation (~30  | Customer followed AD DS steps to manually create objects in AAD DS domain. SRP r | 1) Verify AAD DS in ASC Resource Explorer. 2) Check setspn -Q - if object not in | 🔵 7.5 | AW |
| 22 | Linux mount error(115): Operation now in progress, or mount error: could not resolve address <Storag | Linux client unable to communicate with Azure Files server due to DNS resolution | 1) Verify Linux and CIFS client support SMB 2.1+. 2) Ensure VM is in same region | 🔵 7.5 | AW |
| 23 | Linux CIFS mount returns STATUS_LOGON_FAILURE (error -13) on existing Azure Files SMB mounts. dmesg  | Storage account migrated to a different storage cluster (load balancing or custo | Clear kernel DNS resolver cache: 1) grep .dns_resolver /proc/keys to get key ID. | 🔵 7.5 | AW |
| 24 | System Error 1326 when mounting AD-joined Azure file share using Storage Account Access Keys on Wind | NTLM authentication is disabled on the AD-joined Storage Account (only Kerberos  | Enable NTLMv2 authentication: Update-AzStorageFileServiceProperty -ResourceGroup | 🔵 7.5 | AW |
| 25 | Azure Files STATUS_LOGON_FAILURE / Send error in SessSetup = -13 on Linux after storage account migr | Linux kernel DNS resolver bug: after storage account cluster migration or geo-fa | Workaround: 1) Clear kernel DNS resolver cache: sudo keyctl clear $((16#$(grep . | 🔵 7.5 | AW |
| 26 | Azure File Share access fails with Resource temporarily unavailable or mount error(11). STATUS_INSUF | Client app opens excessive file handles without releasing, exceeding 256000 max  | Temp: reboot VM. Permanent: adjust app to release idle handles. Diagnose: ASC Fi | 🔵 7.5 | AW |
| 27 | Azure Files SMB mount fails with managed identity - smbOAuthSettings.isSmbOAuthEnabled not set to tr | Managed Identity for SMB feature is not enabled on the Storage account; smbOAuth | Enable Managed Identity for SMB on the Storage account. Verify via Xportal that  | 🔵 7.5 | AW |
| 28 | Missing Kerberos Service Ticket when mounting Azure Files SMB with Entra-Only Kerberos; klist shows  | Admin Consent not granted on Entra application for Azure Files SMB authenticatio | 1) Grant Admin Consent on the Entra app registration. 2) Enable client setting t | 🔵 7.5 | AW |
| 29 | SMB Multichannel not triggered on Azure Premium File Share after enabling the feature. Get-SmbMultic | After any changes to SMB client or storage account multichannel config, must unm | Unmount, wait 60 seconds, remount. Generate IO load (robocopy /MT or diskspd). V | 🔵 7.5 | AW |
| 30 | Slow performance after mounting Azure file share on Windows Server 2012 R2 or Windows 8.1. | Known OS-level issue in Windows Server 2012 R2 and Windows 8.1 affecting SMB fil | Apply the April 2014 cumulative update for Windows Server 2012 R2 and Windows 8. | 🔵 6.5 | AW |
| 31 | Network drive mapping fails when using Microsoft Entra ID authentication for Azure Files. Mount comm | Stale/cached credentials from previous authentication methods conflict with new  | 1) List stored credentials: cmdkey /list; 2) Remove stale credential: cmdkey /de | 🔵 6.5 | AW |
| 32 | Azure File Share mount fails when accessing from on-premises or a different Azure region. Client can | Client OS only supports SMB 2.1 (e.g., Windows Server 2008 R2, Windows 7, or Lin | Upgrade client OS to one supporting SMB 3.0 with encryption: Windows Server 2012 | 🔵 6.5 | AW |
| 33 | Azure File Share mount fails on Linux with DNS lookup failure for Azure Files endpoint. | Technologies like Mesosphere manipulate DNS configuration, causing DNS lookup of | Mount directly via IP address, or add DNS name and IP mapping into /etc/hosts to | 🔵 6.5 | AW |
| 34 | Azure File Share mount fails on Linux during system boot. The fstab entry for the file share fails t | Entries in /etc/fstab are processed before networking is enabled, causing mount  | Add the _netdev option to the Azure File Share entry in /etc/fstab so it is not  | 🔵 6.5 | AW |
| 35 | Access Denied error when trying to mount Azure File Share using storage account key (NTLMv2 authenti | Storage account SMB security settings restrict authentication methods (e.g., onl | Check and adjust SMB security settings: PowerShell Update-AzStorageFileServicePr | 🔵 6.5 | AW |
| 36 | File Explorer tab with Azure File Share mount (mapped drive via GPO) closes unexpectedly every ~90 m | GPO Drive Map policy configured with Replace action causes the mapped drive to d | Change the GPO Drive Map action from Replace to Update in New Drive Properties d | 🔵 6.5 | AW |
| 37 | Azure SMB file share mounted on Linux VM suddenly fails with unknown error 524; kernel log shows CIF | Azure storage stamp migration caused the mounted file share to become inaccessib | Verify storage stamp migration using the LosingMountafterStampMigration TSG (wik | 🔵 6.5 | AW |
| 38 | Application or service cannot connect to mounted Azure Files drive. Mapped drive not visible when ap | Mapped drives are mounted per user. If the application/service runs under a diff | Mount the Azure File share from the same user context as the application using P | 🔵 6.5 | AW |
| 39 | Error System 86 "The specified network password is not correct" when mounting Azure File Share confi | Server closed the authentication connection because no Kerberos tickets were pre | Remove the stale entry from Windows Credential Manager for the storage account.  | 🔵 6.5 | AW |
| 40 | Unable to mount Azure File Share with Microsoft Entra Kerberos. Entra sign-in logs show error AADSTS | "Assignment required" is enabled on the enterprise application representing the  | Do not select "Assignment required" for the Microsoft Entra application represen | 🔵 6.5 | AW |
| 41 | Unable to mount Azure File Share with Entra Kerberos. Entra sign-in logs show errorCode 50158: "Exte | External security challenge (terms of use, third-party MFA provider, or conditio | Disable MFA on the Microsoft Entra app representing the storage account. Create  | 🔵 6.5 | AW |
| 42 | User without proper RBAC/NTFS permissions can unexpectedly mount Azure File Share; Wireshark shows A | Storage account key credentials cached on the client machine from a previous ses | Delete cached credentials via Windows Credential Manager (Control Panel > Creden | 🔵 6.5 | AW |
| 43 | 'File already exists' error when copying files to mounted Azure file share using Windows File Explor | Files have NTFS Alternate Data Streams which Azure File Shares do not support | Remove alternate data streams before copying: Remove-Item <filepath> -Stream *;  | 🔵 6.5 | AW |
| 44 | Mapping network drive with net use fails with error: The option /StorageAccountKey== is unknown, whe | Command Interpreter (cmd.exe) interprets the forward slash (/) in the storage ac | Use PowerShell instead of cmd.exe: New-SmbMapping -LocalPath z: -RemotePath \\st | 🔵 6.5 | AW |
| 45 | Copying files into mounted Azure File Share using Windows File Explorer shows File already exists or | Source files have Alternate Data Streams (ADS). Azure File Shares do not support | Check for ADS with Get-Item <file> -Stream *. If streams beyond :$DATA exist, re | 🔵 6.5 | AW |
| 46 | Azure Files AAD Kerberos mount prompts for credentials repeatedly. klist returns error 0x80090342 SE | RC4 encryption is not enabled on the customer domain, causing Kerberos ticket re | Enable RC4 encryption on the domain. See related TSG: 1396 - The target account  | 🔵 6.5 | AW |
| 47 | Linux mount error(13): Permission denied when mounting Azure Files SMB share | Wrong storage account username or password used in mount command | Verify the username (must be the storage account name) and password (must be a v | 🔵 6.5 | AW |
| 48 | Linux mount error(2): mount(2) error system call failed: No route to host when mounting Azure Files  | cifs-utils package (and its dependency keyutils) not installed on Linux. These p | Install cifs-utils using the distro package manager: Ubuntu: sudo apt install ci | 🔵 6.5 | AW |
| 49 | The network path was not found (error 0x80070035) when mounting Azure file share on Windows, even th | TCP/IP NetBIOS Helper (lmhosts) service is disabled. Windows 2019 and older depe | Enable and start the TCP/IP NetBIOS Helper (lmhosts) service. Note: disabling lm | 🔵 6.5 | AW |
| 50 | Error: Multiple connections to a server or shared resource by the same user, using more than one use | Windows does not allow multiple connections to the same server with different cr | Use cmdkey to store credentials with AZURE domain prefix: cmdkey /add:<StorageAc | 🔵 6.5 | AW |
| 51 | Azure Files mount error(115) "Operation now in progress" or "could not resolve address: Unknown erro | Linux client cannot communicate with Azure Files server due to DNS resolution fa | 1) Verify Linux/CIFS client supports SMB 2.1+; 2) Ensure VM is in same region as | 🔵 6.5 | AW |
| 52 | Azure Files mount error(13) "Permission denied" on Linux when mounting SMB share | Wrong storage account username or password used in mount command | Verify username is the storage account name and password is the correct storage  | 🔵 6.5 | AW |
| 53 | Azure Files mount error(13) Permission denied on Linux when storage account uses CMK with Managed Se | Managed Service Identity (MSI) is no longer working correctly for the storage ac | Step 1: Renew MSI - Set-AzStorageAccount -IdentityType None then Set-AzStorageAc | 🔵 6.5 | AW |
| 54 | Azure Files mount error(2) "No route to host" on Linux when mounting SMB share with -t cifs | cifs-utils package (and its dependency keyutils) not installed on the Linux VM.  | Install cifs-utils using the distro package manager: Ubuntu: sudo apt install ci | 🔵 6.5 | AW |
| 55 | Net Use or New-SmbMapping succeeds but mapped drive letter does not appear in Windows File Explorer  | Drive mapped from an elevated (administrator) command prompt. Windows mapped dri | Resolution 1: Mount the share from a non-administrator command line. Resolution  | 🔵 6.5 | AW |
| 56 | "The network path was not found" (error 0x80070035) when mounting Azure File share on Windows, despi | TCP/IP NetBIOS Helper (lmhosts) service is disabled. Windows Server 2019 and old | Enable and start the TCP/IP NetBIOS Helper (lmhosts) service. Note: Disabling lm | 🔵 6.5 | AW |
| 57 | "Multiple connections to a server or shared resource by the same user, using more than one user name | Windows prevents multiple concurrent connections to the same server with differe | Use cmdkey to add credentials with AZURE\ domain prefix: cmdkey /add:StorageAcco | 🔵 6.5 | AW |
| 58 | Error The option ID == is unknown when mapping Azure File Share with net use, storage account key co | cmd.exe interprets forward slash in storage account key as command line option s | Use PowerShell New-SmbMapping instead of cmd net use to map the drive. | 🔵 6.5 | AW |
| 59 | Opening Excel files from mounted Azure File Share or UNC path is significantly slower than locally | Multiple causes: network latency, xlsx format overhead, Excel unsupported ops, a | Use XLS/XLSB, disable Protected View, add storage FQDN to Trusted Sites, verify  | 🔵 6.5 | AW |
| 60 | System Error 5 Access Denied on Windows or mount error(13) Permission denied on Linux when mounting  | Cross-region: client lacks SMB 3.0 encryption (Win2008R2=SMB2.1, Linux<4.11). Sa | Cross-region: use Win Server 2012+ or Linux kernel 4.11+. Same-region: disable S | 🔵 6.5 | AW |
| 61 | AzFilesSMBMIClient.exe refresh command fails when retrieving OAuth token for managed identity SMB mo | Storage Account network ACLs do not allow network connectivity from the client V | Ensure appropriate firewall rules are configured on the storage account to allow | 🔵 6.5 | AW |
| 62 | Access Denied when mounting Azure Files SMB with Entra-Only Kerberos cloud-only identities | Default Share Permissions not configured on the storage account; RBAC is not sup | Configure Default Share Permissions on the storage account. In non-canary region | 🔵 6.5 | AW |
| 63 | 20-30 second delay when browsing Azure File shares configured with DFS namespace showing contacting  | Windows client redirector sequentially tries SMB (445) then NetBIOS (139) then W | Disable the WebClient service on the client machine. Azure Files only supports S | 🔵 6.5 | AW |
| 64 | Windows 7 or Windows Server 2008 R2 client fails to mount Azure File Share from external location wi | SMB 2.1 (max protocol on Win7/2008R2) does not support encryption. Azure Files r | Use a client OS supporting SMB 3.0+ (Windows 8/Server 2012+) for external Azure  | 🟡 4.0 | AW |
| 65 | Windows 7 or Windows Server 2008 R2 client fails to mount Azure File Share with Access Denied even f | Secure Transfer Required forces encrypted connections. SMB 2.1 on Win7/2008R2 ca | Disable Secure Transfer Required on the storage account (if security policy allo | 🟡 4.0 | AW |

## 快速排查路径

1. **Azure Files SMB mount via UNC path fails with STATUS_OBJECT_NAME_NOT_FOUND inste**
   - 根因: Azure Files Storage service bug (starting 8/14/2023) changed SMB Session Setup error code from STATUS_LOGON_FAILURE to S
   - 方案: Provide credentials explicitly: (1) Use net use command with credential parameters, or (2) Use cmdkey /add:<account>.file.core.windows.net /user:<acco
   - `[🔵 7.5 | AW]`

2. **Cannot mount Azure File Share from on-premises or different Azure region. Mount **
   - 根因: Mounting Azure File Share outside hosting region requires SMB 3.0 with encryption. SMB 2.1 clients and Linux kernels bef
   - 方案: Upgrade to OS supporting SMB 3.0+: Windows Server 2012+, Windows 8.1+, Linux kernel 4.11+. SMB 2.1 clients can only access shares from VMs in same Azu
   - `[🔵 7.5 | AW]`

3. **Access Denied error when mounting Azure File Share using storage account key aft**
   - 根因: Storage account SMB security settings configured to allow only Kerberos, disabling NTLMv2. Storage account key mounting 
   - 方案: Update SMB security settings: PowerShell Update-AzStorageFileServiceProperty -SmbAuthenticationMethod "NTLMv2;Kerberos". CLI: az storage account file-
   - `[🔵 7.5 | AW]`

4. **Azure Files mount fails with private endpoint configured; DNS resolves storage a**
   - 根因: Private DNS zone (privatelink.file.core.windows.net) not associated with the private endpoint, or virtual network link n
   - 方案: 1) Verify private DNS zone (privatelink.file.core.windows.net) is associated with the private endpoint. 2) Configure virtual network link from private
   - `[🔵 7.5 | AW]`

5. **Azure Files AD DS Kerberos authentication fails when mounting using privatelink **
   - 根因: Kerberos authentication requires the standard FQDN (*.file.core.windows.net) for SPN matching; privatelink subdomain or 
   - 方案: Always mount using the standard FQDN: net use * \\<storage-account>.file.core.windows.net\<share-name>. Never use the privatelink subdomain or private
   - `[🔵 7.5 | AW]`

