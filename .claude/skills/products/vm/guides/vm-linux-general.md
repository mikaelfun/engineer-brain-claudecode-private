# VM Linux OS 通用问题 — 排查速查

**来源数**: 4 (AW, KB, ML, ON) | **条目**: 33 | **21V**: 32/33
**最后更新**: 2026-04-24

## 症状速查

| # | 症状 | 根因 | 方案 | 分数 | 来源 |
|---|------|------|------|------|------|
| 1 | Linux VM sporadic app failures; dmesg shows Out of memory: Kill process; OOM Killer terminating proc | Memory pressure: swap not configured, NUMA/HugePages, app memory leak, or insuff | 1) Configure swap file. 2) Resize VM if workload peak. 3) Check NUMA/HugePages.  | 🟢 8 | ON |
| 2 | SLES 11 SP4 VM cannot enable ASR A2A replication; mobility service install fails with curl error 60  | Missing root CA certificates (VeriSign Class 3 G5, Baltimore CyberTrust Root, Di | 1) Check /var/log/azure/Microsoft.Azure.RecoveryServices.SiteRecovery.LinuxSLES1 | 🟢 8 | ON |
| 3 | Ubuntu 24.04 VM needs account lockout configuration: lock login for N minutes after N failed attempt | Ubuntu 24.04 defaults to pam_faillock (replacing older pam_tally2). Three PAM co | 1) Edit /etc/security/faillock.conf: deny=3, unlock_time=600, fail_interval=900, | 🟢 8 | ON |
| 4 | VM disappears from Update Management (UM) schedule update deployment preview panel. VM not showing i | The crond service on the Linux VM was disabled. The PerformRequiredConfiguration | Enable and start crond: systemctl enable crond; systemctl start crond. Then run: | 🟢 8 | ON |
| 5 | ASR V2A: VMware cloned Linux VM replication stuck at 0%; both original and cloned VM replication fai | Cloned VM inherits the same AgentGeneratedId (host ID generated by Mobility Agen | 1. Disable replication for both VMs in portal (Disable replication and delete).  | 🔵 7.5 | ON |
| 6 | Azure VM screenshot shows VM stuck on Hyper-V screen and not booting past the Hyper-V logo (Windows  | Multiple possible causes: (1) Windows bug check or guest OS issue preventing boo | Take multiple screenshots via ASC to confirm not reboot loop. Check ASC Insights | 🔵 7.5 | AW |
| 7 | VMs do not appear in Update Management 'schedule update deployment' preview list at subscription lev | The Linux VM is configured with a proxy that intercepts traffic to IMDS endpoint | Add no_proxy=169.254.169.254 to the Linux environment proxy configuration to byp | 🔵 7 | ON |
| 8 | Accelerated Networking not working or unavailable on DCsv3/DCdsv3 Linux VM | Azure Accelerated Networking (AN) is not supported on DCsv3/DCdsv3 Linux Virtual | This is a known limitation. Do not enable AN on DCsv3/DCdsv3 Linux VMs. If netwo | 🔵 7.0 | AW |
| 9 | Update Management online troubleshoot link (learn.microsoft.com) does not work on Mooncake for Linux | The troubleshoot endpoint referenced in public docs is not available in the Moon | Run troubleshooting from Azure Portal: VM > Updates > Troubleshoot. Alternativel | 🔵 7 | ON |
| 10 | apt update fails with Temporary failure resolving azure.archive.ubuntu.com | Custom DNS resolver not functioning correctly, or DNS server in different subnet | Switch to Azure default DNS to test. Verify custom DNS on port 53. If DNS in dif | 🔵 6.5 | ML |
| 11 | apt update fails with GPG signature error: NO_PUBKEY for third-party repository | Third-party repository added but its GPG public key was not imported. | Import GPG key using curl to /etc/apt/trusted.gpg.d/. Use gpg --dearmor to conve | 🔵 6.5 | ML |
| 12 | yum/dnf 404 Not Found for repomd.xml. Wrong releasever or EUS/non-EUS mismatch. | Incorrect releasever in /etc/yum/vars/ or /etc/dnf/vars/. EUS/non-EUS repo misma | EUS: set correct version lock. Non-EUS: clear releasever. OL: clear ociregion. C | 🔵 6.5 | ML |
| 13 | VM running SLES 12 SP5 no longer receives security updates or standard support after Oct 31 2024 (En | SUSE Linux Enterprise Server 12 SP5 reached end of standard support lifecycle on | Three options: (1) In-place upgrade to SLES 15 per SUSE upgrade guide; (2) Fresh | 🔵 6.5 | AW |
| 14 | Linux VM fails to boot due to XFS filesystem corruption. Serial log shows XFS Metadata CRC error. | XFS disk corruption. Uncommitted journal entries or metadata CRC errors. | xfs_repair -n to check, xfs_repair to fix. Journal error: try mount first. If fa | 🔵 6.5 | ML |
| 15 | Linux VM logs Out of memory: Kill process or invoked oom-killer - OOM Killer terminates processes | Both RAM and swap are exhausted; OOM Killer selects process with highest oom_sco | Identify high-memory process via top/ps. Check for memory leaks. Scale up VM siz | 🔵 6.5 | ML |
| 16 | You have a Ubuntu VMs behind an Azure Load Balancer (ALB), after upgrading the walinuxagent via a pa | This applies to only Ubuntu 14.04 / 16.04 / 17.04 VMs that have installed 2.2.16 | This has been resolved in Linux Agent, please upgrade using the package manager. | 🔵 6 | KB |
| 17 | Accelerated Networking fails to enable or does not work on Linux DCsv3/DCdsv3 VMs; live migration fa | DCsv3/DCdsv3 series does not support Accelerated Networking on Linux or Live Mig | Do not enable Accelerated Networking on Linux DCsv3/DCdsv3 VMs; plan for VM down | 🔵 6.0 | AW |
| 18 | Linux VM encounters page allocation failure (GFP_ATOMIC) indicating memory allocation failure under  | System runs out of available memory pages; can be caused by unexpected load spik | Diagnose with free/vmstat/top/htop/cat /proc/meminfo/buddyinfo. Scale up VM memo | 🔵 5.5 | ML |
| 19 | Cannot resize MBR partition for data disk larger than 2 TB using fdisk; partition stays at 2 TB maxi | MBR partition table has 32-bit sector address limitation (2^32 sectors), limitin | Convert partition from MBR to GPT using gdisk tool: backup data, unmount filesys | 🔵 5.5 | ML |
| 20 | SUSE Pacemaker does not start after SysRq-triggered reboot. SBD msgwait conflict with fast Azure VM  | SBD_DELAY_START=no causes conflict when VMs reboot faster than SBD msgwait timeo | Set SBD_DELAY_START=yes in /etc/sysconfig/sbd. May need to adjust sbd.service Ti | 🔵 5.5 | ML |
| 21 | SLES migration: IsADirectoryError Errno 21 /system-root/etc/pki/trust/anchors/temp in suse-migration | Unexpected directory in /etc/pki/trust/anchors breaks migration script. | mv /etc/pki/trust/anchors/temp to backup, install suse-migration-sle15-activatio | 🔵 5.5 | ML |
| 22 | rpm/yum BDB0113 Thread died in Berkeley DB. Cannot open Packages database. | Corrupted RPM database. Concurrent access, disk space, or abrupt termination. | rm -f /var/lib/rpm/__db.*; rpmdb_verify Packages; rpm -vv --rebuilddb. | 🔵 5.5 | ML |
| 23 | SLES 12 SP5->15 SP6: Signature verification failed for repomd.xml after intermediate SP3 migration | SUSE changed GPG signing key to RSA 4096 mid-2023, old systems lack new key. | Import new GPG key (3fa1d6ce-63c9481c): rpm --import repo-signing-key-sle-15.txt | 🔵 5.5 | ML |
| 24 | apt update fails with Unknown error executing apt-key and GPG signature verification errors | Permission issues on /etc/apt/trusted.gpg.d/*.gpg - not readable by _apt user. O | chmod 644 /etc/apt/trusted.gpg.d/*.gpg. Verify umask in /etc/login.defs is 0022. | 🔵 5.5 | ML |
| 25 | SLES 12 SP5->15 SP1 migration: Not ready to read within timeout for multiple repos | regionService dir moved from /var/lib to /usr/lib, DMS only looks under /var/lib | mkdir -p /var/lib/regionService/certs, cp certs from /usr/lib, update certLocati | 🔵 5.5 | ML |
| 26 | Allocation failure when deploying custom/gallery/marketplace Linux image. Cluster cannot support req | New VM request pinned to cluster that cannot support the VM size or has no avail | Retry with smaller VM size, or stop all VMs in availability set first then creat | 🔵 5.5 | ML |
| 27 | Azure fence agent fails in SUSE Pacemaker. Missing fence-agents-azure-arm package after Python 3.11  | SUSE rebuilt fence agent package for Python 3.11. Old package not compatible. | Install fence-agents-azure-arm package: zypper in fence-agents-azure-arm on all  | 🔵 5.5 | ML |
| 28 | yum/dnf dependency conflict. Package requires version X but providers cannot be installed. | Incompatible packages from different OS releases coexisting. | yum remove <old-pkg>. Or --allowerasing/--skip-broken/--nobest. | 🔵 5.5 | ML |
| 29 | Ubuntu 18.04 LTS VM no longer receives security/feature/maintenance updates after May 31, 2023 (end  | Canonical Ubuntu 18.04 LTS reached end of standard support on May 31, 2023. No m | Three options: (1) Upgrade to Ubuntu 20.04 LTS then to 22.04 LTS (no direct 18.0 | 🔵 5.5 | ML |
| 30 | apt update fails with does not have a Release file for armhf packages on x86_64 Azure VM | /etc/apt/sources.list contains ARM (armhf) architecture repos, invalid for x86_6 | Remove or comment out armhf repository lines from sources.list or sources.list.d | 🔵 5.5 | ML |
| 31 | SUSE VM: No configuration found for eth4 / eth0 No interface found - VM not accessible over network | Stale udev rules under /etc/udev/rules.d contain entries for old network interfa | Remove the files that contain entries for eth0 or eth1 under /etc/udev/rules.d | 🔵 5.5 | ML |
| 32 | Linux VM logs Failed to fork: Cannot allocate memory - systemd or applications cannot create new pro | Insufficient free memory to allocate initial process state for new fork. System  | Scale up VM memory, configure swap space, identify and fix memory-consuming proc | 🔵 5.5 | ML |
| 33 | CentOS VM: eth0 does not seem to be present / Device eth0 has different MAC address than expected /  | HWADDR directive in /etc/sysconfig/network-scripts/ifcfg-eth0 contains old MAC a | Remove the HWADDR directive from /etc/sysconfig/network-scripts/ifcfg-eth0 | 🔵 5.5 | ML |

## 快速排查路径

1. **Linux VM sporadic app failures; dmesg shows Out of memory: Kill process; OOM Kil**
   - 根因: Memory pressure: swap not configured, NUMA/HugePages, app memory leak, or insufficient VM size
   - 方案: 1) Configure swap file. 2) Resize VM if workload peak. 3) Check NUMA/HugePages. 4) Protect process: echo -17 > /proc/PID/oom_adj. 5) Disable overcommi
   - `[🟢 8 | ON]`

2. **SLES 11 SP4 VM cannot enable ASR A2A replication; mobility service install fails**
   - 根因: Missing root CA certificates (VeriSign Class 3 G5, Baltimore CyberTrust Root, DigiCert Global Root CA) in /etc/ssl/certs
   - 方案: 1) Check /var/log/azure/Microsoft.Azure.RecoveryServices.SiteRecovery.LinuxSLES11SP4/<ver>/CommandExecution.log and /var/log/AzureRcmCli.log for curl 
   - `[🟢 8 | ON]`

3. **Ubuntu 24.04 VM needs account lockout configuration: lock login for N minutes af**
   - 根因: Ubuntu 24.04 defaults to pam_faillock (replacing older pam_tally2). Three PAM config files need modification: /etc/secur
   - 方案: 1) Edit /etc/security/faillock.conf: deny=3, unlock_time=600, fail_interval=900, even_deny_root, root_unlock_time=900. 2) Edit /etc/pam.d/common-auth:
   - `[🟢 8 | ON]`

4. **VM disappears from Update Management (UM) schedule update deployment preview pan**
   - 根因: The crond service on the Linux VM was disabled. The PerformRequiredConfigurationChecks.py script (called every 15 minute
   - 方案: Enable and start crond: systemctl enable crond; systemctl start crond. Then run: sudo -u omsagent python /opt/microsoft/omsconfig/Scripts/PerformRequi
   - `[🟢 8 | ON]`

5. **ASR V2A: VMware cloned Linux VM replication stuck at 0%; both original and clone**
   - 根因: Cloned VM inherits the same AgentGeneratedId (host ID generated by Mobility Agent at install time) as the source VM; whe
   - 方案: 1. Disable replication for both VMs in portal (Disable replication and delete). 2. In CS MySQL, run query to verify host entries: select id,name,ipadd
   - `[🔵 7.5 | ON]`

